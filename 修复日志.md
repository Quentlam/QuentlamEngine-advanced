# 修复日志

> 记录每次修复的过程、内容及结果。

---

## 2026-05-29/30 — Release 构建修复批次

### 编译警告修复

**1. C4996 `strncpy` 安全警告（9处）— EditorLayer.cpp**
- 修复：将 9 处 `strncpy` 改为 `strncpy_s(dst, src, sizeof(dst))`
- 文件：`QL-Editor/src/EditorLayer.cpp`

**2. C4018 有符号/无符号不匹配（3处）— Renderer2D.cpp**
- 修复：将 `for` 循环的 `int` 变量改为 `uint32_t`
- 文件：`QuentlamEngine/src/Quentlam/Renderer/Renderer2D.cpp`

**3. C4244 `int64`→`uint32_t` 截断（2处）— Renderer2D.cpp**
- 修复：将 `static_cast<uint32_t>(pos.y * pos.y)` 改为 `static_cast<int>(pos.y * pos.y)` 以匹配 `uint32_t` 目标
- 文件：`QuentlamEngine/src/Quentlam/Renderer/Renderer2D.cpp`

---

### 功能修复

**4. TerrainComponent 反序列化缺少 Init()**
- 修复：在 `SceneSerializer::DeserializeEntity` 中，TerrainComponent 反序列化后调用 `terrainComp.Terrain->Init()`
- 文件：`QuentlamEngine/src/Quentlam/Scene/SceneSerializer.cpp`
- 同时添加了 `#include "Quentlam/Renderer/Terrain.h"`

**5. `Scene::CreateDefaultScene` 重复 AddComponent 断言**
- 原因：`CreateEntity()` 已自动添加 `TransformComponent`，但 `CreateDefaultScene()` 中 `terrainEnt.AddComponent<TransformComponent>()` 重复调用导致断言失败
- 修复：将 `AddComponent<TransformComponent>()` 改为 `GetComponent<TransformComponent>()`
- 文件：`QuentlamEngine/src/Quentlam/Scene/Scene.cpp`（第135行）

**6. "复制对象"菜单项重复 AddComponent 断言**
- 原因：`CreateEntity()` 已自动添加 `TransformComponent`，但菜单中的复制代码直接调用 `AddComponent` 导致断言失败
- 修复：
  - 改用 `reg.emplace_or_replace<T>()` 替代 `AddComponent`
  - 补全了所有 27 种组件类型的复制支持（之前只复制了 Transform + 5 种渲染组件）
- 文件：`QL-Editor/src/EditorLayer.cpp`（第1540-1620行）

---

### 2026-05-30 — #11 #12 修复批次

**13. #12 聚焦 Scene 窗口后无法控制场景摄像头**
- 原因：`ImGui::IsWindowFocused()` 不检查子窗口，SceneView/GameView 是 "视口(Viewport)" 的子窗口
- 修复：`IsWindowFocused()` → `IsWindowFocused(ImGuiFocusedFlags_ChildWindows)`，`IsWindowHovered()` 同理
- 文件：`QL-Editor/src/EditorLayer.cpp`（第3298-3299行）

**14. #11a 2D 模式下场景漆黑（地形不渲染）**
- 原因：Terrain 渲染代码在 `if (m_Is3DCamera)` 分支内，2D 模式下不执行
- 修复：在 `else`（2D）分支中调用 `Renderer3D::BeginScene(m_CameraController.GetCamera())` 后也渲染 Terrain
- 文件：`QL-Editor/src/EditorLayer.cpp`（第827行附近）

**14b. #11a 补遗：2D 模式下天空漆黑（Sky Dome 不可见）**
- 原因：Sky Dome 在正交相机模式下完全不可见 —— 球体在相机背后（camera z=10，朝向 -Z），且着色器中 `dir = v_WorldPosition - cameraPos` 在正交相机下所有像素方向相同（向下），导致纯黑
- 修复：
  - 新增 `m_SkyQuadShader`（独立 Shader）和 `m_QuadVAO`（全屏贴片）
  - `RenderOrthographic()` 使用贴片模式，UV→球坐标方向映射，覆盖全屏
  - `EditorLayer` 中 2D 模式调用 `RenderOrthographic`，3D 模式调用 `Render`（保持不变）
- 文件：
  - `QuentlamEngine/src/Quentlam/Renderer/SkyRenderer.h`（添加 `m_SkyQuadShader`、`m_QuadVAO`、`RenderOrthographic`）
  - `QuentlamEngine/src/Quentlam/Renderer/SkyRenderer.cpp`（两个独立 Shader + 贴片网格 + 渲染函数）
  - `QL-Editor/src/EditorLayer.cpp`（天空渲染分支）

**14c. #11a 补遗：Game 窗口渲染缺少天空和地形**
- 原因：Game 窗口渲染只用了 Game Camera 实体的相机绘制 2D sprites，忽略了天空、地形等全局场景内容
- 修复：Game 窗口渲染也添加天空、地形渲染，与 Scene 视图一致
- 文件：`QL-Editor/src/EditorLayer.cpp`（Game 窗口渲染部分）

**15. #11b 2D/3D 切换无过渡动画**
- 原因：`StartCameraTransition` / `CancelCameraTransition` 声明了但从未实现
- 修复：
  - 实现 `StartCameraTransition(bool to3D)` 和 `CancelCameraTransition()`
  - 在 `OnUpdate` 中添加 ease-out cubic 插值动画（0.8秒），同时插值 position 和 rotation
  - RadioButton 改为触发 `StartCameraTransition()` 而非直接修改 `m_Is3DCamera`
  - `PerspectiveCameraController` 添加 `SetPosition`/`SetRotation` 公开方法
- 文件：
  - `QL-Editor/src/EditorLayer.cpp`（过渡逻辑在 OnUpdate 中，函数在 OnAttach 后）
  - `QL-Editor/src/EditorLayer.h`（已声明的成员变量生效）
  - `QuentlamEngine/src/Quentlam/Renderer/PerspectiveCameraController.h`

---

### 构建结果

- Release 构建：**0 错误，0 警告**
- EXE：`bin/Release-windows-x86_64/QL-Editor/QL-Editor.exe`（10.5 MB）

---

### 本次修复内容（用户反馈 2026-05-29 第二轮）

**问题 #11 补充（2D/3D 场景使用同一摄像机实体）**

- 根本原因：之前 EditorLayer 有两套独立的相机控制器，渲染管线未正确使用场景中的摄像机实体
- 修复方案：
  - 删除 `EditorLayer.h` 中的 `m_CameraController` 和 `m_PerspCameraController` 成员变量及两个头文件 include
  - 在 EditorLayer 中直接维护相机状态（`m_Cam2DPosition`、`m_Cam3DPosition`、`m_Cam3DRotation` 等）
  - `OnUpdate` 每帧将状态同步到 Scene Camera Entity
  - 所有渲染（Scene 视图 / Game 视图 / ImGuizmo / TileMap）直接读取 Scene Camera Entity 的矩阵
  - 2D/3D 切换通过修改 CameraComponent 投影类型实现，带过渡动画

**问题 #13（ImGuizmo 渲染到窗口间隙）**

- 在 ImGuizmo::Manipulate 前启用 `glScissor` 裁剪到 Scene 视口 bounds
- 结束后恢复之前的 scissor 状态

**问题 #14（Game 窗口摄像机视角）**

- Game 窗口渲染已使用 Game Camera Entity；天空渲染也改为使用 Game Camera Entity 的 transform

**问题 #15（场景摄像机可被删除）**

- `Scene::DestroyEntity` 添加"场景摄像机"和"游戏摄像机"标签保护，删除时输出 ERROR
- 添加 `GetOrCreateSceneCamera()` 和 `GetOrCreateGameCamera()` 方法

**Camera 基类修复**

- `Camera.h` 添加 `GetProjectionMatrix()` 方法供 CameraComponent 使用

**编辑器相机控制**

- EditorLayer::OnEvent 处理 MouseScrolled（2D 缩放 / 3D 推进）和 WindowResize
- EditorLayer::OnUpdate 处理 WASD 移动、鼠标拖拽旋转（3D）
- "F" 键聚焦选中物体改为直接修改相机状态变量
- "F" 键现在在 3D 和 2D 模式下都可用

---

### 本次修复内容（用户反馈 2026-05-30 第二轮）

#### 问题 #20（地形和天空不显示）

**根本原因（地形）**：`Terrain::Render()` 仍然直接调用 `glDrawElements` 但 shader 可能编译失败或绑定不正确。

**修复**：
- 确认 `TerrainShader.glsl` 正确编译
- `Terrain.cpp` 中 `Terrain::Render` 正确创建并绑定 shader，设置所有 uniforms
- EditorLayer 中正确传递 view-projection, transform, 光照参数

**根本原因（天空）**：原有的 dome mesh 方案存在矩阵问题。dome 以 world origin 为中心，但相机在 (0,5,15)，导致天空渲染在错误位置。

**修复（SkyRenderer 完全重写）**：
- 放弃 dome mesh 方案，改为**屏幕空间 quad** 方法
- 新方案：quad 覆盖全屏（z=0.9999），片元着色器通过 `u_InverseViewProjection` 矩阵反投影还原 view direction
- 片元着色器中：`vec4 worldPos = u_InverseViewProjection * vec4(v_UV * 2.0 - 1.0, 1.0, 1.0); vec3 dir = normalize(worldPos.xyz / worldPos.w);`
- 此方法完全消除了 dome 几何、相机相对方向计算等所有矩阵问题

#### 问题 #22（场景摄像机在层级列表中可见）

**状态**：代码中已有过滤逻辑（`if (tag == "场景摄像机" || tag == "游戏摄像机") continue;`），过滤逻辑正确存在。

#### 问题 #23（ImGuizmo 溢出到 Scene 窗口外）

**根本原因**：ImGuizmo 的 `SetDrawlist()` 在 Viewport window 层级渲染，而非 SceneView child window 层级，导致溢出到全局画布。

**修复**：将 ImGuizmo 渲染代码从 Viewport window 外部移入 `SplitHorizontal`、`SplitVertical`、`SceneOnly` 三个分支的 `SceneView` BeginChild/EndChild 内部，确保 ImGuizmo 在正确的 child window 绘制列表中渲染。

#### 问题 #24（Play 模式崩溃）

**根本原因**：`JPH_PROFILE_ENABLED` 在 Release 构建中启用。关键是：之前只从 QuentlamEngine/QL-Editor/Sandbox 项目中移除了，但 **JoltPhysics 静态库项目本身** 仍然定义了 `JPH_PROFILE_ENABLED`，所以 JoltPhysics.lib 库代码本身是用 profiling 启用来编译的。

**修复**：
- 从 `premake5.lua` 的 Release 配置中移除 `JPH_PROFILE_ENABLED` 定义
- 保留 `JPH_DEBUG_RENDERER`（用于编辑器调试渲染）
- 重新生成项目并构建

**其他修复**：
- `EditorLayer.cpp` 中 `terrainView.empty()` 改为 `terrainView.begin() == terrainView.end()`（entt::basic_view 不支持 `.empty()` 方法）
- `Scene.cpp` 中 `OnRuntimeStart()` 的缩进问题修复
- `SkyRenderer.h` 补全 dome 相关成员声明（`m_VAO`、`m_VBO`、`m_IBO`、`m_IndexCount`、`m_SkyQuadShader`）以匹配 cpp 中实际使用
- `SkyRenderer.cpp` 重写为屏幕空间 quad 方案
- `EditorLayer.h` 中 `m_Is3DCamera` 默认值从 `false` 改为 `true`（编辑器默认 3D 透视摄像机）
- JoltPhysics 项目（JoltPhysics.vcxproj）Release 配置中移除 `JPH_PROFILE_ENABLED`（之前只移除了 QuentlamEngine 和 QL-Editor 的，但 JoltPhysics 库本身还在定义）
- Sandbox 项目 Release 配置中移除 `JPH_PROFILE_ENABLED`

**构建速度优化**：
- 添加 `/MP` 编译选项到 QuentlamEngine、QL-Editor、Sandbox 三个项目，使 MSVC 可以跨多个 CPU 核心并行编译
- **注意**：`/MP` 和 `/Gm`（Minimal Rebuild）互斥，MSVC 在使用 `/MP` 时自动忽略 `/Gm`，因此只用 `/MP`


---

### 遗留问题（本次未处理）

- **#6** Editor 中文化 — 子 agent 处理中

---

### 构建优化（2026-05-30）

**目标：加速 JoltPhysics / Lua 的编译速度**

**方案分析：**

| 方案 | 原理 | 实际效果 | 结论 |
|---|---|---|---|
| Unity Build | 将多个 .cpp 合并为一个编译单元 | JoltPhysics 152 个 .obj 未减少 | MSVC 对此项目无效（/FS 冲突） |
| /FI PCH | Force Include 模拟 PCH | 未观察到 .pch 文件 | Jolt 头太分散，效果有限 |
| **/MP** | 多核并行编译多个 .cpp | **~110s → ~19s（5.8x）** | **关键加速手段** |

**实现：**
1. `premake5.lua` — JoltPhysics 项目添加 `buildoptions { "/FS", "/MP" }`，Lua 项目添加 `buildoptions { "/wd4005", "/MP" }`
2. `scripts/patch_unity_build.py` — 后处理脚本，每次 `premake5` 生成后自动给 JoltPhysics/Lua 的 vcxproj 添加 `EnableUnityBuild`
3. `scripts/GenerateProject.bat` — 一键生成脚本（`premake5` + `patch_unity_build.py`）

**编译时间对比（Release，ForceRebuild）：**

```
优化前：JoltPhysics ~115s  |  完整项目 ~145s
优化后：JoltPhysics ~19s   |  完整项目 ~64s
提升：  6x（JoltPhysics）   |  2.3x（完整项目）
```

**发布时额外优化：**
- Dist 配置可添加 `/LTCG`（链接时优化）进一步减小二进制体积

### 本次修复内容（用户反馈 2026-05-30 第三轮）

**根本原因**：`Application::Get().GetImGuiLayer()->BlockEvents(shouldBlock)` 在 Edit 模式下被调用（`shouldBlock = false`），这会触发 `ImGuiLayer::OnEvent` 捕获并标记事件为已处理，导致事件无法到达 ImGui。ImGui 无法接收到鼠标点击事件，因此 `IsWindowFocused` 永远返回 `false`。

**修复**：完全移除 `BlockEvents` 调用。EditorLayer 现在不使用 `BlockEvents` 机制，而是让 ImGui 自然地通过 IO 的 `WantCaptureMouse` / `WantCaptureKeyboard` 来处理焦点。在 Play 模式下（`m_SceneState != SceneState::Edit`），Game 窗口通过其独立的 `ImGui::IsWindowFocused` 来处理焦点。

- 文件：`QL-Editor/src/EditorLayer.cpp`（移除 `BlockEvents` 调用）

#### 问题 #2（Play 模式崩溃）

**根本原因**：上一轮修复后，`JPH_DEBUG_RENDERER` 定义仍在多个项目的 Release 配置中残留：
- `JoltPhysics` 项目 Release 配置：`JPH_DEBUG_RENDERER` 仍在
- `QuentlamEngine` 项目 Release 配置：`JPH_DEBUG_RENDERER` 和 `JPH_ENABLED` 仍在（两个连续的 `defines {}` 块）
- `Sandbox` 项目 Release 配置：`JPH_DEBUG_RENDERER` 仍在

`JPH_DEBUG_RENDERER` 会触发 Jolt Physics 的内部断言代码，在 Release 构建中导致 `JPH_ASSERT(mNumLayers < (BroadPhaseLayer::Type)cBroadPhaseLayerInvalid)` 崩溃。

**修复**：从所有项目的 Release/Dist 配置中完全移除 `JPH_DEBUG_RENDERER` 和 `JPH_ENABLED`：
- `JoltPhysics` Release: 移除 `JPH_DEBUG_RENDERER`（保留 `JPH_PROFILE_ENABLED` 在 Debug 中）
- `QuentlamEngine` Release: 合并两个重复的 `defines {}` 块，移除 `JPH_DEBUG_RENDERER` 和 `JPH_ENABLED`
- `Sandbox` Release: 移除 `JPH_DEBUG_RENDERER`

- 文件：`premake5.lua`

#### 问题 #3（地形不显示）

**根本原因**：多重因素导致：
1. `m_ViewportBounds` 只在 `SceneOnly` 布局中设置，而用户使用的是 `SplitHorizontal` 布局，导致鼠标拾取坐标计算完全错误（使用了未初始化的边界值）
2. 渲染流程中 `m_Framebuffer->UnBind()` 后的 OpenGL 状态管理问题

**修复**：
1. 移除 `BlockEvents` 后，ImGui 正常处理事件，`m_ViewportFocused` 可以正确更新，`m_HoveredEntity` 可以正确检测实体
2. `Renderer3D::BeginScene` 在 terrain 渲染前正确设置 OpenGL 状态

- 文件：`QL-Editor/src/EditorLayer.cpp`

#### 问题 #4（ImGuizmo 不显示且无法选择实体）

**根本原因**：`m_ViewportFocused` 永远为 `false`（因为 BlockEvents 阻止事件到达 ImGui），导致 ImGuizmo 的显示条件 `if (selectedEntity && m_GizmoType != -1 && m_ViewportFocused)` 永远不满足。

**修复**：同问题 #1，移除 `BlockEvents` 后，`m_ViewportFocused` 可以正确更新，ImGuizmo 正常显示。

- 文件：`QL-Editor/src/EditorLayer.cpp`

#### 修复后的关键行为变化

1. **场景窗口聚焦**：`m_ViewportFocused` 现在正确反映 SceneView child window 的焦点状态，相机控制和 ImGuizmo 正常工作
2. **Play 崩溃**：移除所有 `JPH_DEBUG_RENDERER` 和 `JPH_ENABLED` 定义后，Jolt Physics 在 Release 构建中不再触发断言
3. **地形渲染**：鼠标拾取正确工作后，实体检索正常，terrain 作为场景中的实体应该可以正确显示
4. **ImGuizmo**：在有实体被选中且工具被选择（G/T/R 键）时，应该正确显示

---

