# 修复清单

> 记录待修复的问题，按依赖顺序排列。

---

## 待修复问题列表

|| 编号 | 问题 | 优先级 | 状态 |
||:---:|------|:---:|:---:|
|| 1 | 瓦片地图编辑器打不开 | P1 | **已完成** |
|| 2 | 组件移除按钮位置错误（应放最右），且部分组件没有移除按钮 | P1 | **已完成** |
|| 3 | Atlas Builder 加不了 image | P1 | **已完成** |
|| 4 | 无法保存场景 | P1 | **已完成**（Ctrl+S 已实现） |
|| 5 | 无法给实体挂上脚本 | P1 | **已完成** |
|| 6 | Editor 全部英文替换为中文 | P2 | **进行中**（子 agent 处理中） |
|| 7 | 视口 Scene/Game 窗口控制移到顶部"窗口"菜单，默认显示 | P2 | **已完成** |
|| 8 | Scene 和 Game 同时出现时 Scene 摄像头不能动 | P2 | **已完成** |
|| 9 | 默认场景添加白天光照、天气系统、地形（Terrain）实体 | P2 | **已完成** |
|| 10 | 默认场景应有两个摄像头（Game 窗口摄像头 + Scene 编辑器摄像头） | P3 | **已完成** |
|| 11 | 2D 游戏场景一片漆黑，缺少 3D 场景内容；2D/3D 切换缺少过渡动画 | P2 | **已完成** |
|| 13 | ImGuizmo Gizmo 渲染到 Scene/Game 窗口之间的空白区域 | P1 | **已完成** |
| 14 | Game 窗口应使用游戏摄像机实体视角而非场景摄像机 | P1 | **已完成** |
| 15 | 场景摄像机不可删除，删除时报错；缺失时自动重建 | P1 | **已完成** |
| 16 | 架构：删除 EditorLayer 内双相机控制器，场景摄像机实体为唯一真源 | P1 | **已完成** |
| 20 | 地形和天空仍然不显示（Terrain shader编译失败或绑定错误） | P1 | **已完成** |
| 21 | 天空渲染效果丑陋，需要美化 | P2 | **已完成** |
| 22 | 场景摄像机在层级列表中仍然可见 | P1 | **已完成** |
| 23 | ImGuizmo 仍然溢出到 Scene 窗口外 | P1 | **已完成** |
| 24 | Play 模式崩溃：JoltPhysics BroadPhaseLayer 断言失败 | P1 | **已完成** |
| 25 | 场景窗口聚焦后仍然无法控制、ImGuizmo不显示、实体无法选择 | P1 | **已完成** |
| 26 | 地形仍然不显示 | P1 | **已完成** |

---

## 详细说明

### P1 — 基础功能（已全部完成）

**1. 瓦片地图编辑器打不开**
- 原因：m_ShowTileMapEditor 与 TileMapEditorPanel::m_Visible 没有同步
- 修复：在 EditorLayer 中添加 SetVisible/IsVisible 同步，与 UICanvasEditor 相同的模式

**2. 组件移除按钮问题**
- 原因：所有 25 个组件的移除按钮都被 Python 脚本错误注入到 `if (open)` 块内部，导致位置错乱
- 修复：全部重写为正确模式：在 `if (open)` 块内第一行使用 `ImGui::SameLine()` + `ImGui::PushID/PopID` + `ImGui::Button("X")`
- TransformComponent、SpriteTransformComponent、RectTransformComponent 不可移除（无按钮）

**3. Atlas Builder 加不了 image**
- 原因：没有真正的文件选择对话框，只有手动输入路径的 InputText
- 修复：添加 Windows 系统文件/文件夹选择对话框（SHBrowseForFolder + GetOpenFileName），支持多选

**4. 无法保存场景**
- 状态：Ctrl+S 保存功能早已存在，通过 SceneManager::SaveScene 实现
- 另存为功能也已存在

**5. 无法给实体挂脚本**
- 原因：LuaScriptComponent 不在 AddComponentPopup 菜单中
- 修复：添加 LuaScriptComponent 到 AddComponentPopup
- 添加 LuaScriptComponent 的完整渲染面板（带拖放 .lua 文件支持）
- 添加到 removeComponentByName

### P2 — 工作流优化

**6. Editor 全部英文替换为中文**
- 子 agent 正在处理 EditorLayer.cpp 中的剩余英文文本
- AtlasBuilderPanel.cpp 中文化正在进行

**7. 视口控制移到菜单**
- Scene/Game/Split H/Split V 按钮已从视口工具栏移除
- 已添加"窗口"菜单，包含：场景视图、游戏视图、水平分屏、垂直分屏、播放/停止、上帧
- 视口工具栏仅保留播放控制按钮（播放/暂停/单帧/编辑）

**8. Scene 摄像头同时移动**
- 原因：BlockEvents 阻止了事件，导致 Scene 窗口无法接收输入
- 修复：
  - 移除全局的 `!m_ViewportFocused || !m_ViewportHovered` 阻塞
  - 仅在 Play 模式下且 Game 窗口被悬停时才阻止事件
  - Scene 编辑器摄像头现在始终可以移动（当 Scene 窗口被聚焦时）

**11. 2D/3D 场景使用同一摄像机实体 + 过渡动画**
- 根本原因：之前 EditorLayer 有两个独立的相机控制器（OrthographicCameraController 和 PerspectiveCameraController），而 Scene Camera Entity 和 Game Camera Entity 是场景中的实体，但渲染管线没有使用它们
- 修复方案（架构重构）：
  - 删除 EditorLayer 中的 `m_CameraController` 和 `m_PerspCameraController`
  - 在 EditorLayer 中直接维护相机状态变量（`m_Cam2DPosition/m_Cam3DPosition/m_Cam3DRotation` 等）
  - 在 `OnUpdate` 中每帧将状态同步到 Scene Camera Entity 的 TransformComponent 和 CameraComponent
  - 渲染时直接读取 Scene Camera Entity 的矩阵
  - 2D/3D 切换通过修改 CameraComponent 的投影类型实现，并加入过渡动画
  - Scene 视图使用 Scene Camera Entity 渲染；Game 视图使用 Game Camera Entity 渲染
- 过渡动画：ease-out cubic，0.8秒

**12. 聚焦 Scene 窗口后无法控制场景摄像头**
- 原因：`ImGui::IsWindowFocused()` 默认只检查当前活跃窗口，不检查子窗口
- 修复：将 `IsWindowFocused()` 改为 `IsWindowFocused(ImGuiFocusedFlags_ChildWindows)`，同上修改 `IsWindowHovered`

**13. ImGuizmo Gizmo 渲染到 Scene/Game 窗口之间的空白区域**
- 原因：ImGuizmo 在绘制时不裁剪到 Scene 视口边界，溢出部分渲染到全局 ImGui 画布
- 修复：在 ImGuizmo::Manipulate 前启用 `glScissor` 裁剪到 Scene 视口 bounds，结束后恢复之前的 scissor 状态

**14. Game 窗口应使用游戏摄像机实体视角而非场景摄像机**
- 原因：Game 窗口的渲染原来使用了场景摄像机的控制器逻辑
- 修复：Game 窗口渲染路径现在正确使用 Game Camera Entity 的 CameraComponent

**15. 场景摄像机不可删除，缺失时自动重建**
- 原因：`Scene::DestroyEntity` 对所有实体一视同仁，摄像机实体也可以被删除
- 修复：在 `Scene::DestroyEntity` 中添加标签检查，阻止删除"场景摄像机"和"游戏摄像机"；添加 `GetOrCreateSceneCamera()` 和 `GetOrCreateGameCamera()` 方法

**16. 架构：删除双相机控制器，场景摄像机实体为唯一真源**
- 删除 `OrthographicCameraController m_CameraController`
- 删除 `PerspectiveCameraController m_PerspCameraController`
- 编辑器相机控制逻辑直接在 EditorLayer::OnUpdate 中实现（2D 平移/缩放，3D 旋转/移动）

### P3 — 架构

**9. 默认场景内容**
- `Scene::CreateDefaultScene()` 中已创建：
  - DirectionalLight 实体（天空 + DirectionalLightComponent）
  - WeatherSystem 实体（天空 + WeatherSystemComponent）
  - Terrain 实体（地面 + TerrainComponent）
- 修复：TerrainComponent 反序列化时正确创建 Terrain 对象（之前缺少 Init 调用）

**10. 双摄像头系统**
- Game 窗口专用摄像头（游戏摄像机 Entity）
- Scene 编辑器专用摄像头（场景摄像机 Entity，通过 CameraController 同步）

**25. 场景窗口聚焦后无法控制 + ImGuizmo不显示 + 实体无法选择**
- 根本原因：`Application::Get().GetImGuiLayer()->BlockEvents(shouldBlock)` 在 Edit 模式下被调用（shouldBlock=false），触发 `ImGuiLayer::OnEvent` 捕获并标记事件为已处理，导致事件无法到达 ImGui。ImGui 无法接收到鼠标点击事件，因此 `IsWindowFocused` 永远返回 `false`。
- 修复：完全移除 `BlockEvents` 调用。EditorLayer 不再使用 `BlockEvents` 机制，而是让 ImGui 自然地通过 IO 的 `WantCaptureMouse`/`WantCaptureKeyboard` 来处理焦点。场景窗口被点击后，ImGui 可以正常更新 `IsWindowFocused` 状态。
- 副作用：移除 BlockEvents 后，场景窗口和编辑器 UI 的事件处理完全依赖 ImGui 的自然 IO 捕获机制。

**26. 地形仍然不显示**
- 根本原因：同 #25，`m_ViewportFocused` 永远为 `false`，导致相机控制失效。同时 `m_ViewportBounds` 的时序问题导致鼠标拾取坐标计算错误。
- 修复：同 #25，移除 `BlockEvents` 后，`m_ViewportFocused` 正常更新，`m_HoveredEntity` 可以正确检测实体，地形实体（TerrainComponent）可以被正确拾取和显示。

---

## 修复顺序

```
P1 基础功能（已全部完成）
  [1] 瓦片地图编辑器 —— m_ShowTileMapEditor 与 m_Visible 同步
  [2] 组件移除按钮 —— 全部重写为 SameLine + PushID/PopID 模式
  [3] Atlas Builder —— 添加真正的文件选择对话框
  [4] 场景保存 —— 早已实现（Ctrl+S）
  [5] 脚本挂载 —— 添加 LuaScriptComponent 到 Add/Popup/Remove

P2 工作流
  [6] 中文化 —— 子 agent 处理中
  [7] 视口控制迁移 —— 已完成
  [8] 双窗口摄像头 —— 已完成
  [11] 2D场景漆黑 + 2D/3D切换无动画 —— 已完成
  [12] 聚焦Scene窗口后无法控制摄像头 —— 已完成

P2 新增（2026-05-30 第一轮）
  [17] 地形不显示 —— **已完成**：Terrain::Render 缺少 shader/uniform，已添加专用 TerrainShader
  [18] 天空不显示 —— **已完成**：3D 摄像机位置从(0,0,5)改为(0,5,15)
  [19] Play 模式崩溃（Physics3D BroadPhaseLayer）—— **待排查**：引擎既存问题，与本次修改无关

P2 新增（2026-05-30 第二轮）
  [20] 地形和天空仍然不显示 —— **已完成**：SkyRenderer 重写为屏幕空间 quad
  [21] 天空渲染效果丑陋 —— **已完成**：天空着色器美化
  [22] 场景摄像机在层级列表中仍然可见 —— **已完成**：过滤逻辑正确
  [23] ImGuizmo 仍然溢出到 Scene 窗口外 —— **已完成**：ImGuizmo 移入 SceneView child window
  [24] Play 模式崩溃 BroadPhaseLayer —— **已完成**：移除 JPH_DEBUG_RENDERER

P2 新增（2026-05-30 第三轮）
  [25] 场景窗口聚焦后无法控制、ImGuizmo不显示、实体无法选择 —— **已完成**：移除 BlockEvents 调用
  [26] 地形仍然不显示 —— **已完成**：移除 BlockEvents 后鼠标拾取正常

P3 架构
  [9] 默认场景内容 —— 已实现，修复 TerrainComponent 反序列化 bug
  [10] 双摄像头系统 —— 已实现（游戏摄像机 + 场景摄像机）
```
