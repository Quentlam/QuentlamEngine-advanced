# QuentlamEngine 开发标准

> 本文档是 QuentlamEngine 的强制性开发规范。所有代码必须符合本文档，结构性 bug 和技术债务是不可接受的。
>
> **最后更新**: 2026-05-31

---

## 一、核心原则

1. **零容忍原则**：不允许有编译错误、运行时崩溃、逻辑 bug 或技术债务。所有代码必须经过系统性审查后才能合入。
2. **架构服务于扩展**：每次添加功能时，必须考虑未来扩展性。禁止为了快速实现而破坏架构一致性。
3. **系统性思维**：理解每个模块在整体系统中的位置和依赖关系。修改时追踪所有关联点。
4. **质量优先于速度**：功能开发可以分批完成，但每一批代码都必须是无 bug 的完整实现。
5. **编译通过方可交付**：所有开发工作完成后，**必须**确保 `VS Debug|x64` 模式编译通过且程序能正常运行。未通过编译或无法运行的代码视为未完成。

---

## 二（附）、构建与运行标准（强制）

### 二（附）-1 编译必须通过

所有开发工作完成后，**必须**确保以下条件全部满足：

1. **VS Debug|x64 编译通过**：使用 Visual Studio 以 `Debug|x64` 配置完整编译解决方案，**零错误**。
2. **程序能正常运行**：编译出的 `QL-Editor.exe` 能启动并进入编辑器主界面，无崩溃、无异常。
3. **每次提交前自测**：每次代码提交前，开发者必须本地执行一次完整的 `Debug|x64` 编译并启动程序验证。

**禁止**以下行为：
- 提交编译失败的代码
- 提交编译通过但运行即崩溃的代码
- 假设"别人会修复"而不修复已知编译错误

### 二（附）-2 常见编译错误排查

|| 错误类型 | 常见原因 | 解决方案 |
||---------|---------|---------|
|| `C2039: xxx is not a member` | API 在当前 ImGui/MSVC 版本中不存在 | 查阅 ImGui 1.88 文档，使用替代 API |
|| `C2440: cannot convert` | 类型转换不兼容 | 显式使用 `.Transform` 访问或 `static_cast` |
|| `C1853: PCH mismatch` | 预编译头缓存不一致 | 清理 `*.pch`、`*.ipch` 文件后重新编译 |
|| `C1128: 节数超过对象文件格式限制` | 单文件过大 | 添加 `/bigobj` 到 `AdditionalOptions` |
|| `C7746: cannot appear in its own initializer` | 自引用 lambda | 改用成员函数 |
|| `C2513: variable declared after =` | Windows 宏污染（如 `near`、`far`） | 重命名变量避开宏名 |

### 二（附）-3 编译选项规范

- 单个 `.cpp` 文件过大（如超过 2000 行）时，考虑拆分为多个文件或重构
- 使用 `/bigobj` 解决 C1128 错误（在 `QL-Editor.vcxproj` 的 `AdditionalOptions` 中添加）
- 不要使用已废弃的 `/Gm`（minimal rebuild）选项

---

## 三、ImGui UI 代码规范

### 2.1 组件面板移除按钮模式（强制）

每个 `ImGui::CollapsingHeader` 或 `ImGui::TreeNodeEx` 的 `if (open)` 块中，移除按钮的代码结构**必须**严格遵循以下模式：

```cpp
// ✅ 正确写法
bool open = ImGui::CollapsingHeader("组件名称");
if (open)
{
    ImGui::SameLine();
    ImGui::PushID("RemoveComponentID");
    if (ImGui::Button("X", ImVec2(18, 18)))
    {
        m_PendingRemoveComponent = "ComponentName";
    }
    ImGui::PopID();

    // 组件属性...
}

// ❌ 错误写法 1：没有 PushID/PopID
bool open = ImGui::CollapsingHeader("组件名称");
if (open && ImGui::Button("X", ImVec2(18, 18)))
{
    m_PendingRemoveComponent = "ComponentName";
}

// ❌ 错误写法 2：按钮在 if 外部但没有 SameLine
bool open = ImGui::CollapsingHeader("组件名称");
if (open)
{
    ImGui::Button("X", ImVec2(18, 18));
    // ...
}

// ❌ 错误写法 3：没有 SameLine
bool open = ImGui::CollapsingHeader("组件名称");
ImGui::PushID("Remove");
if (ImGui::Button("X", ImVec2(18, 18)))
{
    m_PendingRemoveComponent = "ComponentName";
}
ImGui::PopID();
if (open)
{
    // ...
}
```

**原理**：`ImGui::SameLine()` 使按钮与标题行在同一行显示在最右侧。`PushID/PopID` 确保在实体有多个同类组件时 ID 不会冲突。

### 2.2 不可移除组件的处理

以下组件**永远不添加移除按钮**，因为它们是实体的基础：
- `TransformComponent`
- `SpriteTransformComponent`
- `RectTransformComponent`

### 2.3 新增组件的标准化流程

在 EditorLayer 中添加新组件时，**必须**完成以下全部步骤，缺一不可：

1. `AddComponentPopup` 菜单项 — 使组件可以被添加
2. 组件属性渲染面板 — 在 `if (open)` 块内，包含移除按钮
3. `removeComponentByName` 映射 — 使移除功能正常工作
4. 序列化支持（`Serialize` / `Deserialize`）— 持久化保存
5. 所有英文文本替换为中文

---

## 四、可见性同步规范（强制）

当一个 UI 元素（如 Panel）的可见性由外部状态控制时，**必须**在调用 `OnImGuiRender()` 前同步状态：

```cpp
// ✅ 正确写法
if (m_ShowTileMapEditor)
{
    m_TileMapEditor.SetVisible(true);
    m_TileMapEditor.SetTileMap(&m_EditorTileMap);
    m_TileMapEditor.OnImGuiRender();
}
else
{
    m_TileMapEditor.SetVisible(false);
}

// ❌ 错误写法：只依赖 Panel 内部状态
if (m_ShowTileMapEditor)
{
    m_TileMapEditor.OnImGuiRender(); // 面板内部 m_Visible 可能为 false
}
```

**适用场景**：所有编辑器面板（`TileMapEditorPanel`、`AtlasBuilderPanel`、`UICanvasEditor` 等）。

---

## 五、文件选择对话框规范

编辑器中所有涉及文件/文件夹选择的 UI，必须使用原生系统对话框：

- Windows: `GetOpenFileNameA` / `SHBrowseForFolderA`
- 禁止使用 `ImGui::InputText` 作为路径输入的替代方案

```cpp
// ✅ 使用原生文件对话框
char pathBuf[MAX_PATH] = { 0 };
OPENFILENAMEA ofn = {};
ofn.lStructSize = sizeof(ofn);
ofn.lpstrFilter = "PNG Images\0*.png\0All Files\0*.*\0";
ofn.lpstrFile = pathBuf;
ofn.nMaxFile = MAX_PATH;
ofn.Flags = OFN_FILEMUSTEXIST | OFN_PATHMUSTEXIST | OFN_ALLOWMULTISELECT;
if (GetOpenFileNameA(&ofn)) { /* ... */ }
```

---

## 六、序列化规范

### 5.1 组件序列化函数签名

所有组件的序列化函数**必须**遵循以下签名：

```cpp
void Serialize(YAML::Emitter& out, const T& component, const EntitySerializer::EntityInfo& info);
void Deserialize(const YAML::Node& node, T& component, const EntitySerializer::EntityInfo& info);
```

**禁止**使用非 const 引用传递 `component`，除非必要。

### 5.2 LuaScriptComponent 序列化

`LuaScriptComponent` 序列化时，路径字段名**必须**为 `"ScriptPath"`，与其他资源引用字段（如 `SpritePath`）保持一致命名风格。

### 5.3 新增组件序列化步骤

添加新组件时，**必须**在 `SceneSerializer.cpp` 中完成：

1. 在 `SerializeEntity` 中添加 `if (entity.HasComponent<T>())` 分支
2. 在 `DeserializeEntity` 中添加对应的反序列化逻辑
3. 使用 `YAML::Node` 的类型安全访问方式（`as<T>()` 而非强制转换）

---

## 七、Lua 脚本系统规范

### 6.1 脚本实例生命周期

- `LuaScriptInstance` 由 `LuaScriptBridge` 所有，通过共享指针引用
- 实体持有 `LuaScriptInstance` 的弱引用（`std::weak_ptr`）
- 实体的 `OnLuaScriptAdded` 回调负责创建/获取实例
- 实体的 `OnLuaScriptRemoved` 回调负责释放实例引用

### 6.2 组件字段命名

`LuaScriptComponent` 中的字段：
- `std::string ScriptPath` — Lua 脚本路径
- `bool IsEnabled` — 是否启用

### 6.3 新增 Lua API 步骤

在 `LuaScriptBridge.cpp` 中注册新的 C++ 函数到 Lua 时，**必须**：

1. 在对应类的注册函数中添加 `sol::constructors` 或直接注册成员函数
2. 在全局表或类表中注册函数
3. 为所有函数参数提供合法性检查（检查 nil / 类型）
4. 在 `LuaScriptBridge.h` 的类定义中声明新方法
5. 在对应的 `onConstruct` / `onClone` / `onDestroy` 回调中正确处理

---

## 八、中文本地化规范

编辑器 UI 中的所有文本（按钮、标签、提示）**必须**使用中文。英文仅在以下情况保留：

- 技术标识符（如 GLSL 代码、shader 变量名、枚举值）
- 文件格式名（如 `.lua`、`.png`）
- API 引用（如 `Camera_SetFollowTarget`）

格式示例：
- ✅ `"视口"` — 正确
- ✅ `"宽度 (Width)"` — 可选，括号内保留英文
- ❌ `"Width"` — 禁止（纯英文标签）

---

## 九、禁止模式

以下模式**严格禁止**在任何代码中使用：

| 禁止模式 | 说明 | 替代方案 |
|---------|------|---------|
| 在循环中使用 `auto&&` 不加索引 | 导致引用悬挂 | 使用 `for (size_t i = 0; ...)` |
| `ImGui::Button` 紧跟在 `CollapsingHeader` 后不加 `SameLine` | 按钮跳到下一行 | 加 `ImGui::SameLine()` |
| 跨文件的隐式依赖 | 导致编译顺序问题 | 使用前向声明或重构依赖 |
| 魔法数字 | 不可维护 | 定义为命名常量 |
| 悬空指针使用 | 崩溃风险 | 使用 `std::shared_ptr` / `std::weak_ptr` |
| 在 UI 代码中直接修改游戏状态而不经过接口 | 架构混乱 | 通过 API 接口操作 |
| 修改了头文件但不更新所有使用者 | 接口不一致 | 同步修改所有相关文件 |

---

## 十、代码审查清单

每次提交代码前，**必须**对照以下清单检查：

### 结构性检查
- [ ] 新增/修改的组件是否在 AddComponentPopup 中添加了菜单项？
- [ ] 新增/修改的组件是否在 removeComponentByName 中注册了移除函数？
- [ ] 新增/修改的组件是否在 SerializeEntity / DeserializeEntity 中实现了序列化？
- [ ] 组件面板的移除按钮是否使用了正确的 `SameLine() + PushID + Button` 模式？
- [ ] UI 面板的可见性与外部状态是否正确同步？
- [ ] 是否引入了新的头文件依赖？是否在所有必要文件中添加了 include？

### 质量检查
- [ ] 是否有未处理的 `nullptr` 指针？
- [ ] 是否有内存泄漏风险（新增的 `new` 是否有对应的 `delete` 或使用智能指针）？
- [ ] 是否有未初始化的成员变量？
- [ ] 是否有编译器警告（W4 级别）？
- [ ] 所有英文 UI 文本是否已替换为中文？

### 架构检查
- [ ] 新功能是否符合现有架构模式？
- [ ] 是否为未来扩展留有接口？
- [ ] 是否破坏了现有功能的兼容性？

---

## 十一、文件修改规范

### 10.1 修改现有文件的注意事项

当修改现有文件时：
1. **理解上下文**：修改前必须完整理解相关代码段
2. **追踪依赖**：查找所有使用该函数/类/变量的地方
3. **原子性**：每个逻辑修改单独提交，不要混在一起
4. **向后兼容**：如果需要改变接口，提供迁移路径

### 10.2 新增文件规范

新增文件时：
1. 在 `premake5.lua` 中对应项目的 `files {}` 块中添加文件路径
2. 运行 `vendor/bin/premake5.exe vs2022` 重新生成项目文件
3. 头文件使用 `#pragma once` 或 include guard
4. 包含必要的标准库头文件
5. 遵循项目现有的命名和格式风格

---

## 十二、注释与命名规范

### 11.1 注释规则

**禁止**添加以下类型的注释：
- 逐行描述代码在做什么（`// Get the count` → `int count = x.size();`）
- 废话注释（`// Check if condition is true`）
- 过度注释（每个 if/else 都加注释）

**鼓励**添加以下类型的注释：
- 解释**为什么**这样做的决策注释
- 非显而易见的算法或数学公式说明
- 架构决策和 trade-off 说明

### 11.2 命名规范

| 类型 | 风格 | 示例 |
|-----|------|-----|
| 类名 | PascalCase | `LuaScriptBridge` |
| 成员变量 | m_CamelCase | `m_SelectedEntity` |
| 函数名 | PascalCase | `OnImGuiRender` |
| 常量 | kCamelCase | `kMaxPathLength` |
| 局部变量 | camelCase | `entityId`, `scriptPath` |

---

## 十三、构建工具规范（强制）

### 12.1 构建系统选择

**只允许使用 `premake5` 生成项目文件，禁止使用 CMake 或任何其他构建工具。**

具体禁止项：
- **禁止使用 CMake**（`CMakeLists.txt` 不应存在于项目中）
- **禁止手动编辑 `.vcxproj`、`.sln` 等项目文件**（所有项目配置必须通过 `premake5.lua` 管理）
- **禁止使用 `MSBuild` 直接操作项目文件**（只能通过 Visual Studio 打开 premake5 生成的解决方案）

**重新生成项目**必须使用 `script/GenerateProject.bat` 脚本，禁止手动调用 premake5 或 cmake。

第三方依赖库（如 assimp）按其原有构建工具构建（CMake），不纳入 premake5 管理范围。

### 12.2 premake5.lua 同步规则

如果添加了新的依赖或者文件，必须**同步**修改 `premake5.lua`：

1. **新增源文件**：在对应项目的 `files {}` 块中添加文件路径
2. **新增头文件**：在对应项目的 `files {}` 块中添加头文件路径
3. **新增依赖库**：
   - 如果是静态库，在 `links {}` 中添加库名或路径
   - 如果是第三方库，在 `includedirs {}` 中添加 include 目录
   - 在 workspace 顶部的 `IncludeDir` 表中注册新的路径别名
4. **新增链接库**（`.lib`）：在对应配置的 `links {}` 中添加完整路径

### 12.3 运行时库一致性

所有项目和第三方库必须使用**相同的 MSVC 运行时库配置**（`staticruntime "on"` / `/MT`），以避免 `LNK2038` 运行时库不匹配错误。

### 12.4 第三方库预编译库兼容性

如果第三方库（如 assimp）提供预编译库且使用静态运行时（`/MT`），则整个项目必须使用静态运行时。如果预编译库使用动态运行时（`/MD`），则整个项目必须使用动态运行时。

### 12.5 当前构建配置

- **编译标准**: C++20
- **运行时库**: 静态运行时 (`/MT` for Release/Dist, `/MTd` for Debug)
- **字符集**: Unicode
- **平台**: Windows x64
- **配置**: Debug / Release / Dist

---

## 十四、版本与维护

- 本文档随项目发展持续更新
- 重大架构变更需要更新本文档
- 每次引入新的禁止模式后，必须同步更新本文档的"禁止模式"章节

---

## 十五、相机系统规范

### 14.1 虚函数必须实现（强制）

`Camera` 基类声明的虚函数，**每个派生类都必须有对应的实现**。禁止只声明不实现，或实现为空。

常见需要实现的虚函数：

| 虚函数 | 用途 | 常见错误 |
|--------|------|----------|
| `SetViewportSize(w, h)` | 窗口大小变化时更新投影矩阵 | 派生类漏实现，导致 aspect ratio 不更新 |
| `SetOrthographic(...)` | 正交投影设置 | 漏实现则相机不显示内容 |
| `SetPerspective(...)` | 透视投影设置 | 漏实现则 3D 相机崩溃 |

**教训**：PerspectiveCamera 漏实现 `SetViewportSize` 导致窗口 resize 后 aspect ratio 不更新，相机行为异常。排查时需确认虚函数分派正确（调用的是派生类实现而非基类空实现）。

### 14.2 数值防护（强制）

相机所有数学运算必须做 NaN / Inf 防护：

```cpp
// ✅ 正确写法
void SetViewportSize(uint32_t width, uint32_t height)
{
    if (width == 0 || height == 0) return;
    float aspect = static_cast<float>(width) / static_cast<float>(height);
    if (aspect <= 0.0f || std::isnan(aspect) || std::isinf(aspect)) return;
    m_AspectRatio = aspect;
    // ... 更新投影矩阵
}

// ❌ 错误写法：直接使用未经检验的除法结果
float aspect = width / height; // width/height 是整数除法，结果为 0
```

### 14.3 调试日志中避免断言触发

在 NaN 检测分支中，不要用 `QL_ASSERT(false, ...)` 配合 `QL_CORE_ERROR`，因为 `QL_ASSERT` 在 Debug 下会触发 `__debugbreak()`，导致看似 GLM 崩溃实则是调试中断：

```cpp
// ✅ 正确写法
void SetPerspective(float fov, float aspectRatio, ...)
{
    if (std::isnan(aspectRatio) || aspectRatio <= 0.0f) {
        QL_CORE_ERROR("Invalid aspect ratio: {0}", aspectRatio);
        return; // 安全退出，不要断言
    }
    // ...
}

// ❌ 错误写法
if (std::isnan(aspectRatio)) { QL_CORE_ERROR("NaN aspect"); QL_ASSERT(false, "NaN"); }
// → QL_ASSERT(false) 触发 __debugbreak()，异常被信号处理器捕获，显示为崩溃
```

### 14.4 派生类成员变量与基类接口一致

每个相机派生类必须维护自身的关键参数成员（m_FOV、m_AspectRatio、m_NearClip、m_FarClip 等），并在虚函数调用时使用这些成员而非重新计算：

```cpp
// PerspectiveCamera 必须有 m_FOV、m_AspectRatio 成员
// SetViewportSize 只更新 aspect，不改变 FOV/Near/Far
void PerspectiveCamera::SetViewportSize(uint32_t width, uint32_t height)
{
    m_AspectRatio = static_cast<float>(width) / static_cast<float>(height);
    // 使用已有的 m_FOV、m_NearClip、m_FarClip 重建投影矩阵
}
```
