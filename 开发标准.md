# QuentlamEngine 代码规范与编译标准

本文档涵盖 C++ 编码规范和项目配置规范，目的是确保代码在 Visual Studio 2022 + C++20 环境下能顺利编译通过，避免因违反规范导致编译错误。

---

## 一、编码规范（C++）

### 1.1 类型与命名

#### 枚举类型（Enum）

- **强制使用强类型枚举（`enum class`）**，禁止使用普通 `enum`，避免命名空间污染和隐式转换。
- 枚举值命名统一使用 `PascalCase`，并带上有意义的前缀。
  ```cpp
  // ✅ 正确
  enum class EAUDIOSTATE : uint8_t
  {
      Stopped = 0,
      Playing = 1,
      Paused = 2
  };

  // ❌ 错误：普通 enum 会污染命名空间
  enum EAUDIOSTATE { Stopped, Playing, Paused };
  ```
- **同一个枚举类型禁止在多个头文件中重复定义**（包括通过 `#include` 间接引入）。若发现重复定义，检查是否有 `AudioState.h` 和 `AudioModule.h` 都定义了 `EAUDIOSTATE` 的情况，删除重复的定义，保留一处即可。
- 枚举底层类型应显式指定（如 `: uint8_t`），避免平台差异。

#### 成员变量命名

- 类成员变量统一使用 `m_` 前缀 + camelCase：`m_VariableName`
- 枚举值不要加 `m_` 前缀。
- **头文件中的成员函数实现（内联）与声明的成员变量名必须严格一致。** 编译器不会提示这种错误，但会导致链接失败或运行时崩溃。
  ```cpp
  // ✅ 正确：声明和实现一致
  EAUDIOSTATE GetAudioState() const { return m_TheState; }
  // ...
  EAUDIOSTATE m_TheState = EAUDIOSTATE::Stopped;

  // ❌ 错误：声明用 m_State，实现用 m_TheState，编译器不报错但链接/运行时崩溃
  EAUDIOSTATE GetAudioState() const { return m_TheState; } // header
  // ...
  EAUDIOSTATE m_State = EAUDIOSTATE::Stopped;               // header
  ```
- **枚举类型名在声明和实现中必须完全一致。**
  ```cpp
  // ❌ 错误
  EAUDIOSTATE GetAudioState() const { return m_State; }   // header: EAUDIOSTATE
  m_State = EAUDIOState::Playing;                        // cpp: EAUDIOState（拼写错误！）

  // ✅ 正确
  EAUDIOSTATE GetAudioState() const { return m_TheState; }
  m_TheState = EAUDIOSTATE::Playing;
  ```

#### 类型别名

- 使用 `using` 而非 `typedef`：
  ```cpp
  using Ref = std::shared_ptr<T>;
  using Scope = std::unique_ptr<T>;
  ```

---

### 1.2 智能指针与工厂函数

#### `CreateRef` vs `CreateScope`

- `CreateRef<T>(args...)` 返回 `std::shared_ptr<T>`（`Ref<T>`）。
- `CreateScope<T>(args...)` 返回 `std::unique_ptr<T>`（`Scope<T>`）。
- **两者返回值类型不同，不能混用。** 存入 `Ref<T>` 的容器必须用 `CreateRef`，存入 `Scope<T>` 的容器必须用 `CreateScope`。
  ```cpp
  // ✅ 正确
  std::unordered_map<std::string, Ref<Scene>> m_Scenes;
  auto scene = CreateRef<Scene>(name);   // shared_ptr
  m_Scenes[name] = scene;

  // ❌ 错误：unique_ptr 无法隐式转为 shared_ptr
  std::unordered_map<std::string, Ref<Scene>> m_Scenes;
  auto scene = CreateScope<Scene>(name); // unique_ptr → 编译错误！
  m_Scenes[name] = scene;
  ```

#### 构造函数参数

- 若类提供非默认构造函数，**同时也要保留默认构造函数**（`= default`），以便在不提供参数时仍能创建对象（如在 `std::vector`、`std::unordered_map` 等容器中默认构造）。
  ```cpp
  // ✅ 正确：既有默认构造函数，也有带参数的构造函数
  struct Node
  {
      glm::ivec2 pos = glm::ivec2(0);
      float g = 0.0f, h = 0.0f, f = 0.0f;
      glm::ivec2 parent = glm::ivec2(0);
      Node() = default;  // 默认构造，使 unordered_map::operator[] 可用
      Node(glm::ivec2 p, float gScore, float hScore, glm::ivec2 par)
          : pos(p), g(gScore), h(hScore), f(gScore + hScore), parent(par) {}
  };

  // ❌ 错误：只有带参数构造函数
  struct Node
  {
      glm::ivec2 pos;
      float g, h, f;
      glm::ivec2 parent;
      Node(glm::ivec2 p, float gScore, float hScore, glm::ivec2 par)
          : pos(p), g(gScore), h(hScore), f(gScore + hScore), parent(par) {}
  };
  // → std::unordered_map<..., Node> openSet; openSet[key] 会报错（需要默认构造）
  ```

---

### 1.3 头文件包含（Include）

- **每个 `.cpp` 文件必须包含它所使用的所有类型的完整定义**，仅靠其他头文件的间接包含是不够的。
- 编译器按包含顺序处理，头文件中使用前向声明（`class Foo;`），在 `.cpp` 中包含完整定义。
  ```cpp
  // ✅ 正确：SceneManager.cpp 包含了 SceneSerializer.h
  #include "Quentlam/Scene/SceneManager.h"
  #include "Quentlam/Scene/SceneSerializer.h"  // SceneSerializer 的完整定义

  // ❌ 错误：仅依赖间接包含
  #include "Quentlam/Scene/SceneManager.h"
  // SceneSerializer 在 SceneManager.h 中前向声明了，但没有包含完整定义
  SceneSerializer serializer(scene);  // → "未声明的标识符"
  ```

#### 防止重复包含

- 所有头文件顶部必须有 `#pragma once` 或 `#ifndef` 保护。

---

### 1.4 函数重载与重复声明

- **同一个类中禁止存在两组签名完全相同的成员函数**（即使实现不同），这会导致"已定义或声明"错误。
- 若需要用文件名或 slotId 等不同参数，重载函数应有明确不同的参数类型：
  ```cpp
  // ❌ 错误：两组 SaveToFile(const std::string&, const SaveSchema&) 完全相同
  ESaveStatus SaveToFile(const std::string& slotId, const SaveSchema& schema);
  ESaveStatus SaveToFile(const std::string& filepath, const SaveSchema& schema);

  // ✅ 正确：参数类型有语义区分
  ESaveStatus SaveToFile(const std::string& filepath, const SaveSchema& schema);
  // 通过调用方自行调用内部辅助函数 GetSlotFilePath(slotId) 来转换
  ```

---

### 1.5 不可拷贝类型的容器使用

- 若类定义了 `= delete` 拷贝构造函数，**禁止直接将该类实例存入 `std::vector` 等需要拷贝的容器**（`push_back`、`emplace_back` 等操作在幕后需要拷贝）。
- **正确做法**：存储指针（`std::vector<T*>` 或 `std::vector<std::unique_ptr<T>>`），或改为存储 `std::shared_ptr<T>`。
  ```cpp
  class AudioPlaybackNode
  {
  public:
      AudioPlaybackNode(const AudioPlaybackNode&) = delete; // 不可拷贝
      AudioPlaybackNode(AudioPlaybackNode&&) noexcept;      // 可移动
  };

  // ❌ 错误：vector 尝试拷贝
  std::vector<AudioPlaybackNode> m_ManagedNodes;
  m_ManagedNodes.push_back(node); // → C2280: 尝试引用已删除的拷贝构造

  // ✅ 正确：存储指针
  std::vector<AudioPlaybackNode*> m_ManagedNodes;
  m_ManagedNodes.push_back(new AudioPlaybackNode(std::move(node)));
  ```

---

### 1.6 Lambda 作为模板参数（C++20 MSVC 特例）

- `decltype(lambda)` 作为 `std::unordered_map`/`std::unordered_set` 的 Hash/KeyEq 模板参数时，在 MSVC（C++20）下行为不稳定，特别是调用 `erase()`、`operator[]` 等会触发类型约束检查。
- **强制要求**：使用命名结构体作为哈希函数，而非 lambda：
  ```cpp
  // ✅ 正确：命名结构体
  struct Ivec2Hash
  {
      size_t operator()(const glm::ivec2& v) const
      {
          size_t h1 = std::hash<int>()(v.x);
          size_t h2 = std::hash<int>()(v.y);
          return h1 ^ (h2 << 16);
      }
  };
  std::unordered_map<glm::ivec2, Node, Ivec2Hash> openSet;

  // ❌ 错误：lambda 作为模板参数（MSVC C++20 行为不稳定）
  auto NodeHash = [](const glm::ivec2& v) -> size_t { ... };
  std::unordered_map<glm::ivec2, Node, decltype(NodeHash)> openSet(NodeHash);
  // → erase()/operator[] 可能触发 C2064 / C4244 等莫名错误
  ```

---

### 1.7 第三方库 API 调用

#### Dear ImGui

- 使用正确的 ImGui 标志名称（注意不同版本 ImGui 标志名称可能不同，引擎使用 ImGui 1.88）：
  ```cpp
  // ❌ 错误：标志名拼写错误
  ImGuiWindowFlags_NoBringToFrontOnTop  // 不存在

  // ✅ 正确
  ImGuiWindowFlags_NoBringToFrontOnFocus
  ```
- `ImGui::InputText` 的 `label` 参数是 `const char*`，**不接受 `std::string*`**：
  ```cpp
  // ✅ 正确：使用 char 缓冲区
  char mapNameBuf[128] = {};
  strncpy_s(mapNameBuf, m_MapName.c_str(), sizeof(mapNameBuf) - 1);
  if (ImGui::InputText("地图名称", mapNameBuf, sizeof(mapNameBuf)))
  {
      m_MapName = mapNameBuf;
  }

  // ❌ 错误：std::string* 不是 ImGui::InputText 的有效参数
  ImGui::InputText("地图名称", &m_MapName);
  ```

#### miniaudio

- 若使用 miniaudio 的 `.c` 源文件，**必须将其以 `.cpp` 扩展名加入项目**，因为项目的 `LanguageStandard=stdcpp20` 会强制所有 `.c` 文件按 C++ 编译，导致 STL 相关宏冲突。
  ```bash
  # 将 miniaudio.c 重命名为 miniaudio.cpp
  ren miniaudio.c miniaudio.cpp
  ```
- 在项目 `.vcxproj` 中同步更新文件引用。

---

### 1.8 分号与括号匹配

- **内联函数定义的末尾禁止使用多余的分号**：
  ```cpp
  // ✅ 正确
  uint32_t GetLayerCount() const { return static_cast<uint32_t>(m_Layers.size()); }

  // ❌ 错误：多余分号（虽然通常不报错，但不规范）
  uint32_t GetLayerCount() const { return static_cast<uint32_t>(m_Layers.size()); };
  ```
- **嵌套 if/for/while 等代码块必须严格匹配大括号。** 一个缺失的 `}` 会导致编译器将后续函数当作内联定义处理，产生大量无法解释的错误。**用大括号始终包裹多行代码块，即使只有一行。**

---

### 1.9 私有成员访问与链接问题

- 类的 private/protected 成员函数/变量不能被类外部直接访问。
- 若需要跨编译单元（`.cpp`）访问，使用 `public` 成员函数或友元（`friend`）包装。
- **遇到 `C2248: 无法访问 private 成员` 时**，优先检查：你调用的函数是否真的存在于类的 public 部分，还是拼写错误导致调用了错误的函数名。

#### 链接器找不到符号（LNK2019）

**常见原因及排查顺序：**

1. **`.cpp` 文件未加入 `.vcxproj`**（最常见）：新增源文件后必须同步添加到 `QuentlabEngine.vcxproj` 的 `<ItemGroup>` 中（`<ClCompile Include="...">`），否则该文件永远不会被编译进库。排查方式：对比 dumpbin 输出的符号列表与实际调用的符号。

2. **函数被内联优化掉**：在静态库中，若函数实现短小且无歧义，编译器可能将其完全内联，导致链接器看到符号为 `UNDEF`。表现为 dumpbin 中该符号标记为 `UNDEF notype () External`，但 `.cpp` 中明明有实现。解决方案：
   - 添加强制非内联修饰符：`__declspec(noinline)` 标记在实现处
   - 或将函数实现移出类体外（避免隐式 inline）
   - 或添加 `extern "C"`（仅适用于 C 风格函数）

3. **函数仅在头文件中声明但从未实现**：链接器在链接时找不到实现体。若 `.cpp` 未加入项目，也会导致此问题。

```cpp
// PersistenceModule.h
class QUENTLAM_API SaveManager
{
public:
    ESaveStatus SaveToFile(const std::string& filepath, const SaveSchema& schema); // 声明
    // ...
};

// PersistenceModule.cpp
__declspec(noinline) // 防止被内联，确保静态库中保留外部符号
ESaveStatus SaveManager::SaveToFile(const std::string& filepath, const SaveSchema& schema)
{
    // 实现...
}
```

**排查 LNK2019 的标准流程：**
1. 从链接错误信息中提取"无法解析的符号"（mangled name）
2. 使用 `dumpbin /symbols QuentlamEngine.lib | findstr "SymbolName"` 检查该符号在库中的状态
3. 若为 `UNDEF` → 该符号从未被编译进库 → 检查 `.cpp` 是否在 vcxproj 中 + 是否被内联优化掉
4. 若库中不存在该符号 → 搜索源码确认实现是否存在
5. 若库中符号状态正常 → 检查调用方是否正确 `#include` 了定义该符号的头文件

---

### 1.10 访问不存在/未定义的类成员

- 编译器通常不会报告"你调用了一个不存在的函数"，而是报出 `EAUDIOState: 不是类或命名空间名称` 或 `无法访问 private 成员` 等误导性错误。
- **遇到这类"找不到 X" 的错误时，优先检查：该类型是否在当前文件中正确 `#include`？成员变量名/函数名是否拼写正确？**

---

## 二、项目配置规范

### 2.2 Visual Studio 项目文件（.vcxproj）文件管理

#### 新增源文件必须加入项目

- 新增 `.cpp` 或 `.h` 文件后，**必须手动将其添加到 `.vcxproj`** 的 `<ItemGroup>` 中：
  - 头文件：添加 `<ClInclude Include="...">`
  - 源文件：添加 `<ClCompile Include="...">`
- 仅在文件系统中创建文件**不会**使其自动被编译。这是链接错误的常见原因。
- 确认方式：搜索文件名是否在 `.vcxproj` 中有对应条目：
  ```
  <ClCompile Include="src\Quentlam\Persistence\PersistenceModule.cpp" />
  ```

#### C 文件处理

- 项目 `LanguageStandard` 设置为 `stdcpp20` 时，`.c` 文件会被强制按 C++ 编译，与 STL 头文件冲突（`yvals_core.h` 检测到 `__cplusplus` 但期望 C 编译器）。
- **解决方案**：将所有 C 第三方库源文件（`.c`）的扩展名改为 `.cpp`，或创建独立的静态库项目处理 C 代码。

#### 预编译头（PCH）

- PCH 文件（`qlpch.h`）包含大量 C++ 标准库头文件，**C 源文件不能使用 PCH**（会导致 `C1853` 错误）。
- 确保所有 `.c` 文件或非 PCH 兼容的 `.cpp` 文件设置为 `PrecompiledHeader: NotUsing`。

#### 平台工具集

- 必须使用 **Visual Studio 2022 (v143)** 或更高版本。
- 目标平台：**x64**（32 位构建不受支持）。

#### 项目引用与构建顺序

- 作为静态库的 `QuentlamEngine` 项目必须先于依赖它的项目（`Sandbox`、`QL-Editor`）构建完成。
- 若遇到 `LNK1104: 无法打开 QuentlamEngine.lib`，执行 **Build → Rebuild Solution** 而非仅 Build，或手动先编译 `QuentlamEngine` 项目。

---

## 三、常见编译错误速查

| 错误代码 / 错误信息 | 常见原因 | 解决方案 |
|---|---|---|
| `C2065: 未声明的标识符` | 1. 缺少 `#include`<br>2. 成员变量名/函数名拼写错误<br>3. 头文件未被正确包含 | 检查所有用到的类型是否有完整定义 |
| `C2535: 已经定义或声明成员函数` | 同一类中存在两组签名完全相同的函数 | 删除重复的重载 |
| `C2280: 尝试引用已删除的拷贝构造` | 不可拷贝类型被存入 vector/map 等容器 | 改为存储指针或 shared_ptr |
| `C2064: 项不会计算为接受 N 个参数的函数` | lambda 作为 unordered_map 模板参数时 MSVC 异常 | 改用命名结构体 |
| `C3646: 未知重写说明符` | `override` 关键字用于非虚函数，或函数名拼写不一致 | 检查函数名和返回类型 |
| `C1853: 预编译头文件来自编译器的其他版本` | `.c` 文件使用了 C++ PCH，或 PCH 版本不匹配 | `.c` 文件禁用 PCH，或将 `.c` 改为 `.cpp` |
| `C1189: Unexpected compiler, expected C++ compiler` | `.c` 文件被项目级 `LanguageStandard=stdcpp20` 强制当 C++ 编译 | 将 `.c` 重命名为 `.cpp` |
| `C2248: 无法访问 private 成员` | 类外部直接访问了 private 方法 | 改为通过 public 方法访问，或检查是否调用了不存在的 public 方法 |
| `LNK1104: 无法打开 *.lib` | 依赖的库项目未先构建，或库不存在 | Rebuild Solution，或确认库路径正确 |
| `LNK2019: 无法解析的外部符号` | 1. `.cpp` 未加入 `.vcxproj`<br>2. 函数被内联优化掉（静态库中符号为 UNDEF）<br>3. 函数在头文件中声明但未实现 | 确认文件在项目中有 `<ClCompile>` 条目；检查链接器输出的 UNDEF 符号；添加强制非内联修饰符或确保有非内联实现 |
| `ImGuiWindowFlags_NoBringToFrontOnTop: 未声明的标识符` | ImGui 标志名称拼写错误 | 确认为 `NoBringToFrontOnFocus` |
| `ImGui::InputText: 函数不接受 2 个参数` | `ImGui::InputText` 不接受 `std::string*` | 使用 `char buf[size]` 缓冲区 |
| `C2065: 成员变量名 未声明的标识符`（虚函数定义中） | MSVC PCH 缓存了旧版头文件，即使源文件已正确修改，编译器仍报告成员不存在 | 删除 `$(IntDir)` 中对应的 `.obj` 文件后重建；必要时清理整个 `obj/` 目录 |
| `C1083: 无法打开源文件 vendor\*.c` | vcxproj 中 ClCompile 条目引用了 `.c` 文件，但实际文件是 `.cpp` | 将 `<ClCompile Include="vendor\...\.c">` 改为 `.cpp` |
