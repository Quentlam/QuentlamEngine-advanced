# QuentlamEngine 开发清单

## 项目完成状态

> **状态**: 全部开发任务已完成 ✅
> **完成时间**: 2026-05-29
> **总阶段数**: 6 (P0-P5)

### 已完成清单

| 阶段 | 任务 | 状态 |
|------|------|------|
| P0-A | Prefab 实例化系统 | ✅ |
| P0-B | UI 控件系统 | ✅ |
| P0-C | 2D 射线检测 | ✅ |
| P0-D | 导航网格编辑器 | ✅ |
| P1-A | 动画状态机 | ✅ |
| P1-B | 资源 GUID 引用系统 | ✅ |
| P1-C | 输入系统重构 | ✅ |
| P1-D | 字体 + 国际化 | ✅ |
| P2-A | 后处理管线 | ✅ |
| P2-B | 2D 光照 | ✅ |
| P2-C | LOD 系统 | ✅ |
| P3-A | 地形系统 | ✅ |
| P3-B | 植被实例化 | ✅ |
| P4-A | 2D 物理关节 | ✅ |
| P4-B | 触发器 + 物理材质编辑器 | ✅ |
| P5-A | 网络同步基础 | ✅ |
| P5-B | 构建自动化 | ✅ |

---

## 目标
把 QuentlamEngine 发展成一个能够支持“星露谷”类型 2D 生活模拟游戏的通用引擎框架。

## 一、项目定位

QuentlamEngine 当前已经具备较完整的底层能力，包括：
- 2D / 3D 渲染
- ECS 与场景管理
- 2D 物理与碰撞
- 资源加载与线程池
- 编辑器与调试基础

但要支撑“星露谷”类型的项目，还需要补齐的是“游戏框架层”与“内容生产层”，而不是继续堆底层图形能力。

## 二、必须补齐的核心模块

### 1. `WorldGridModule`
职责：
- 负责格子地图、分层、区域、碰撞查询、地图切换。
- 负责世界坐标与格子坐标之间的转换。
- 负责地图触发器、传送门、区域边界与局部加载。

输出：
- `TileMap`
- `Region`
- `Portal`
- `Chunk`
- `GridQuery`

### 2. `AnimationModule`
职责：
- 负责 2D 精灵动画、方向状态、播放控制、事件帧。
- 负责角色、工具、作物、动物、门、特效等统一动画播放。

输出：
- `AnimationClip`
- `Animator`
- `SpriteAtlasBinding`
- `AnimationStateMachine`

### 3. `InteractionModule`
职责：
- 负责玩家与世界的交互入口。
- 负责格子选择、实体选择、触发器执行、上下文提示。

输出：
- `Interactable`
- `InteractionQuery`
- `ActionDispatcher`
- `InteractionPrompt`

### 4. `InventoryModule`
职责：
- 负责物品、堆叠、工具、容器、装备、交易、配方。
- 负责物品实例状态与定义数据分离。

输出：
- `ItemDef`
- `ItemStack`
- `InventorySlot`
- `Container`
- `Recipe`
- `ShopStock`

### 5. `SimulationModule`
职责：
- 负责时间、日历、天气、季节、日切换。
- 负责每日结算、事件广播、规则触发。

输出：
- `GameClock`
- `Calendar`
- `WeatherState`
- `SeasonRule`
- `DailyEventBus`

### 6. `NpcModule`
职责：
- 负责 NPC 日程、路径、关系、对话入口和状态记忆。
- 负责 NPC 的地点切换、行为切换与条件判断。

输出：
- `NpcSchedule`
- `PathGoal`
- `RelationshipState`
- `NpcMood`

### 7. `QuestEventModule`
职责：
- 负责任务、事件树、过场、日记、邮件。
- 负责条件触发与分支推进。

输出：
- `EventGraph`
- `QuestState`
- `CutsceneStep`
- `MailItem`
- `EventCondition`

### 8. `PersistenceModule`
职责：
- 负责存档、读档、版本迁移、局部保存、完整性校验。
- 负责把世界状态恢复为可运行的完整快照。

输出：
- `SaveSlot`
- `SaveSchema`
- `MigrationStep`
- `StateSerializer`
- `SaveValidator`

### 9. `UIGameModule`
职责：
- 负责运行时 UI、焦点、窗口栈、输入上下文。
- 负责游戏内界面与编辑器界面分离。

输出：
- `ScreenStack`
- `WidgetTree`
- `FocusManager`
- `InputContext`
- `CursorState`

### 10. `ContentPipelineModule`
职责：
- 负责资源导入、图集、校验、热加载、内容打包。
- 负责把编辑资源转换为运行时资源。

输出：
- `Importer`
- `AtlasPacker`
- `ContentManifest`
- `HotReloadWatcher`
- `AssetValidator`

### 11. `AudioModule`
职责：
- 负责音乐、音效、环境音、分层播放、淡入淡出。
- 负责事件驱动的音频播放控制。

输出：
- `AudioBus`
- `MusicController`
- `SfxEmitter`
- `AmbientZone`
- `AudioMixerState`

### 12. `LocalizationModule`
职责：
- 负责字符串表、语言切换、字体回退、文本格式化。
- 负责 UI 文本与内容文本的统一管理。

输出：
- `StringTable`
- `LocalizedText`
- `FontFallbackSet`
- `TextFormatter`

### 13. `DebugToolsModule`
职责：
- 负责地图检查、碰撞可视化、实体拾取、存档诊断。
- 负责开发期叠层、调试命令与校验输出。

输出：
- `MapInspector`
- `ColliderOverlay`
- `EntityPicker`
- `SaveValidator`
- `DebugConsole`

### 14. `ModdingModule`
职责：
- 负责内容包、扩展点、脚本桥接、加载顺序。
- 负责模组的发现、解析与安全加载。

输出：
- `ModLoader`
- `ContentPack`
- `ScriptBridge`
- `LoadOrderResolver`
- `ModManifest`

## 三、推荐目录结构

```text
QuentlamEngine/
  src/Quentlam/
    World/
    Gameplay/
    UI/
    Audio/
    Content/
    Persistence/
    DebugTools/
    Modding/
```

## 四、开发顺序

### 第一阶段：打地基
1. `WorldGridModule`
2. `TileMap` 数据结构
3. `AnimationModule`
4. `InteractionModule`
5. `PersistenceModule`

### 第二阶段：形成可玩性
1. `InventoryModule`
2. `SimulationModule`
3. `UIGameModule`
4. `NpcModule`
5. `QuestEventModule`

### 第三阶段：内容生产能力
1. `ContentPipelineModule`
2. `LocalizationModule`
3. `AudioModule`
4. `DebugToolsModule`

### 第四阶段：MOD 框架
1. `ModdingModule` + Lua 脚本引擎（见第九节）

## 五、最小可玩版本目标

做到以下内容后，才算进入“可玩原型”阶段：
- 玩家可以在网格地图上移动
- 玩家可以与地块或物体交互
- 玩家可以打开背包并拾取物品
- 游戏内有时间推进和天气变化
- 游戏可以存档和读档
- 至少有一个 NPC 和一条事件链

## 六、技术原则

- 玩法层必须数据驱动。
- 地图、物品、NPC、事件要尽量配置化。
- 存档必须从一开始就设计版本迁移。
- 编辑器要为内容生产服务，而不是只用于摆放实体。
- 渲染、物理、逻辑、UI 要职责分离。
- 优先做通用框架，再做具体玩法。

## 七、下一步建议

1. 先实现 `WorldGridModule` 和 `TileMap`。
2. 再实现 `AnimationModule` 和 `InteractionModule`。
3. 然后补 `PersistenceModule`。
4. 最后再扩展 UI、NPC、任务、内容管线。

---

## 八、P0 编辑器核心功能（第四轮开发，2026-05-28）

### 已完成

- **P0-1: Undo/Redo 系统（Command 模式）**
  - `EditorCommand.h/.cpp` - Command 基类、EditorCommandStack、CreateEntityCommand、DeleteEntityCommand、TransformCommand、BatchCommand
  - `Scene` 新增 `DestroyEntity()`、`GetEntity()` 方法
  - EditorLayer 集成 Ctrl+Z/Y 快捷键，Delete 键改用 CommandStack，右键菜单和菜单栏创建实体均通过 Command
  - Play 时自动清空撤销栈

- **P0-2: TileMap 视口绘制交互**
  - `TileMapEditorPanel` 新增 `HandleBrushStrokeContinuous()`、`HandleBrushStrokeContinuousReset()` 支持拖拽连续绘制
  - `EditorLayer` 视口鼠标点击/拖拽时将屏幕坐标转为世界坐标，调用 TileMap 笔刷绘制

- **P0-3: 资源拖放创建实体**
  - EditorLayer 已存在视口拖放逻辑（.png/.jpg/.obj/.fbx），已集成 CommandStack 创建

- **P0-4: Scene Hierarchy 搜索过滤**
  - Hierarchy 面板顶部添加搜索框，实时按 Tag 过滤实体列表（大小写不敏感）

- **P1-5: Shader/Material 选择器**
  - `MaterialDefaults` 新增 `GetAllDefaults()` 接口
  - Properties Panel SpriteRendererComponent 区域添加 Material 下拉选择器，支持默认材质和内置材质切换

- **P1-6: UI 编辑器实时同步**
  - `UIGameModule` 新增 `GetRegisteredScreens()` 访问器
  - `UIHierarchyPanel` 新增 `SetUIGameModule()` 接口，`OnImGuiRender()` 改为每帧重建树实现实时同步
  - EditorLayer 构造函数连接 `UIHierarchyPanel` 到 `UIGameModule`
  - 支持查看已注册但未激活的 Screen，提供 Push 按钮

- **P1-7: 完善 Ctrl+C/V**
  - EditorLayer 新增 `m_CopiedEntity` 成员，Ctrl+C 复制选中实体 ID，Ctrl+V 在原位偏移创建新实体并复制所有渲染组件
  - 粘贴后新实体向右上方偏移 0.5 单位，自动选中

- **P2-8: 多选支持**
  - `m_SelectedEntities` 支持 Shift+点击多选，清除历史选择
  - Hierarchy 空白处点击清空所有选中
  - 右键菜单支持"删除选中实体 (N)"批量删除，通过 BatchCommand 支持撤销

- **P2-9: 资源右键菜单**
  - Content Browser 文件/目录支持右键菜单：重命名（弹出输入框）、在资源管理器中显示（ShellExecute）、删除（带确认对话框）
  - EditorLayer 新增 `m_RenamingAsset`、`m_DeletingAsset`、`m_RenameBuffer` 等状态变量

- **P2-10: Shader 热编译**
  - MaterialEditorPanel 重构，`LoadShaderSourceFromFile()` 从磁盘解析 `#type vertex/fragment` 单文件 Shader
  - "从磁盘热重载"按钮：选中自定义材质后，修改 .glsl 文件点击按钮即可重新编译 Shader 并应用到材质
  - 编译错误信息显示在编辑器内

### 进行中

- **P3-1: Lua 源码引入** ✅
  - 将 Lua 5.4.6 源码引入 `vendor/lua/`（59 个核心源文件）

- **P3-3: Sol2 基础绑定** ✅
  - `LuaScriptBridge` 实现 `ScriptBridge` 接口
  - 绑定 `QL` 枚举（VERSION、ENGINE_NAME）
  - 绑定 `QL_Log`、`QL_LogWarn`、`QL_LogError` 调试函数
  - 支持 `load()`、`update(dt)`、`onSave()`、`onLoad()` 生命周期调用

- **P3-4: ModdingModule 框架** ✅
  - `ModLoader.cpp` 完整实现：manifest.json 解析（id/name/author/version/dependencies/content）
  - 目录扫描 `ScanModsDirectory()` 自动发现 mods 文件夹下的 MOD
  - 依赖排序（拓扑排序 + 循环检测）
  - MOD 启用/禁用/重载完整生命周期
  - `ModdingModule` 接入 Application：启动时扫描，关闭时清理
  - 示例 MOD：`Sandbox/mods/example-mod/`，包含 manifest.json + main.lua

- **P3-7: 示例 MOD + 文档** ✅
  - `docs/MOD开发指南.md` — 完整 MOD 开发文档
  - 涵盖目录结构、manifest.json 字段说明、生命周期函数、引擎 API
  - 包含开发流程、调试方法、发布步骤

---

## 九、MOD 框架：Lua + SWIG（P3 阶段）

### 背景

QuentlamEngine 为 C++ 引擎，需要脚本层支持 MOD 扩展。考虑到：
- Android 发布目标 → C# .NET 运行时依赖成本高，不可行
- Lua 轻量（~15KB，无运行时依赖）、Android NDK 原生支持、业界手游主流方案
- SWIG 提供半自动化绑定，减少手写胶水代码
- MOD 开发者上手门槛低（1-2 天可入门）

### 技术方案

```
C++ 引擎  ←(SWIG 绑定层)→  Lua 5.4 脚本
                              ↓
                         Sol2 C++ API
```

- **Lua**：运行时引擎，MOD 脚本执行环境
- **Sol2**：C++ 到 Lua 的现代绑定库，比手写绑定更清晰
- **SWIG**：辅助生成部分重复性绑定代码

### P3 开发任务

#### P3-1: Lua 源码引入
- 将 Lua 5.4 源码引入 `vendor/lua/` 目录
- Lua 5.4.tar.gz 解压后仅 4 个 `.c` + 2 个 `.h` 文件

#### P3-2: CMake 构建配置
- 在 QuentlamEngine CMakeLists.txt 中配置 Lua 编译为静态库
- 解决 Android NDK 交叉编译问题
- 验证 `lua_State*` 可用

#### P3-3: Sol2 引入与基础绑定
- 将 Sol2 引入 `vendor/sol2/`
- 绑定基础引擎 API：`Game`、`Scene`、`Entity`、`TransformComponent`
- 验证 Lua 脚本可读取/修改引擎对象

#### P3-4: ModdingModule 框架
- `ModManifest` — mod.json 清单解析（id、name、version、dependencies）
- `ModLoader` — MOD 目录扫描、依赖排序、加载顺序
- `ModSandbox` — 沙箱隔离（每个 MOD 独立 Lua state）

#### P3-5: 扩展点 API 绑定
- `Inventory` — `AddItem`、`RemoveItem`、`GetItem`、`GetItemCount`
- `GameClock` — `GetDay`、`GetSeason`、`AdvanceDay`
- `EventBus` — `Subscribe`、`Fire`、`Unsubscribe`
- `NpcModule` — `GetNpc`、`SetRelationship`、`GetRelationship`
- `QuestSystem` — `StartQuest`、`CompleteQuest`、`GetQuestState`

#### P3-6: MOD 生命周期管理
- MOD 初始化 `load()`
- 每帧更新 `update(dt)`
- 存档时 `onSave(context)`
- 读档时 `onLoad(context)`
- 引擎事件挂接（`OnDayStart`、`OnItemUsed`、`OnNpcTalked`）

#### P3-7: 示例 MOD + 文档
- 一个可运行的示例 MOD（添加新物品 + 新事件）
- MOD 开发文档（编写规范、API 列表、调试方法）

### MOD 目录结构

```
mods/
  my-mod/
    manifest.json          # MOD 元信息
    scripts/
      main.lua            # 入口脚本
    assets/
      textures/           # MOD 资源
      data/               # MOD 数据
```

### 开发顺序

1. P3-1 Lua 源码引入
2. P3-2 CMake 构建配置
3. P3-3 Sol2 基础绑定
4. P3-4 ModdingModule 框架
5. P3-5 扩展点 API 绑定
6. P3-6 MOD 生命周期
7. P3-7 示例 MOD + 文档

---

## 十、开发标准

### 10.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| C++ 类/结构体 | PascalCase | `LuaScriptInstance`、`Rigidbody2DComponent` |
| C++ 成员变量 | `m_` 前缀 + PascalCase | `m_EntityID`、`m_ScriptPath` |
| C++ 静态变量 | `s_` 前缀 + PascalCase | `s_SharedLua`、`s_SharedInitialized` |
| C++ 方法/函数 | PascalCase | `InstantiateLuaScripts`、`OnUpdateRuntime` |
| C++ 文件 | PascalCase 或 snake_case | `LuaScriptInstance.h`、`Scene.cpp` |
| Lua 脚本 | snake_case | `main.lua`、`snake_game` |
| Lua 函数/变量 | snake_case | `entity_set_position`、`grid_to_world` |
| Lua 全局表 | PascalCase | `Game`、`DIR`、`Entity_Create` |
| 组件类型名 | `XxxComponent` | `LuaScriptComponent`、`TransformComponent` |
| 模块类型名 | `XxxModule` | `NpcModule`、`InventoryModule` |
| 枚举值 | PascalCase | `SceneState::Play`、`GameState::Playing` |
| 场景/资源路径 | 相对路径，`/` 分隔 | `mods/snake-game/scripts/main.lua` |

### 10.2 代码质量标准

**所有新增代码必须满足：**
- 编译零错误（允许警告但不得有新警告引入）
- 不修改已有功能的编译行为
- 遵循现有代码风格（缩进、命名与项目一致）
- 不得引入不必要的第三方依赖
- 新增的 `.h/.cpp` 文件必须加入对应的 `.vcxproj`

**提交前自检清单：**
- [ ] `LuaScriptInstance` 是否有独立的 `.h` 和 `.cpp`？
- [ ] 是否在 `Components.h` 末尾添加了新组件（不得插入中间）？
- [ ] `Scene.h` 是否新增了公开方法声明？
- [ ] `Scene.cpp` 是否在对应 `.h` 的方法前包含正确的头文件？
- [ ] 引擎库编译通过（`QuentlamEngine.vcxproj`）
- [ ] 编辑器编译通过（`QL-Editor.vcxproj`）

### 10.3 架构约束

- **脚本层只读不写 C++**：Lua 脚本只能通过引擎暴露的 API 操作场景，不得直接访问 C++ 对象
- **组件单一职责**：每个组件只存储数据，逻辑放在系统/模块层
- **场景解耦**：游戏逻辑不得硬编码在 `EditorLayer`，必须通过 Scene/Entity 体系承载
- **序列化先行**：任何新增的可编辑字段必须同步更新场景序列化逻辑
- **Play 模式安全**：编辑器 Play 模式崩溃不得影响编辑状态，异常被 `try/catch` 包裹

### 10.4 组件开发模板

新增组件时，按以下顺序添加文件：

```
1. Components.h          → 添加 struct XxxComponent（末尾）
2. ModuleOrSystem.h/cpp → 添加业务逻辑（如需）
3. Scene.cpp            → 在 OnRuntimeStart/Stop/Update 中添加钩子（如需）
4. EditorLayer.cpp      → 在属性面板渲染 UI
5. SceneSerializer.cpp  → 序列化/反序列化（如需）
6. .vcxproj            → 注册新的 .cpp 文件
```

---

## 十一、编辑器核心功能（第五轮，2026-05-28）

### 已完成

- **P3-1: Lua 源码引入** ✅
  - 将 Lua 5.4.6 源码引入 `vendor/lua/`

- **P3-3: Sol2 基础绑定** ✅
  - `LuaScriptBridge` 实现 `ScriptBridge` 接口
  - 绑定 `QL` 枚举、`QL_Log`、`Input_*`、`Math_*` 等 API

- **P3-4: ModdingModule 框架** ✅
  - `ModLoader` 实现 manifest.json 解析、目录扫描、依赖排序

- **P3-7: 示例 MOD + 文档** ✅
  - `docs/MOD开发指南.md` + `Sandbox/mods/example-mod/`

- **P4-1: LuaScriptInstance 核心架构** ✅
  - `LuaScriptInstance.h/.cpp`：每个实体持有独立 `sol::environment`，共享全局 API
  - 生命周期：`Instantiate()` → `OnLoad()` → 每帧 `OnUpdate(dt)` → `OnDestroy()`
  - 全局 API（Key、Input、Math）一次性设置在 `s_SharedLua`；实例 API（Entity_*、Game_*）在每个 `sol::environment` 中单独注册
  - `LuaScriptBridge` 保持独立，不受新架构影响（向后兼容）

- **P4-2: LuaScriptComponent ECS 集成** ✅
  - `Components.h` 添加 `LuaScriptComponent`
  - `Scene.h/.cpp` 新增 `InstantiateLuaScripts()`、`UpdateLuaScripts()`、`DestroyLuaScripts()`
  - `OnRuntimeStart()` 调用 `InstantiateLuaScripts()`，`OnRuntimeStop()` 调用 `DestroyLuaScripts()`
  - `OnUpdateRuntime()` 调用 `UpdateLuaScripts(ts)`

- **P4-3: 编辑器 UI 脚本组件面板** ✅
  - `EditorLayer.cpp` 属性面板添加 **Scripts** 区域
  - 脚本路径输入框 + 浏览按钮
  - 显示 Instance 状态（Active / Inactive / No script）
  - Edit 模式下可编辑，Play 模式下锁定

- **P4-4: 统一渲染管道** ✅
  - `Scene::RenderLuaEntities()` 读取 Lua 脚本中的 entity 状态
  - 通过 `Renderer2D::DrawQuad` 渲染，不再依赖 ImGui 绘制
  - 实体的 world position / color / visibility 由 Lua 脚本每帧更新

- **P4-5: 工作目录自动解析** ✅
  - `QLEditorApp.cpp` 构造函数自动检测 `bin/` 文件夹
  - 无论从 VS 还是直接双击 exe 启动，均将 CWD 统一到项目根目录
  - 解决了 `mods/snake-game/scripts/main.lua` 相对路径加载失败问题

- **P4-6: 贪吃蛇 ECS 适配** ✅
  - `Sandbox/mods/snake-game/scripts/main.lua` 重构
  - 使用 `Entity_Create`/`Entity_SetPosition`/`Entity_SetColor` 替代原有的 ImDrawList 渲染
  - WASD/方向键通过 `Input_IsKeyPressed()` 轮询
  - `SandboxApp.cpp` 移除 `SnakeGameLayer`，Sandbox 恢复为干净演示场景

### 进行中

（无）

### 待开发

| 序号 | 任务 | 状态 |
|------|------|------|
| P5-1 | UI 实体纳入 ECS 层级 | **已完成** |
| P5-2 | Prefab 系统 | **已完成** |
| P5-3 | 控制台面板（Console） | **已开发** |
| P5-4 | 编辑器 Profiler | **已开发** |
| P5-5 | Physics2D 完善 | **已完成** |
| P5-6 | 粒子系统 | **已完成** |
| P5-7 | 音频与 ECS 集成 | **已完成** |
| P5-8 | Camera2D 跟随系统 | **已完成** |
| P5-9 | 音效音乐统一接口 | **已完成** |
| P5-10 | Sprite Sheet 动画编辑器 | **已完成** |
| P5-11 | 资源 Asset Browser 重构 | **已开发** |
| P5-12 | TileMap 图层编辑增强 | **已完成** |

### 后续任务

| 序号 | 任务 | 说明 |
|------|------|------|
| P4-11 | 撤销/重做（Undo/Redo）完善 | 当前 EditorCommandStack 基础功能已完成 |
| P4-12 | 场景导出为 MOD | 将场景打包为可分发的 mod |
| P4-13 | Lua API 文档生成 | 从脚本绑定自动生成 API 文档 |
| P4-14 | 粒子系统 | GPU 粒子或 CPU 粒子组件 |
| P4-15 | 音频组件集成 | AudioModule 与 ECS 组件绑定 |

### 技术债务（已清理）

| 原优先级 | 原债务项 | 现状 |
|---------|---------|------|
| P0 | 场景序列化空壳 | ✅ P4-7 已解决 |
| P1 | Transform 无父子层级 | ✅ P4-10 已解决 |
| P2 | 无脚本热重载 | ✅ P4-8 已解决 |
| P2 | SnakeGameLayer 未删除 | ✅ P4-6 已解决 |

---

## 十三、最小可玩版本验证流程

完成 P4-7 后，应执行以下验证：

1. 打开 QL-Editor
2. 创建 Entity，添加 **Lua Script** 组件，填入 `mods/snake-game/scripts/main.lua`
3. 保存场景（Ctrl+S）
4. 关闭编辑器，重新打开
5. 加载场景（Ctrl+O）
6. 点击 **Play**，验证：
   - 蛇正常显示并移动
   - WASD/方向键控制方向
   - 吃食物后分数增加
   - 撞墙/撞自己后 Game Over
   - 点击 **Stop** 回到 Edit 模式
7. 如发现问题，优先检查 `LuaScriptInstance` 日志输出（`QL_Log`）

---

## 十五、2D 游戏能力补齐（第六轮，2026-05-29）

### 背景

QuentlamEngine 已具备基础渲染、物理、ECS，但距离完整制作"星露谷"类型 2D 游戏仍有系统性差距。以下按制作流程排序，从"编辑器内制作内容"到"游戏运行体验"逐层补齐。

---

### 十五、1 预研确认（美术规范）

在开发具体模块之前，需先确定 2D 美术规格，作为所有渲染模块的输入约束：

| 参数 | 规格 | 说明 |
|------|------|------|
| 默认分辨率 | 1280×720 | 编辑器视口和游戏窗口默认分辨率 |
| 纹理格式 | PNG (RGBA8) | 带透明度，支持 2D 精灵 |
| 精灵像素密度 | 64×64 | 每格地图对应 64×64 像素 |
| 地图缩放系数 | 0.5x | 屏幕像素密度高于游戏像素密度时的整体缩放 |
| 着色器模型 | GLSL 330 | 确保着色器兼容 |

---

### 十五、2 编辑器基础补齐（P5 系列）

#### P5-1: UI 实体纳入 ECS 层级
- **现状**：UI 层级（UIScreen）和场景实体（Entity）是两套完全独立的系统
- **目标**：将 UI 实体也纳入场景层级，支持与场景实体相同的 Add Component 操作
- **方案**：
  - 将 `UIScreen` 改为由 `Entity` 持有（`Entity.AddComponent<UIEntityComponent>()`）
  - `UIEntityComponent` 持有 UIScreen 的所有属性（ID/Position/Size/Visibility/Style）
  - 现有 ScreenStack/FocusManager/InputContext 作为渲染后端，不影响编辑器体验
  - 编辑器的 UI 层级面板改为读取场景中持有 UIEntityComponent 的实体
- **文件**：
  - `Components.h` → 新增 `struct UIEntityComponent`（ScreenId/Visible/Blocking/Focusable/ZOrder/InputContext）
  - `SceneSerializer.cpp` → UIEntityComponent 序列化/反序列化
  - `EditorLayer.cpp` → UIEntityComponent 菜单项和属性面板
- **依赖**：无
- **状态**：**已完成** ✅

#### P5-2: Prefab 系统
- **现状**：场景序列化只能导出 JSON 实体，无法复用
- **目标**：支持实体预制体（Prefab）：定义 → 实例化 → 覆盖 → 嵌套
- **方案**：
  - 新增 `PrefabAsset` 资源格式（`.qult` 二进制）
  - `SceneSerializer` 支持序列化/反序列化 PrefabReference
  - `Entity::InstantiatePrefab()` 实例化预制体到场景
  - 支持嵌套 Prefab（Prefab A 包含 Prefab B）
  - 支持属性覆盖（局部覆盖预制体默认值）
- **文件**：
  - `PrefabAsset.h` → 预制体数据结构（PrefabEntityData/PrefabComponentData）
  - `Components.h` → 新增 `PrefabReferenceComponent`
  - `SceneSerializer.cpp` → PrefabReferenceComponent 序列化/反序列化
  - `EditorLayer.cpp` → Prefab 菜单项和属性面板
- **依赖**：P5-1
- **状态**：**已完成** ✅

#### P5-3: 控制台面板（Console）
- **现状**：编辑器内无 Console，调试输出不可见
- **目标**：编辑器内置 Console 面板，显示 `QL_Log` 系列输出，支持输入调试命令
- **方案**：
  - `EditorConsolePanel.h/.cpp` → ImGui 多行输出框
  - 接管 `Log` 模块的输出，编辑器模式下同时输出到 Console 面板
  - 支持命令历史（↑↓）、命令过滤、错误等级高亮
  - 可折叠/展开，支持 Clear
- **文件**：
  - `EditorConsolePanel.h/.cpp` → 新增
  - `DebugToolsModule.h` → 已有的 DebugConsole 提供命令注册和执行
  - `EditorLayer.cpp` → 集成 Console 面板
- **依赖**：无
- **状态**：**已完成** ✅
  - `EditorConsolePanel.h/.cpp` 实现完整 Console 面板
  - 集成到 EditorLayer，支持 Clear/Copy/Filter/AutoScroll
  - 绑定 DebugConsole 命令系统（help/clear/checkpoint 等）
  - 通过 `窗口 → 控制台` 菜单打开

#### P5-4: 编辑器 Profiler
- **现状**：仅有帧率计数
- **目标**：显示帧时间、Draw Call、物理时间、脚本时间分项统计
- **方案**：
  - `Profiler` 类（已有基础）扩展为支持分模块计时
  - `EditorLayer` 在角落叠加显示各模块耗时
  - 未来可发展为独立 Profiler 面板
- **文件**：
  - `ProfilerPanel.h/.cpp` → 重构为 `EditorProfilerOverlay`
  - `EditorLayer.cpp` → 集成显示
- **依赖**：无
- **状态**：**已完成** ✅
  - `EditorProfilerOverlay` 实时采集 `Renderer2D::GetStatistics()` 和 `Renderer3D::GetStatistics()`
  - 显示 FPS、帧时间、Draw Calls (2D/3D)、Quad/Cube 数量、顶点数量
  - 右下角浮动叠加显示，支持 FPS 历史折线图
  - 颜色编码（绿色=正常，黄色=警告，红色=过高）

---

### 十五、3 核心游戏模块

#### P5-5: Physics2D 物理完善
- **现状**：仅有 BoxCollider2D，无关节、触发器、物理材质
- **目标**：补齐 2D 物理缺失能力
- **方案**：
  - 新增 `CircleCollider2DComponent`（圆形碰撞体，含 Offset/Radius/Density/Friction/Restitution）
  - 新增 `Rigidbody2DComponent` CollisionLayer 和 CollisionMask 字段（位掩码碰撞层级）
  - 新增射线检测接口（`Physics2D::Raycast`）
- **文件**：
  - `Components.h` → `CircleCollider2DComponent`、`Rigidbody2DComponent` 碰撞层级字段
  - `EditorLayer.cpp` → CircleCollider2DComponent 菜单项和属性面板、碰撞层级 UI
- **依赖**：P5-1
- **状态**：**已完成** ✅

#### P5-6: 粒子系统
- **现状**：无
- **目标**：支持 2D 粒子效果（火焰、爆炸、雨雪、特效）
- **方案**：
  - 新增 `ParticleSystem2DComponent`（ECS 组件）
  - 新增 `ParticleEmitter` 数据结构（发射率、生命、速度、旋转、缩放、颜色渐变）
  - `Renderer2D` 支持粒子批次绘制（Billboard 精灵）
  - 编辑器粒子组件面板：编辑器内调节参数，实时预览
  - 支持 SpriteSheet 粒子纹理（多帧动画粒子）
- **文件**：
  - `ParticleSystem.h/.cpp` → 粒子模拟逻辑
  - `Components.h` → `ParticleSystem2DComponent`
  - `Renderer2D.cpp` → 粒子批次渲染
  - `EditorLayer.cpp` → 粒子组件编辑器
- **依赖**：P5-1
- **状态**：**已完成** ✅

#### P5-7: 音频与 ECS 集成
- **现状**：`AudioModule` 是独立模块，与 ECS 无关联
- **目标**：将音频源作为 ECS 组件，支持空间化
- **方案**：
  - 新增 `AudioSource2DComponent`（播放、停止、音量、音调、循环）
  - 新增 `AudioListenerComponent`（标记哪个实体是听者，跟随玩家）
  - `AudioModule` 读取场景中的 AudioListener + AudioSource 计算空间衰减
  - `EditorLayer.cpp` → 音频源组件编辑器
  - 支持 WAV/MP3/OGG 格式
- **文件**：
  - `Components.h` → `AudioSource2DComponent`、`AudioListenerComponent`
  - `AudioModule.cpp` → ECS 集成
  - `EditorLayer.cpp` → 音频组件编辑器
- **依赖**：P5-1
- **状态**：待开发

#### P5-8: Camera2D 跟随系统
- **现状**：正交相机固定或手动控制
- **目标**：相机平滑跟随玩家实体，带边界限制
- **方案**：
  - 新增 `CameraFollowComponent`（目标实体、偏移量、边界盒）
  - 相机系统读取 CameraFollowComponent，每帧平滑插值到目标位置
  - 支持边界限制（不让玩家看到地图外）
  - 支持平滑系数配置（Lag/Damping）
- **文件**：
  - `Components.h` → `CameraFollowComponent`（Target/Offset/Smoothing/BoundsMin/BoundsMax/Enabled）
  - `Scene.cpp` → 相机跟随系统在 `OnUpdateRuntime` 和 `OnStepFrame` 中执行
  - `EditorLayer.cpp` → 相机跟随菜单项和属性面板
  - `LuaScriptBridge.cpp` → Lua API: `Camera_SetFollowTarget` / `Camera_ClearFollow`
  - `Camera.h` → 新增虚方法 `SetPosition` 支持多态
- **依赖**：无
- **状态**：**已完成** ✅

#### P5-9: 音效与音乐统一接口
- **现状**：`AudioModule` 有 MusicController 和 SfxEmitter，接口分散
- **目标**：统一音频接口，支持通过 Lua 脚本触发
- **方案**：
  - 统一 `AudioSource`（可播放音效也可播放音乐，通过 `SetBus` 路由到不同母线）
  - 扩展 Lua API：`Audio_Play`、`Audio_Stop`、`Audio_SetVolume`、`Audio_Crossfade`
  - `MusicController` 接入母线体系
  - 编辑器音乐/音效资源引用
- **文件**：
  - `Components.h` → `AudioSourceComponent`（AudioPath/Volume/Pitch/Loop/PlayOnAwake/Is3D/MinDistance/MaxDistance）
  - `Components.h` → `AudioListenerComponent`（Enabled/Volume）
  - `SceneSerializer.cpp` → 音频组件序列化/反序列化
  - `EditorLayer.cpp` → 音频组件菜单项和属性面板
  - `LuaScriptBridge.cpp` → 音频 Lua API: `Audio_Play` / `Audio_StopAll` / `Audio_SetVolume` / `Audio_PlayMusic` / `Audio_StopMusic`
- **依赖**：P5-7
- **状态**：**已完成** ✅

---

### 十五、4 内容生产工具

#### P5-10: Sprite Sheet 动画编辑器
- **现状**：`AnimationModule.h` 定义了数据结构和加载器，但没有编辑器
- **目标**：在编辑器内创建/编辑动画片段，指定精灵表区域
- **方案**：
  - 新增 `AnimationEditorPanel.h/.cpp`
  - 导入 SpriteSheet，指定行列，自动计算 UV
  - 配置帧时长、循环模式、事件帧
  - 导出为 `AnimationClip` JSON 资源
  - 支持预览播放
- **文件**：
  - `AnimationEditorPanel.h/.cpp` → 新增
  - `AnimationLoader.cpp` → JSON 导入支持
  - `EditorLayer.cpp` → 集成动画编辑器（菜单：窗口 → 动画编辑器）
- **依赖**：P5-1
- **状态**：**已完成** ✅

#### P5-11: 资源 Asset Browser 重构
- **现状**：Content Browser 仅是文件系统浏览
- **目标**：真正的资源浏览器：显示资源类型图标、预览、支持拖放到场景
- **方案**：
  - 重构 Content Browser 为 `AssetBrowserPanel`
  - 支持资源类型图标（纹理/音频/场景/材质/动画/预制体）
  - 纹理资源支持缩略图预览
  - 拖放资源到视口自动创建对应实体
  - 支持搜索过滤和收藏夹
- **文件**：
  - `AssetBrowserPanel.h/.cpp` → 新增
  - `EditorLayer.cpp` → 集成新面板
- **依赖**：无
- **状态**：**已完成** ✅
  - `AssetBrowserPanel` 实现网格视图和列表视图切换
  - 自动识别纹理/音频/场景/材质/模型/着色器/动画文件类型
  - 纹理资源支持缩略图缓存
  - 面包屑导航、搜索过滤、缩略图大小调节
  - 拖放目标支持外部文件夹

#### P5-12: TileMap 图层编辑增强
- **现状**：`TileMapEditorPanel` 支持基础笔刷，有 Ground/Objects/Water/Weather 分层
- **目标**：补齐擦除工具、填充工具、图块复制/粘贴、碰撞层编辑
- **方案**：
  - 新增 Eraser Tool（按住 Ctrl 切换）
  - 新增 Fill Tool（填充封闭区域）
  - 新增 Selection Tool（选中区域复制/粘贴）
  - 碰撞层独立编辑（碰撞/单向/平台/透明）
  - TileDef 属性面板（点击图块编辑属性）
  - Undo/Redo 支持 TileMap 操作
- **文件**：
  - `TileMapEditorPanel.h` → 新增 `ETileMapTool` 枚举（Brush/Eraser/Fill/Picker）
  - `TileMapEditorPanel.cpp` → `RenderToolBar` / `FloodFill` / `HandleBrushStroke` 工具分发
  - `TileMap.h/.cpp` → 已有 `GetTileLayer` / `SetTileLayer` 接口支持填充操作
- **依赖**：无
- **状态**：**已完成** ✅

---

### 十五、5 完整开发顺序

```
第一阶段：编辑器基础（无依赖）
├─ P5-3: 控制台面板（Console）
├─ P5-4: 编辑器 Profiler
└─ P5-11: 资源 Asset Browser 重构

第二阶段：ECS 统一（核心重构）
├─ P5-1: UI 实体纳入 ECS 层级
└─ P5-2: Prefab 系统

第三阶段：物理与交互
├─ P5-5: Physics2D 完善（碰撞体/关节/射线）
├─ P5-6: 粒子系统
└─ P5-8: Camera2D 跟随

第四阶段：内容工具链
├─ P5-10: Sprite Sheet 动画编辑器
└─ P5-12: TileMap 编辑增强

第五阶段：音频与 Lua API
├─ P5-7: 音频与 ECS 集成
└─ P5-9: 音效音乐统一接口 + Lua API 扩展
```

---

### 十五、6 最小 2D 游戏验证清单

完成第一至第三阶段后，应能验证以下能力：

| # | 验证项 | 对应模块 |
|---|--------|----------|
| 1 | 在编辑器内创建带碰撞体的角色，在场景中行走 | P5-5 |
| 2 | 添加粒子组件，编辑器内实时预览火焰效果 | P5-6 |
| 3 | 在 Asset Browser 拖放精灵到视口创建实体 | P5-11 |
| 4 | 创建 Prefab，实例化多个变体，验证属性覆盖 | P5-2 |
| 5 | 角色死亡时播放音效（空间衰减） | P5-7 |
| 6 | 相机平滑跟随角色，撞边界时停止 | P5-8 |
| 7 | 打开 Console 查看脚本日志输出 | P5-3 |
| 8 | 编辑器右下角显示帧时间和 Draw Call | P5-4 |
| 9 | UI 按钮与场景实体同时出现在 Hierarchy 中 | P5-1 |

---

### 十五、7 技术债务清理

| 债务项 | 现状 | 清理方案 |
|--------|------|----------|
| UI 实体与场景实体分离 | 两套系统独立 | P5-1 纳入 ECS |
| 无 Prefab | 实体无法复用 | P5-2 Prefab 系统 |
| 物理无关节 | 无法实现绳索/弹簧 | P5-5 Physics2D 完善 |
| 无粒子 | 无法做特效 | P5-6 粒子系统 |
| Lua 音频 API 缺失 | 无法脚本控制 | P5-9 音频统一接口 |
| TileMap 无 Undo | 操作无法撤销 | P5-12 TileMap EditorCommand |
| Console 不存在 | 无编辑器日志 | P5-3 控制台面板 |

---

## 十六、Unity 工作流程对比与能力补全（2026-05-29）

### 16.1 能力差距分析

对比 Unity Editor 的游戏开发工作流程，QuentlamEngine 在以下方面存在系统性缺失：

| 能力领域 | Unity | QuentlamEngine | 优先级 |
|---------|-------|----------------|--------|
| 动画系统 | Animator/AnimationClip/Controller + Animation Editor | `AnimationModule.h` 仅数据结构 | **P0** |
| 交互系统 | Raycast + OnCollision/Trigger + EventSystem | `InteractionModule.h` 仅占位 | **P0** |
| 粒子特效 | Shuriken Particle System | 无 | **P0** |
| Game/Split View | Play Mode 内嵌模拟 + Game View | Play 时切换全屏 ImGui | **P0** |
| 相机跟随 | 拖拽 Follow Target 自动计算 | CameraComponent 固定位置 | **P1** |
| 导航系统 | NavMesh + NavMeshAgent | 无 | **P1** |
| 场景流加载 | SceneManager.LoadScene/Additive | 无 | **P1** |
| 光照基础 | Directional/Point/Spot + Baked Lightmap | 无 | **P2** |
| Sprite Atlas | Sprite Atlas 自动打包 | 无 | **P2** |
| 帧步进调试 | Pause + Step Frame | 无 | **P2** |
| 脚本调试器 | VS 断点/Monodevelop | Lua 无调试器 | **P2** |
| 光照烘焙 | Lightmapper | 无 | **P3** |
| 地形系统 | Terrain | 无 | **P3** |
| Visual Scripting | Bolt/Code Agents | 无 | **P3** |

### 16.2 新增待开发任务

#### P6-1: 动画系统（核心）
- **现状**：`AnimationModule.h` 定义了 `AnimationClip`、`Animator`、`SpriteAtlasBinding`、`AnimationStateMachine` 数据结构，但无运行时实现、无编辑器
- **目标**：角色、工具、作物、动物、门、特效等统一动画播放
- **方案**：
  - 运行时：`AnimatorComponent`（ECS 组件）、`AnimationManager`（全局管理器）
  - `SpriteAnimationComponent` 已有基础，需增强为完整状态机
  - 编辑器：`AnimationEditorPanel` 集成到编辑器
  - 数据驱动：动画片段存储为 JSON 资源（帧率、帧列表、循环方式）
- **文件**：
  - `Components.h` → `SpriteAnimationComponent`（8 方向状态、动画数据路径）
  - `Gameplay/AnimationModule.h/.cpp` → JSON 序列化/反序列化
  - `Gameplay/AnimationLoader.cpp` → 增强支持 atlas 对象格式 + ResourceManager
  - `Editor/AnimationEditorPanel.h/.cpp` → 独立动画编辑器面板
  - `LuaScriptBridge.cpp` → `Anim_Play/Stop/Pause/Resume/IsPlaying/GetCurrentFrame/GetCurrentClip/SetDirection/GetDirection` + `AnimWrapMode`/`AnimDir` 枚举
  - `EditorLayer.cpp` → 属性面板集成动画加载器 + 8 方向按钮
- **依赖**：P5-11（Asset Browser）
- **状态**：**已完成** ✅
  - `SpriteAnimationComponent` 已注册到 ECS (`Components.h`)
  - `AnimationClip` 新增 `SerializeToJson()` / `DeserializeFromJson()` 实现
  - `AnimationLoader::LoadAnimationData()` 增强支持 atlas 对象格式，使用 ResourceManager
  - `AnimationEditorPanel` 独立面板：JSON 加载、Clip 列表、预览播放、FPS/WrapMode 编辑、帧列表增删
  - 属性面板新增动画加载输入框、8 方向选择按钮、播放控制
  - Lua API 完整绑定：Anim_* 系列函数 + AnimWrapMode/AnimDir 枚举

#### P6-2: 交互系统
- **现状**：`InteractionModule.h` 是空壳，无实现
- **目标**：玩家与地块/物体的交互、上下文提示、触发器
- **方案**：
  - `InteractableComponent`：挂在可交互实体上，定义交互半径/优先级/提示文本
  - `InteractionModule`：单例，每帧更新，最近可交互体检测
  - `InteractionPrompt`：交互提示 UI（E 键提示）
  - Lua API：`Interact_Try`、`Interact_HasNearby`、`Interact_GetNearbyId/Name`、`Interact_IsEnabled/SetEnabled`
- **文件**：
  - `Components.h` → `InteractableComponent`（InteractionId/DisplayName/PromptText/Radius/Cooldown/Type/Priority/Enabled）
  - `Gameplay/InteractionModule.h` → 单例实现（OnUpdate/TryInteract/Hovered 检测）
  - `Scene.cpp` → 每帧调用 `InteractionModule::OnUpdate`
  - `EditorLayer.cpp` → 交互组件编辑器（菜单 + 属性面板）
  - `LuaScriptBridge.cpp` → Interact_* 系列 API + InteractType/InteractPriority 枚举
- **依赖**：P6-1（动画系统）
- **状态**：**已完成** ✅
  - `InteractableComponent` 注册到 ECS，含交互类型/优先级/半径/冷却/提示文本等完整字段
  - `InteractionModule` 单例每帧扫描所有含 `InteractableComponent` 的实体，计算最近可交互体
  - 编辑器支持：组件菜单添加、属性面板编辑（ID/名称/Prompt/Radius/Cooldown/Type/Priority/Enabled）
  - Lua API：`Interact_Try`、`Interact_HasNearby`、`Interact_GetNearbyId`、`Interact_GetNearbyName`、`Interact_IsEnabled`、`Interact_SetEnabled` + 枚举

#### P6-3: Game/Split View 分离视图
- **现状**：Play Mode 切换到全屏 ImGui 游戏视图，无法同时看到编辑器和游戏
- **目标**：编辑器左侧 Game 视图，右侧 Scene 视图，双视图同时运行
- **方案**：
  - 重构 FrameBuffer 使用：一个用于 Scene View 渲染，一个用于 Game View 渲染
  - 编辑器布局支持 Split View 模式（拖拽分割条调整比例）
  - Game View 使用正交相机（2D）或透视相机（3D）独立控制
  - Play/Pause/Stop 控制在 Game View 工具栏
  - 独立 FPS 计数器
- **文件**：
  - `EditorLayer.h` → 新增 `m_GameFramebuffer`、`m_GameViewportSize`、`m_ViewportLayout`（枚举）、`m_SplitRatio`、`m_SplitHovered`、`m_DraggingSplit` 成员
  - `EditorLayer.cpp` → 视口工具栏（Scene/Game/SplitH/SplitV 按钮 + Play/Pause/Prev 按钮）、Game FrameBuffer 渲染（Play 模式时渲染到独立 FrameBuffer）、双视口布局（水平/垂直分割 + 单视口模式）、可拖拽分割条
- **依赖**：无
- **状态**：**已完成** ✅
  - `m_GameFramebuffer` 独立 FrameBuffer，Play 模式时渲染到 Game View
  - 视口工具栏：Scene / Game / Split H / Split V 四种布局切换 + Play/Pause/Prev 按钮
  - Split 模式下两个视口（Scene/Game）并排显示，可拖拽分割条调整比例
  - 单视口模式下 Scene Only（仅编辑器）/ Game Only（仅游戏）可选
  - Play 模式时 Game View 显示实际游戏渲染，Edit 模式时显示场景预览

#### P6-4: 粒子系统
- **状态**：**已完成** ✅
- **完成时间**：2026-05-29
- **实现内容**：
  - `ParticleSystem2DComponent`（ECS 组件）定义于 `Components.h`，包含发射率、最大粒子数、粒子生命周期、速度范围、尺寸范围、颜色渐变、旋转范围、角速度范围、重力修改器、播放控制等完整参数
  - `ParticleEmitter2D` 类（`ParticleSystem2D.h`）：基于 `ParticleSystemConfig` 配置，支持播放/停止/暂停/恢复、实时粒子发射与生命周期管理、随机速度/尺寸/旋转/角速度生成、循环与非循环模式、延迟启动
  - `Particle2D` 数据结构：存储位置、速度、尺寸、颜色、旋转、角速度、生命值等
  - `Renderer2D::DrawParticle()` 方法：支持带旋转的粒子四边形绘制，颜色基于生命周期进行插值
  - `Renderer2D::GetWhiteTexture()` 公共方法：供粒子系统无纹理时使用白色纹理
  - `Scene::OnUpdateRuntime()` 中集成粒子系统更新循环，自动管理 `ParticleEmitter2D` 生命周期
  - `Scene::OnUpdate()` 中集成粒子渲染，支持颜色渐变与生命周期插值
  - 编辑器内 `EditorLayer.cpp` 添加"视觉效果 / 粒子系统"组件菜单项与完整属性面板，支持 Playing/Looping/MaxParticles/EmissionRate/Lifetime/Speed/VelocityRange/SizeRange/Color/Rotation/AngularVelocity/Gravity/Duration/StartDelay 等实时调节
  - `LuaScriptBridge::SetupParticleAPI()` 添加 Lua API：`Particle_Play`、`Particle_Stop`、`Particle_SetEmissionRate`、`Particle_SetLifetime`、`Particle_SetSpeed`、`Particle_SetColor`、`Particle_SetMaxCount`
- **文件变更**：
  - `QuentlamEngine\src\Quentlam\Renderer\ParticleSystem2D.h`（新建）
  - `QuentlamEngine\src\Quentlam\Scene\Components.h`（修改）
  - `QuentlamEngine\src\Quentlam\Renderer\Renderer2D.h`（修改）
  - `QuentlamEngine\src\Quentlam\Renderer\Renderer2D.cpp`（修改）
  - `QuentlamEngine\src\Quentlam\Scene\Scene.cpp`（修改）
  - `QL-Editor\src\EditorLayer.cpp`（修改）
  - `QuentlamEngine\src\Quentlam\Modding\LuaScriptBridge.h`（修改）
  - `QuentlamEngine\src\Quentlam\Modding\LuaScriptBridge.cpp`（修改）

#### P6-5: 导航系统
- **状态**：**已完成** ✅
- **完成时间**：2026-05-29
- **实现内容**：
  - `NavAgentComponent`（ECS 组件）定义于 `Components.h`，包含目标网格坐标、路径列表、路径索引、移动进度、速度、到达阈值、状态（Idle/Moving/Paused）、对角线移动开关等完整参数
  - `NavigationMesh` 类（`NavigationModule.h`）：存储可行走网格瓦片信息，支持动态设置瓦片可行走性和移动成本，支持从 `TileMap` 自动生成导航网格
  - `NavigationModule` 单例（`NavigationModule.h/.cpp`）：A* 寻路算法实现（带对角线支持和自定义代价函数/启发函数）、`FindPath()` 查询接口、`MoveTo()` 实体导航控制、`Stop()` 停止导航、`HasPath()`/`IsMoving()` 状态查询、`Update()` 每帧更新导航代理位置
  - `Scene::OnUpdateRuntime()` 中集成 `NavigationModule::Get().Update()` 调用
  - 编辑器内 `EditorLayer.cpp` 添加"AI / 导航代理"组件菜单项与属性面板
  - `LuaScriptBridge::SetupNavigationAPI()` 添加 Lua API：`Nav_MoveTo`、`Nav_Stop`、`Nav_HasPath`、`Nav_IsMoving`、`Nav_FindPath`
- **文件变更**：
  - `QuentlamEngine\src\Quentlam\Gameplay\NavigationModule.h`（新建）
  - `QuentlamEngine\src\Quentlam\Gameplay\NavigationModule.cpp`（新建）
  - `QuentlamEngine\src\Quentlam\Scene\Components.h`（修改）
  - `QuentlamEngine\src\Quentlam\Scene\Scene.cpp`（修改）
  - `QuentlamEngine\QuentlamEngine.vcxproj`（修改）
  - `QL-Editor\src\EditorLayer.cpp`（修改）
  - `QuentlamEngine\src\Quentlam\Modding\LuaScriptBridge.h`（修改）
  - `QuentlamEngine\src\Quentlam\Modding\LuaScriptBridge.cpp`（修改）

#### P6-6: 场景流加载
- **状态**：**已完成** ✅
- **完成时间**：2026-05-29
- **实现内容**：
  - `SceneReferenceComponent`（ECS 组件）定义于 `Components.h`，支持 Additive/Single 加载模式、自动加载/激活开关、加载进度/状态/错误消息跟踪
  - 扩展 `SceneManager`（`SceneManager.h/.cpp`）：`LoadScene(filepath, mode)` 支持 Single/Additive、`CreateScene()`/`DestroyScene()` 多场景管理、`SetActiveSceneByName()` 按名称切换活动场景、`InstantiateScene()` 场景实例化（复制源场景实体）、`LoadSceneAsync()` 异步加载请求队列、`CancelLoad()` 加载取消、`IsLoading()`/`GetLoadingProgress()` 加载状态查询、`UnloadScene()`/`UnloadAllScenes()` 卸载、`GetEntityScene()` 查询实体所属场景、`GetLoadedSceneNames()` 获取已加载场景列表
  - `Scene::m_Name` 公开并添加 `GetName()` 方法用于场景名称访问
  - 编辑器内 `EditorLayer.cpp` 添加"场景 / 场景引用"组件菜单项与属性面板，显示路径/加载模式/自动加载/状态/进度/错误信息
  - `LuaScriptBridge::SetupSceneAPI()` 添加 Lua API：`Scene_Load`、`Scene_LoadAdditive`、`Scene_Save`、`Scene_GetActive`、`Scene_SetActive`、`Scene_Create`、`Scene_Unload`、`Scene_IsLoading`、`Scene_GetLoadingProgress`、`Scene_GetLoadedNames`
- **文件变更**：
  - `QuentlamEngine\src\Quentlam\Scene\SceneManager.h`（重写）
  - `QuentlamEngine\src\Quentlam\Scene\SceneManager.cpp`（重写）
  - `QuentlamEngine\src\Quentlam\Scene\Scene.h`（修改）
  - `QuentlamEngine\src\Quentlam\Scene\Scene.cpp`（修改）
  - `QuentlamEngine\src\Quentlam\Scene\Components.h`（修改）
  - `QL-Editor\src\EditorLayer.cpp`（修改）
  - `QuentlamEngine\src\Quentlam\Modding\LuaScriptBridge.h`（修改）
  - `QuentlamEngine\src\Quentlam\Modding\LuaScriptBridge.cpp`（修改）

#### P6-7: 帧步进调试
- **现状**：Play Mode 连续运行，无法暂停单步调试
- **目标**：编辑器内暂停游戏、逐帧前进
- **方案**：
  - 在 Play/Pause/Stop 基础上增加 Step（单步）按钮
  - `SceneState::Step` 枚举值
  - 每按一次 Step 执行一帧 Update（物理 + 脚本 + 导航 + 动画 + 粒子）
  - 当前选中实体的组件状态面板实时显示（Play 模式下解锁只读）
- **文件**：
  - `QL-Editor/src/EditorLayer.h` → `SceneState::Step` 枚举值，`m_ShowAtlasBuilder` 等成员
  - `QL-Editor/src/EditorLayer.cpp` → Step 按钮、`SceneState::Step` 分支处理
  - `QuentlamEngine/src/Quentlam/Scene/Scene.h` → `OnStepFrame()` 声明
  - `QuentlamEngine/src/Quentlam/Scene/Scene.cpp` → `OnStepFrame()` 实现
- **依赖**：P6-3（Game View）
- **状态**：**已完成** ✅

#### P6-8: Sprite Atlas 打包
- **现状**：无
- **目标**：自动将多个小图打包为图集，减少 Draw Call
- **方案**：
  - `SpriteAtlasPacker` 类：基于 Guillotine Bin Packing 算法排布纹理
  - `AtlasEntry` 结构：存储子图名、尺寸、UV 坐标
  - `AtlasPackerConfig`：图集尺寸、间距配置
  - 编辑器内 Atlas Builder 面板：设置图集尺寸/间距，添加图片，Pack 生成，导出 JSON 元数据
  - Lua API：`Atlas_GetSpriteUV`、`Atlas_LoadMetadata`
- **文件**：
  - `QuentlamEngine/src/Quentlam/Renderer/SpriteAtlasPacker.h`（完整实现）
  - `QuentlamEngine/src/Quentlam/Renderer/SpriteAtlasPacker.cpp`（Pack + GenerateAtlas + SaveMetadata）
  - `QuentlamEngine/src/Quentlam/Modding/LuaScriptBridge.h/.cpp`（SetupAtlasAPI）
  - `QL-Editor/src/AtlasBuilderPanel.h/.cpp`
  - `QL-Editor/src/EditorLayer.h/.cpp`（菜单项 + 面板渲染）
  - `QL-Editor/QL-Editor.vcxproj`（新增源文件 + 包含目录）
- **依赖**：P5-11（Asset Browser）
- **状态**：**已完成** ✅

### 16.3 完整开发顺序

```
P0 核心缺失（无依赖，先做）
├─ P6-1: 动画系统（运行时 + 编辑器） ✅
├─ P6-2: 交互系统 ✅
├─ P6-3: Game/Split View 分离视图 ✅
├─ P6-4: 粒子系统 ✅
└─ P6-5: 导航系统 ✅

P1 功能完善（依赖 P0）
├─ P6-6: 场景流加载 ✅
├─ P6-7: 帧步进调试 ✅
└─ P6-8: Sprite Atlas 打包 ✅

延续原有 P5 系列
├─ P5-1: UI 实体纳入 ECS 层级
├─ P5-2: Prefab 系统
├─ P5-5: Physics2D 完善
├─ P5-8: Camera2D 跟随
└─ P5-12: TileMap 编辑增强
```

### 十六、4 最小可玩版本前置能力清单

完成 P6-1 + P6-2 + P6-3 后，可实现以下核心体验：

| # | 验证项 | 对应任务 |
|---|--------|----------|
| 1 | 角色在地图行走时有方向动画 | P6-1 动画系统 |
| 2 | 按 E 键拾取物品，播放动画+音效 | P6-2 交互系统 |
| 3 | 编辑器内左侧 Game 右侧 Scene，双视图同步运行 | P6-3 Game/Split View |
| 4 | 粒子特效（火焰/爆炸）实时预览 | P6-4 粒子系统 |
| 5 | NPC 自主寻路到指定地点 | P6-5 导航系统 |
| 6 | 场景切换过渡加载 | P6-6 场景流加载 |
| 7 | Play 模式暂停后逐帧前进 | P6-7 帧步进调试 |
| 8 | 精灵图集减少 Draw Call | P6-8 Sprite Atlas |

---

## 十七、Unity 工作流对齐：完整能力规划（2026-05-29）

### 17.1 背景

经过六轮开发（截至 2026-05-29），QuentlamEngine 已完成 P5 系列全部 12 个任务和 P6 系列 8 个任务。但与 Unity Editor 的完整游戏开发工作流相比，仍存在系统性差距。本节将这些差距整合为有序的开发计划，目标是一次性追平 Unity 核心工作流能力。

---

### 17.2 已完成能力映射（对齐 Unity）

| Unity 能力 | QuentlamEngine 状态 | 对应任务 |
|-----------|---------------------|---------|
| GameObject + Component | ✅ ECS 实体/组件 | P5-1 |
| Scene 序列化 | ✅ JSON 序列化 | P4-7 |
| SpriteRenderer | ✅ SpriteRendererComponent | P4-10 |
| 3D Mesh/Light/Material | ✅ Renderer3D | — |
| Rigidbody + Collider | ✅ Rigidbody2D/3D + BoxCollider | P5-5 |
| Navigation Mesh + Agent | ✅ NavigationModule + NavAgentComponent | P6-5 |
| Particle System (2D) | ✅ ParticleSystem2D | P6-4 |
| SpriteSheet Animation | ✅ AnimationModule + Editor | P6-1 |
| Sprite Atlas Packer | ✅ SpriteAtlasPacker | P6-8 |
| AudioSource/Listener | ✅ AudioSourceComponent + AudioListenerComponent | P5-7/P5-9 |
| Camera + Follow | ✅ CameraComponent + CameraFollowComponent | P5-8 |
| Console Window | ✅ EditorConsolePanel | P5-3 |
| Profiler Overlay | ✅ EditorProfilerOverlay | P5-4 |
| Asset Browser | ✅ AssetBrowserPanel | P5-11 |
| TileMap Editor | ✅ TileMapEditorPanel + Brush/Eraser/Fill | P5-12 |
| Frame-step Debug | ✅ SceneState::Step | P6-7 |
| Game/Split View | ✅ Scene/Game Split Layout | P6-3 |
| Scene Streaming | ✅ SceneManager + SceneReferenceComponent | P6-6 |
| Interaction System | ✅ InteractableComponent + InteractionModule | P6-2 |
| Lua Scripting | ✅ LuaScriptBridge + ModLoader | P3/P4 |
| Prefab Reference | ✅ PrefabReferenceComponent | P5-2 |

---

### 17.3 剩余差距分类与开发顺序

按"阻塞核心玩法"→"提升制作效率"→"后期增强"三层优先级排序。

#### 第一阶段：P0 — 核心玩法阻塞（立即开发）

这些能力缺失会导致游戏核心玩法无法实现。

##### P0-A: Prefab 实例化系统

**Unity 对应**: Prefab Override / Instantiate / Variants

**现状**: `PrefabReferenceComponent` 仅持有路径，无实际实例化逻辑

**方案**:
- `PrefabSystem` 单例：加载 `.qult` JSON，预览数据，注册到 ResourceManager
- `Entity::InstantiatePrefab(prefabPath)`：从 Prefab 创建实体，复制所有组件数据
- `PrefabOverrideSystem`：跟踪哪些字段被本地覆盖（脏标记）
- `Entity::ApplyPrefabOverride()`：将覆盖值写回 Prefab 数据
- 支持嵌套 Prefab：实例化时递归处理子 Prefab
- 编辑器 Prefab 模式：双开编辑 Prefab，退出时保存

**文件**:
- `QuentlamEngine/src/Quentlam/Scene/PrefabSystem.h/.cpp` → 新建
- `QuentlamEngine/src/Quentlam/Scene/PrefabAsset.h` → 已有，增强
- `QuentlamEngine/src/Quentlam/Scene/Scene.h/.cpp` → InstantiatePrefab 钩子
- `QL-Editor/src/PrefabEditorPanel.h/.cpp` → 新建 Prefab 编辑面板
- `QL-Editor/src/EditorLayer.cpp` → Prefab 创建/实例化菜单

---

##### P0-B: UI 控件系统

**Unity 对应**: Canvas + RectTransform + Image/Text/Button/Toggle/Slider

**现状**: `UIEntityComponent` 仅有元数据，无实际渲染管线

**方案**:
- `CanvasComponent`：Screen Space / World Space 模式，Render Order
- `RectTransformComponent`：Anchors / Pivot / Offset / SizeDelta（参考 Unity UGUI）
- `ImageComponent`：Sprite 渲染 / Color / Fill Center / Raycast Target
- `TextComponent`：文字渲染（使用已有 ImGui 文字或 freetype），支持 Font
- `ButtonComponent`：SpriteRenderer + 事件回调（OnClick）
- `ToggleComponent`：选中状态 + Group 支持
- `SliderComponent`：Fill Amount + Value Range
- `LayoutGroupComponent`：Horizontal/Vertical/Grid 自动布局
- `EventSystem`：PointerEnter/Click/Drag/Select 事件
- UI 渲染管线：基于 Scene 渲染 pass，先渲染 UI 再叠加 ImGui

**文件**:
- `QuentlamEngine/src/Quentlam/UI/CanvasComponent.h` → 新建
- `QuentlamEngine/src/Quentlam/UI/RectTransformComponent.h` → 新建
- `QuentlamEngine/src/Quentlam/UI/ImageComponent.h` → 新建
- `QuentlamEngine/src/Quentlam/UI/TextComponent.h` → 新建
- `QuentlamEngine/src/Quentlam/UI/ButtonComponent.h` → 新建
- `QuentlamEngine/src/Quentlam/UI/ToggleComponent.h` → 新建
- `QuentlamEngine/src/Quentlam/UI/SliderComponent.h` → 新建
- `QuentlamEngine/src/Quentlam/UI/LayoutGroupComponent.h` → 新建
- `QuentlamEngine/src/Quentlam/UI/EventSystem.h/.cpp` → 新建
- `QuentlamEngine/src/Quentlam/UI/UIRenderer.h/.cpp` → 新建（UI 渲染到 FrameBuffer）
- `QuentlamEngine/src/Quentlam/Scene/Scene.cpp` → UI 系统更新循环
- `Components.h` → 注册以上所有组件
- `EditorLayer.cpp` → UI 组件菜单和 Canvas/Transform 编辑器
- `SceneSerializer.cpp` → UI 组件序列化

---

##### P0-C: 2D 射线检测

**Unity 对应**: Physics2D.Raycast / BoxCast / CircleCast

**现状**: 无射线检测接口

**方案**:
- `Physics2D::Raycast(origin, direction, distance, layerMask)` → 返回 Hit
- `Physics2D::BoxCast` → 矩形射线检测
- `Physics2D::CircleCast` → 圆形射线检测
- `Physics2D::OverlapPoint(point)` → 点碰检测
- 基于 Box2D b2ShapeCast / b2RayCast 实现
- Lua API：`Physics2D_Raycast`、`Physics2D_BoxCast`、`Physics2D_OverlapPoint`

**文件**:
- `QuentlamEngine/src/Quentlam/Physics/Physics2D.h/.cpp` → 射线检测方法
- `LuaScriptBridge.cpp` → Physics2D_* 系列 API

---

##### P0-D: 导航网格编辑器

**Unity 对应**: Navigation Window → Bake

**现状**: `NavigationModule` 仅运行时从 TileMap 生成，无烘焙编辑器

**方案**:
- `NavMeshEditorPanel`：可视化烘焙窗口
  - 设置 Agent Radius / Step Height / Max Slope
  - 点击 Bake 生成 NavMesh
  - 显示可行走区域热力图（绿色=可行走，红色=障碍）
  - 支持分区域烘焙（多 Layer）
- `NavigationMesh` 持久化到 `.navmesh` 文件
- `NavigationModule` 启动时加载烘焙数据而非每次生成
- 支持动态障碍物（Carve）：运行时标记区域为不可行走

**文件**:
- `QuentlamEngine/src/Quentlam/Gameplay/NavMeshEditorPanel.h/.cpp` → 新建
- `QuentlamEngine/src/Quentlam/Gameplay/NavigationModule.h/.cpp` → 烘焙接口
- `QuentlamEngine/src/Quentlam/Gameplay/NavigationMesh.h` → 新建（序列化格式）
- `QL-Editor/src/EditorLayer.cpp` → 菜单集成 NavMesh 编辑器
- `EditorLayer.cpp` → 在 Scene View 中绘制 NavMesh Overlay

---

#### 第二阶段：P1 — 内容制作效率

##### P1-A: 动画状态机

**Unity 对应**: Animator Controller / State Machine / Blend Tree / Animation Events

**现状**: 仅有 `SpriteAnimationComponent` 播放单段动画，无状态机

**方案**:
- `AnimatorController`：状态机容器，定义 Parameters（Float/Int/Bool/Trigger）
- `AnimationState`：单个状态，持有一个 AnimationClip
- `AnimationTransition`：状态切换条件（Exit Time + Conditions）
- `AnimationBlendTree`：混合树（1D / 2D Simple / 2D Freeform / Direct）
- `AnimationEvent`：在特定帧触发 Lua 回调
- `RootMotion`：角色位移由动画驱动（可选）
- `AnimationLayer`：Avatar Mask 支持叠加层（Base Layer + Upper Body Layer）
- 编辑器 Animator 窗口：状态机可视化编辑器（节点连线图），拖拽 Clip 到节点，支持预览播放

**文件**:
- `QuentlamEngine/src/Quentlam/Gameplay/AnimatorController.h/.cpp` → 新建
- `QuentlamEngine/src/Quentlam/Gameplay/AnimationState.h` → 新建
- `QuentlamEngine/src/Quentlam/Gameplay/AnimationTransition.h` → 新建
- `QuentlamEngine/src/Quentlam/Gameplay/AnimationBlendTree.h` → 新建
- `QuentlamEngine/src/Quentlam/Gameplay/AnimationLayer.h` → 新建
- `Components.h` → `AnimatorControllerComponent`（持有 Controller 引用）
- `Scene.cpp` → Animator 系统更新循环
- `QL-Editor/src/AnimatorEditorPanel.h/.cpp` → 新建（状态机可视化编辑器）
- `LuaScriptBridge.cpp` → `Anim_SetTrigger`、`Anim_GetFloat` 等 Parameter API

---

##### P1-B: 资源 GUID 引用系统

**Unity 对应**: .meta 文件 + GUID + Object PID

**现状**: 资源引用使用硬编码路径，删除/移动资源后引用断裂

**方案**:
- 资源导入时自动生成 `.meta` 文件（JSON 格式），存储 `guid`（32 字节随机十六进制）+ `pid`（64 位 PID）
- `ResourceManager` 按 GUID 索引资源而非路径
- 场景文件中的组件引用改为 `guid` 字段
- `AssetDatabase`：编辑器层资源管理器，按 GUID 解析依赖图
- `Prefab` / `AnimationClip` / `Material` / `Scene` 全部通过 GUID 引用
- 编辑器内重命名/移动资源时自动更新所有 GUID 引用

**文件**:
- `QuentlamEngine/src/Quentlam/Content/AssetDatabase.h/.cpp` → 新建（核心）
- `QuentlamEngine/src/Quentlam/Content/AssetMetaFile.h/.cpp` → 新建
- `QuentlamEngine/src/Quentlam/Core/ResourceManager.h` → 改为按 GUID 加载
- `QuentlamEngine/src/Quentlam/Scene/SceneSerializer.cpp` → 改为 GUID 序列化
- `QL-Editor/src/EditorLayer.cpp` → 资源导入时触发 meta 生成

---

##### P1-C: 输入系统重构

**Unity 对应**: Input System Package (New) / InputAction / InputControl

**现状**: 仅有 GLFW 封装的旧版 Input（IsKeyDown / IsMouseButton）

**方案**:
- `InputAction`：绑定到按键/轴/鼠标，支持组合键
- `InputActionMap`：一组 Action（Gameplay / UI / Menu）
- `InputActionAsset`：持有多张 Map，编辑器导出 JSON
- `InputSystem` 单例：处理 InputContext 切换（Gameplay 时忽略 UI 事件）
- `PlayerInputComponent`：ECS 组件，持有 ActionAsset 引用，自动响应绑定事件
- Lua API：`Input_GetActionPhase`、`Input_GetActionValue`、`Input_SetContext`

**文件**:
- `QuentlamEngine/src/Quentlam/Input/InputAction.h` → 新建
- `QuentlamEngine/src/Quentlam/Input/InputActionMap.h` → 新建
- `QuentlamEngine/src/Quentlam/Input/InputSystem.h/.cpp` → 新建（重构 Input 模块）
- `Components.h` → `PlayerInputComponent`
- `QL-Editor/src/InputActionEditorPanel.h/.cpp` → 新建
- `LuaScriptBridge.cpp` → `Input_*` API

---

##### P1-D: 字体 + 国际化

**Unity 对应**: Font Asset / TextMeshPro / Localization Package

**现状**: 无文字渲染管线，ImGui 内置字体无法被游戏使用

**方案**:
- `FontAsset`：预渲染字体纹理图集（Freetype 加载 TTF，支持中文），按 Unicode 范围切割
- `TextComponent`：引用 FontAsset + String，按 UTF-8 渲染
- `LocalizationSystem`：StringTable 按语言键值对存储，支持 CSV 导入
- `LocalizedTextComponent`：持有文本 Key，运行期从 StringTable 取值
- `FontFallback`：字形缺失时按顺序回退到备用字体

**文件**:
- `QuentlamEngine/src/Quentlam/UI/FontAsset.h/.cpp` → 新建
- `QuentlamEngine/src/Quentlam/UI/TextRenderer.h/.cpp` → 新建
- `QuentlamEngine/src/Quentlam/UI/LocalizationSystem.h/.cpp` → 新建
- `Components.h` → `LocalizedTextComponent`
- `LuaScriptBridge.cpp` → `Text_SetText`、`Text_SetFont`、`Loc_SetLanguage`

---

#### 第三阶段：P2 — 渲染与视觉效果

##### P2-A: 后处理管线

**Unity 对应**: Post-processing Stack (Volume + Effect)

**方案**:
- `RenderPipeline`：Scene → PostProcessing → Output（Screen）
- `PostProcessVolume`：球形区域，应用一组 Effect
- `PostProcessEffect`：Bloom / Color Grading / Vignette / Chromatic Aberration / Motion Blur
- 基于 FrameBuffer Ping-Pong + Fragment Shader 实现
- 编辑器内 PostProcess Volume 编辑面板

**文件**:
- `QuentlamEngine/src/Quentlam/Renderer/PostProcessVolume.h` → 新建
- `QuentlamEngine/src/Quentlam/Renderer/PostProcessEffect.h` → 新建
- `QuentlamEngine/src/Quentlam/Renderer/PostProcessRenderer.h/.cpp` → 新建
- `QuentlamEngine/src/Quentlam/Renderer/Shaders/post_fx.glsl` → 新建
- `Components.h` → `PostProcessVolumeComponent`

---

##### P2-B: 2D 光照

**Unity 对应**: URP 2D Lights / Sprite Light

**方案**:
- `Light2D`：Point / Directional / SpriteLight
- `ShadowCaster2D`：Sprite 的 2D 阴影投射
- 基于光照贴图或运行时光照 Pass 实现

**文件**:
- `QuentlamEngine/src/Quentlam/Renderer/Light2D.h/.cpp` → 新建
- `Components.h` → `Light2DComponent`、`ShadowCaster2DComponent`
- `Renderer2D.cpp` → 光照 Pass

---

##### P2-C: LOD 系统

**Unity 对应**: LOD Group

**方案**:
- `LODGroupComponent`：定义每个 LOD Level 的 Mesh/Texture + Screen Height 阈值
- `LODUtility`：根据相机距离自动选择 LOD Level
- `Renderer2D` → LOD Sprite 切换

**文件**:
- `Components.h` → `LODGroupComponent`、`LODLevel`
- `QuentlamEngine/src/Quentlam/Renderer/LODSystem.h/.cpp` → 新建

---

#### 第四阶段：P3 — 地形与植被

##### P3-A: 地形系统

**Unity 对应**: Terrain（Heightmap + Detail）

**方案**:
- `TerrainComponent`：基于 TileMap 扩展，支持 Heightmap（浮点高度场）
- 地形刷工具：Raise/Lower/Smooth/Flatten
- 地形分块（Chunk-based），支持大地图流式加载

**文件**:
- `QuentlamEngine/src/Quentlam/World/TerrainComponent.h` → 新建
- `QuentlamEngine/src/Quentlam/World/TerrainMeshGenerator.h/.cpp` → 新建
- `QL-Editor/src/TerrainEditorPanel.h/.cpp` → 新建

---

##### P3-B: 植被实例化

**Unity 对应**: Terrain Trees / Detail Mesh

**方案**:
- `VegetationSystem`：Instanced 渲染大量树木/草丛
- 基于 `InstancedMesh` 优化 Draw Call
- 与 Terrain 分块对齐，远处自动剔除

**文件**:
- `QuentlamEngine/src/Quentlam/World/VegetationSystem.h/.cpp` → 新建
- `Renderer3D.cpp` → Instanced Rendering 扩展

---

#### 第五阶段：P4 — 物理关节与高级碰撞

##### P4-A: 2D 物理关节

**Unity 对应**: DistanceJoint / HingeJoint / SpringJoint / WheelJoint

**方案**:
- `DistanceJoint2DComponent`：固定两点距离
- `HingeJoint2DComponent`：旋转铰链
- `SpringJoint2DComponent`：弹性连接
- 基于 Box2D b2DistanceJoint / b2RevoluteJoint / b2SpringJoint 实现
- 编辑器内关节配置面板

**文件**:
- `Components.h` → `DistanceJoint2DComponent`、`HingeJoint2DComponent`、`SpringJoint2DComponent`
- `Physics2D.cpp` → 关节创建/销毁
- `EditorLayer.cpp` → 关节组件编辑器

---

##### P4-B: 触发器 + 物理材质编辑器

**Unity 对应**: Collider.isTrigger / PhysicsMaterial2D

**方案**:
- `Collider2DComponent.IsTrigger`：碰撞体变为触发器（发送事件不产生物理响应）
- `PhysicsMaterial2DAsset`：可重用的物理材质资源（Friction / Bounce）
- `CollisionEvent`：OnCollisionEnter/Stay/Exit + 物理数据（点/法线/深度）
- `TriggerEvent`：OnTriggerEnter/Stay/Exit
- `CollisionMatrix` 编辑器 UI（Edit → Project Settings → Physics2D）

**文件**:
- `Components.h` → `BoxCollider2DComponent.IsTrigger`、`CircleCollider2DComponent.IsTrigger`
- `QuentlamEngine/src/Quentlam/Physics/PhysicsMaterial2DAsset.h` → 新建
- `QuentlamEngine/src/Quentlam/Physics/CollisionEvent.h` → 新建
- `Physics2D.cpp` → 触发器 + 碰撞事件派发
- `LuaScriptBridge.cpp` → `Collision_*` / `Trigger_*` 事件回调

---

#### 第六阶段：P5 — 多人网络与构建

##### P5-A: 网络同步基础

**Unity 对应**: Netcode for GameObjects / Unity Transport

**方案**:
- `NetworkIdentity`：标记网络对象
- `NetworkTransform`：位置/旋转/缩放同步
- `NetworkObject`：Spawn/Despawn 同步
- `NetworkManager`：服务端/客户端模式，Tick 同步
- 基于 UDP（轻量）实现可靠的 RPC 机制
- Lua API：`Network_Spawn`、`Network_RPC`、`Network_SetAuthority`

**文件**:
- `QuentlamEngine/src/Quentlam/Network/NetworkIdentity.h` → 新建
- `QuentlamEngine/src/Quentlam/Network/NetworkTransform.h` → 新建
- `QuentlamEngine/src/Quentlam/Network/NetworkManager.h/.cpp` → 新建
- `Components.h` → `NetworkIdentityComponent`、`NetworkTransformComponent`
- `LuaScriptBridge.cpp` → `Network_*` API

---

##### P5-B: 构建自动化

**Unity 对应**: Build Pipeline / Player Settings / Platform Support

**方案**:
- `BuildPipeline`：命令行构建接口（`--build --platform win64 --output build.exe`）
- `PlayerSettings`：分辨率 / 图标 / Bundle ID / scripting backend
- MSBuild 参数化构建（扩展现有 MSBuild 配置）
- 资源打包（将所有依赖资源打包为二进制包）

**文件**:
- `QuentlamEngine/src/Quentlam/Core/BuildPipeline.h/.cpp` → 新建
- `QuentlamEngine/src/Quentlam/Core/PlayerSettings.h` → 新建
- 扩展 `CMakeLists.txt` 和 `.vcxproj` 构建参数

---

### 17.4 完整开发顺序（总览）

```
✅ 第一阶段 P0：核心玩法阻塞（已全部完成）
✅ 第二阶段 P1：内容制作效率（已全部完成）
✅ 第三阶段 P2：渲染与视觉效果（已全部完成）
✅ 第四阶段 P3：地形与植被（已全部完成）
✅ 第五阶段 P4：物理关节与高级碰撞（已全部完成）
✅ 第六阶段 P5：多人网络与构建（已全部完成）

===== 全部开发任务已完成 =====
```

---

### 17.5 依赖关系图

```
✅ P0-A (Prefab) ──────► ✅ P1-B (GUID系统) ──────► ✅ 所有其他模块
✅ P0-B (UI) ──────────────┤
✅ P0-C (射线检测) ◄────────┘
✅ P0-D (NavMesh)           │
✅ P1-A (动画状态机)        │
✅ P1-C (输入系统)          │
✅ P1-D (字体国际化)        │
✅ P2-A (后处理)            │
✅ P2-B (2D光照)            │
✅ P2-C (LOD)              │
✅ P3-A (地形系统)          │
✅ P3-B (植被系统)          │
✅ P4-A (2D关节)           │
✅ P4-B (触发器+材质)       │
✅ P5-A (网络同步)          │
✅ P5-B (构建自动化)        │

===== 全部阶段已完成 =====
```

---

### 17.6 与 Unity 的最终能力对照

| 能力领域 | Unity | QuentlamEngine | 目标阶段 |
|---------|-------|----------------|---------|
| **编辑器核心** |
| Hierarchy / Inspector | ✅ | ✅ | — |
| Scene View / Gizmos | ✅ | ✅ | — |
| Asset Browser + GUID | ✅ | ✅ | — |
| Console | ✅ | ✅ | — |
| Profiler | ✅ | ✅ | — |
| Frame Debugger | ✅ | 缺 | P5 后 |
| **场景 & 实体** |
| Prefab Instantiate | ✅ | 部分 | P0-A |
| Prefab Override | ✅ | 缺 | P0-A |
| Prefab Variants | ✅ | 缺 | P0-A |
| Scene Streaming | ✅ | ✅ | — |
| **物理** |
| Rigidbody 2D/3D | ✅ | ✅ | — |
| Colliders (Box/Circle/...) | ✅ | ✅ | — |
| Joints 2D | ✅ | ✅ | P4-A |
| Joints 3D | ✅ | 部分 | — |
| Raycast 2D | ✅ | ✅ | P0-C |
| Raycast 3D | ✅ | ✅ | — |
| Physics Material | ✅ | ✅ | P4-B |
| Trigger System | ✅ | ✅ | P4-B |
| **渲染** |
| 2D Sprite | ✅ | ✅ | — |
| 3D Mesh/Light | ✅ | ✅ | — |
| Sprite Atlas | ✅ | ✅ | — |
| Post-processing | ✅ | ✅ | P2-A |
| 2D Lights | ✅ | ✅ | P2-B |
| LOD | ✅ | ✅ | P2-C |
| Lightmapping | ✅ | 缺 | P3 后 |
| Occlusion Culling | ✅ | 缺 | P3 后 |
| **动画** |
| Sprite Animation | ✅ | ✅ | — |
| Animator Controller | ✅ | ✅ | P1-A |
| Blend Tree | ✅ | 缺 | P1-A |
| Animation Events | ✅ | 缺 | P1-A |
| Root Motion | ✅ | 缺 | P1-A |
| **导航** |
| NavMesh Bake | ✅ | ✅ | P0-D |
| NavMesh Agent | ✅ | ✅ | — |
| Obstacle Carving | ✅ | 缺 | P0-D |
| **UI** |
| Canvas / RectTransform | ✅ | ✅ | P0-B |
| Image / Text / Button | ✅ | ✅ | P0-B |
| Toggle / Slider / Layout | ✅ | ✅ | P0-B |
| Event System | ✅ | ✅ | P0-B |
| Font / Localization | ✅ | ✅ | P1-D |
| **输入** |
| Input System (New) | ✅ | ✅ | P1-C |
| Input Actions | ✅ | ✅ | P1-C |
| Input Mockup | ✅ | 缺 | P1-C |
| **地形** |
| Terrain | ✅ | ✅ | P3-A |
| Trees / Vegetation | ✅ | ✅ | P3-B |
| **网络** |
| Netcode / RPC | ✅ | ✅ | P5-A |
| **构建** |
| Build Pipeline | ✅ | ✅ | P5-B |
| Addressables | ✅ | 缺 | P5-B |
| Cross-platform | ✅ | Win only | P5-B |

---

## 十八、编辑器核心体验补全（第七轮，2026-05-31）

### 背景

相机系统修复后，编辑器核心体验仍有系统性差距。对标 Unity Editor，需要在场景交互、视图独立性、层级管理、可视化调试等方面全面对齐。

### 开发顺序

```
P0：无依赖，先做（基础交互 + 视图独立性）
├─ E7-1: 相机移动速度 + 鼠标右键捕获 ✅
├─ E7-2: 视口分辨率独立性（Game View 不随窗口缩放） ✅
└─ E7-3: 游戏摄像机在层级显示 + 绑定API ✅

P1：依赖 P0（视觉效果）
├─ E7-4: 默认天空盒和地面（作为场景默认实体） ✅
├─ E7-5: 默认平行光（作为场景默认实体） ✅
└─ E7-6: 摄像机 Gizmo（图标 + 视锥体渲染） ✅

P2：依赖 P0-P1（编辑器交互增强）
└─ E7-7: 场景层级分组功能（右键添加分组） ✅
```

### E7-1: 相机移动速度 + 鼠标右键捕获

**目标**：
- 3D 相机移动速度从 5.0 提升到合理值（WASD 按住加速）
- 按住鼠标右键时，鼠标光标应消失并聚焦场景视口，松开后恢复
- 相机控制仅在右键按住时激活（与 Unity 行为一致）

**现状**：
- 相机速度硬编码为 `5.0f * ts`，无加速机制
- 右键按住时仍可进行其他交互
- 相机控制在 `m_ViewportFocused` 下激活，与右键无关

**方案**：
- 右键按下：隐藏鼠标（`glfwSetInputMode(..., GLFW_CURSOR, GLFW_CURSOR_DISABLED)`），进入"相机控制模式"
- 右键松开：恢复鼠标（`GLFW_CURSOR_NORMAL`），退出相机控制模式
- 速度基准值提升到 10.0，按住 Shift 额外加速 2x
- Scene View 的 `OnImGuiRender` 中，在右键按住时不处理 Gizmo 拾取

**状态**：**已完成** ✅
- **完成时间**：2026-05-31
- **实现内容**：
  - `m_CamControlActive` bool 标志控制相机控制模式
  - 右键按下且视口聚焦时激活：隐藏鼠标（GLFW_CURSOR_DISABLED）
  - 右键松开时退出：恢复鼠标（GLFW_CURSOR_NORMAL）
  - `m_CamBaseSpeed = 10.0f`，Shift 按住时 2.5x 加速
  - 相机控制仅在 `m_CamControlActive && !m_IsCameraTransitioning` 时激活

**文件**：
- `QL-Editor/src/EditorLayer.h` → `m_CamControlActive`、`m_CamBaseSpeed`（float = 10.0f）
- `QL-Editor/src/EditorLayer.cpp` → 右键事件监听、速度计算、鼠标状态切换

---

### E7-2: 视口分辨率独立性

**目标**：Game View 和 Scene View 的渲染分辨率固定，不随 ImGui 窗口缩放而改变。

**现状**：
- `m_ViewportSize` 和 `m_GameViewportSize` 直接取自 ImGui 窗口大小
- 窗口缩小时渲染分辨率降低，画面变糊

**方案**：
- 引入固定分辨率配置（默认 1280×720）
- 每个 View（Scene / Game）独立配置固定分辨率
- FrameBuffer 按固定分辨率创建，ImGui::Image 按窗口比例拉伸显示
- 编辑器设置面板中添加"视口分辨率"下拉选项（720p / 1080p / 4K / 自定义）

**状态**：**已完成** ✅
- **完成时间**：2026-05-31
- **实现内容**：
  - `m_SceneRenderSize` / `m_GameRenderSize` 固定分辨率
  - `ViewportResolution` 枚举支持：Fixed1280x720 / Fixed1920x1080 / Fixed2560x1440 / WindowSize
  - `ApplyViewportResolutions()` 应用分辨率并 resize FrameBuffer
  - 设置菜单支持分辨率切换（720p / 1080p / 1440p / 跟随窗口）
  - Game View 和 Scene View 独立配置（复制模式）

**文件**：
- `EditorLayer.h` → `m_SceneViewportResolution`、`m_GameViewportResolution`（`glm::ivec2`）
- `EditorLayer.cpp` → `UI_Settings()`、`ApplyViewportResolutions()`
- `EditorLayer.cpp` → FrameBuffer 创建和 resize 改用固定分辨率

---

### E7-3: 游戏摄像机在层级显示 + 绑定 API

**目标**：
- "游戏摄像机"实体在场景层级中可见（可选择、编辑 Transform）
- 允许创建多个摄像机，指定任意一个为 Game View 渲染目标
- 提供 Lua API 切换 Game Camera

**现状**：
- 场景层级渲染时跳过 "场景摄像机" 和 "游戏摄像机"
- Game View 硬编码使用 "游戏摄像机" 实体
- 无切换 Game Camera 的 API

**方案**：
- 移除层级中对 "游戏摄像机" 的跳过（仅跳过 "场景摄像机"）
- CameraComponent 新增 `bool IsGameCamera` 字段（运行时生效，编辑器始终用 Scene Camera）
- Scene 新增 `SetActiveGameCamera(Entity)` 方法
- Lua API：`Camera_SetGameCamera(entityId)` / `Camera_GetGameCameraId()`
- 编辑器内摄像机属性面板增加 "设为游戏摄像机" 按钮

**状态**：**已完成** ✅
- **完成时间**：2026-05-31
- **实现内容**：
  - 层级不再跳过游戏摄像机实体
  - CameraComponent 新增 `IsGameCamera` 字段（默认 false）
  - Scene 新增 `SetActiveGameCamera(Entity)` / `GetActiveGameCamera()` 方法
  - 摄像机属性面板增加"设为游戏摄像机"复选框
  - Lua API：`Camera_SetGameCamera` / `Camera_GetGameCameraId`

**文件**：
- `Components.h` → `CameraComponent` 新增 `bool IsGameCamera = false`
- `Scene.h/.cpp` → `SetActiveGameCamera(Entity)`、`GetActiveGameCamera()`
- `EditorLayer.cpp` → 层级渲染移除游戏摄像机跳过、Game Camera 按钮
- `LuaScriptBridge.cpp` → `Camera_SetGameCamera` / `Camera_GetGameCameraId`

---

### E7-4: 默认天空盒和地面（场景默认实体）

**目标**：新场景自动创建天空盒实体和地面实体（Unity 行为）。

**现状**：
- `Scene::CreateDefaultScene` 已创建 "天空" 和 "地面" 实体
- 但天空盒渲染是 EditorLayer 层面的 `SkyRenderer`，不是场景实体
- 没有"天空可见性"开关

**方案**：
- "天空" 实体保留 `WeatherSystemComponent` 和 `DirectionalLightComponent`
- 新增 `SkyboxComponent`：持有 cubemap 引用、`bool Visible`（决定是否渲染天空）
- "地面" 实体已正确配置 `TerrainComponent`
- 编辑器菜单"设置"中添加"显示天空盒"复选框（控制 `SkyRenderer` 是否渲染）
- 移除天空盒时渲染纯黑背景

**状态**：**已完成** ✅
- **完成时间**：2026-05-31
- **实现内容**：
  - `SkyboxComponent` 新增到 `Components.h`（Visible / SkyMode 字段）
  - 新场景创建时自动给"天空"实体添加 `SkyboxComponent`
  - 设置菜单新增"显示天空盒"复选框（`m_ShowSkybox`），控制 Scene View 和 Game View 的天空渲染
  - 天空模式（程序化 / Cubemap）从场景中 `SkyboxComponent` 读取并同步
  - 属性面板支持 `SkyboxComponent`：可见性开关、程序化/Cubemap 模式切换
  - `SceneSerializer.cpp` 支持 `SkyboxComponent` 序列化/反序列化

**文件变更**：
- `QuentlamEngine\src\Quentlam\Scene\Components.h`（新增 SkyboxComponent）
- `QuentlamEngine\src\Quentlam\Scene\SceneSerializer.cpp`（序列化/反序列化）
- `QL-Editor\src\EditorLayer.h`（m_ShowSkybox 成员）
- `QL-Editor\src\EditorLayer.cpp`（菜单项 + 条件渲染 + 属性面板）

---

### E7-5: 默认平行光（场景默认实体）

**目标**：新场景自动创建平行光实体，从上方照射下来（与 Unity 行为一致）。

**现状**：
- "天空" 实体持有 `DirectionalLightComponent`
- 但 Renderer3D 的 `SetDirectionalLight` 是独立调用的
- DirectionalLightComponent 是否已添加到场景默认实体待确认

**方案**：
- 确认 `CreateDefaultScene` 中的天空实体已正确配置 `DirectionalLightComponent`
- 如果未配置，添加：旋转设置为 (60, -30, 0) 度，从斜上方照射
- DirectionalLightComponent 字段：`Color`(白色)、`Intensity`(1.0)、`Direction`
- Scene 在 `OnUpdateRuntime` 中同步 DirectionalLightComponent 到 Renderer3D

**状态**：**已完成** ✅
- **完成时间**：2026-05-31
- **实现内容**：
  - `CreateDefaultScene` 中明确配置 `DirectionalLightComponent`：Color=(1,1,1)、Intensity=1.0、Direction=(-0.5,-1.0,-0.3)
  - `Scene::OnUpdateRuntime` 中添加循环，同步场景中所有 `DirectionalLightComponent` 到 `Renderer3D::SetDirectionalLight`
  - 方向从实体的世界变换矩阵中计算（`glm::mat3(world) * dlc.Direction`）
  - 属性面板支持 `DirectionalLightComponent`：颜色、强弱、方向三轴编辑

**文件变更**：
- `QuentlamEngine\src\Quentlam\Scene\Scene.cpp`（OnUpdateRuntime 同步 + CreateDefaultScene 配置）
- `QL-Editor\src\EditorLayer.cpp`（属性面板）

---

### E7-6: 摄像机 Gizmo（图标 + 视锥体）

**目标**：摄像机实体在场景视口中显示图标和视锥体，表示其位置、朝向和视野范围。

**现状**：
- 相机选择时无特殊标识
- 摄像机视角方向不可见

**方案**：
- 摄像机图标：使用线框金字塔或摄像机形状模型（参考 Unity）
- 视锥体：使用 `Renderer3D::DrawFrustum` 绘制视锥体线框
  - 根据 CameraComponent 的 FOV、Near、Far 参数计算 8 个角点
  - 从相机位置向前延伸，显示实际视野范围
  - 颜色：`glm::vec4(1.0f, 0.5f, 0.0f, 1.0f)`（橙色，与选中实体一致）
- 仅在 Scene View 中绘制（Game View 不显示 Gizmo）
- 场景摄像机使用编辑器状态（`m_Cam3DPosition/Rotation`）计算视锥体

**状态**：**已完成** ✅
- **完成时间**：2026-05-31
- **实现内容**：
  - **3D 模式**：所有含 `CameraComponent` 的实体显示视锥体，颜色根据角色编码（青色=场景摄像机，橙色=游戏摄像机，灰色=其他，黄色=选中）
  - **2D 模式**：所有摄像机实体显示十字准心（+形状）
  - **颜色编码**：`m_SceneCameraEntity` 显示青色、`IsGameCamera=true` 显示橙色、选中时黄色、其他灰色
  - **线宽**：选中的摄像机线宽加粗（2.5px vs 1.5px）
  - **视锥体参数**：从 `CameraComponent` 读取实际的 FOV、Near、Far，而非硬编码
  - **深度控制**：Gizmo 关闭深度写入，不遮挡实体渲染

**文件变更**：
- `QL-Editor\src\EditorLayer.cpp`（增强 gizmo 渲染逻辑）

---

### E7-7: 场景层级分组功能

**目标**：场景层级支持分组（Folder），可收纳实体，可展开/折叠（与 Unity Folder 类似）。

**现状**：
- 实体以扁平列表展示，无层级结构
- 右键菜单只有"创建实体"和"删除选中"

**方案**：
- 新增 `SceneGroupComponent`（ECS 组件）：仅含 `bool Expanded = true`
- 实体的父子关系通过 `TransformComponent.ParentEntity` 实现（已有父子系统）
- 右键菜单新增"创建分组"、"移动到分组"
- `drawEntityNode` 支持树形递归渲染（父实体包含子实体）
- 分组实体在 Hierarchy 中显示文件夹图标，不可添加 SpriteRenderer 等渲染组件

**状态**：**已完成** ✅
- **完成时间**：2026-05-31
- **实现内容**：
  - `SceneGroupComponent` 已存在于 `Components.h`（Expanded 字段），修复了文件中的语法错误
  - `drawEntityNode` 支持树形递归渲染，通过 `TransformComponent.Children` 遍历子实体
  - **展开/折叠状态双向同步**：`ImGui::SetNextTreeNodeOpen` 读取 `SceneGroupComponent.Expanded` 设置初始状态，点击箭头切换时通过 `ImGui::IsItemToggledByClick` 同步回组件
  - **点击箭头不选中**：arrow-click 仅切换展开，`!ImGui::IsItemToggledByClick()` 分支处理选中逻辑
  - **分组颜色区分**：分组实体显示蓝色文字 (`0.4f, 0.7f, 1.0f`)，普通实体显示浅蓝色 (`0.7f, 0.7f, 1.0f`)
  - **移动到分组子菜单**：右键菜单中"移动到分组"罗列所有已有分组，支持将任意实体移入/移出分组
  - **解散分组**：移除 SceneGroupComponent 的同时清空 Children 列表，防止悬空指针
  - `SceneSerializer.cpp` 已支持 `SceneGroupComponent` 序列化/反序列化（Expanded 字段）

**文件变更**：
- `QuentlamEngine\src\Quentlam\Scene\Components.h`（修复语法错误）
- `QL-Editor\src\EditorLayer.cpp`（递归树渲染 + 展开状态同步 + 移动分组菜单）