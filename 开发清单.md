# QuentlamEngine 开发清单

## 目标
把 QuentlamEngine 发展成一个能够支持“星露谷”类型 2D 生活模拟游戏的通用引擎框架。

## 一、项目定位

QuentlamEngine 当前已经具备较完整的底层能力，包括：
- 2D / 3D 渲染
- ECS 与场景管理
- 2D 物理与碰撞
- 资源加载与线程池
- 编辑器与调试基础

但要支撑“星露谷”类型的项目，还需要补齐的是“游戏框架层”与“内容生产层”，而不是继续堆底层图形能力。

## 二、必须补齐的核心模块

### 1. `WorldGridModule`
职责：
- 负责格子地图、分层、区域、碰撞查询、地图切换。
- 负责世界坐标与格子坐标之间的转换。
- 负责地图触发器、传送门、区域边界与局部加载。

输出：
- `TileMap`
- `Region`
- `Portal`
- `Chunk`
- `GridQuery`

### 2. `AnimationModule`
职责：
- 负责 2D 精灵动画、方向状态、播放控制、事件帧。
- 负责角色、工具、作物、动物、门、特效等统一动画播放。

输出：
- `AnimationClip`
- `Animator`
- `SpriteAtlasBinding`
- `AnimationStateMachine`

### 3. `InteractionModule`
职责：
- 负责玩家与世界的交互入口。
- 负责格子选择、实体选择、触发器执行、上下文提示。

输出：
- `Interactable`
- `InteractionQuery`
- `ActionDispatcher`
- `InteractionPrompt`

### 4. `InventoryModule`
职责：
- 负责物品、堆叠、工具、容器、装备、交易、配方。
- 负责物品实例状态与定义数据分离。

输出：
- `ItemDef`
- `ItemStack`
- `InventorySlot`
- `Container`
- `Recipe`
- `ShopStock`

### 5. `SimulationModule`
职责：
- 负责时间、日历、天气、季节、日切换。
- 负责每日结算、事件广播、规则触发。

输出：
- `GameClock`
- `Calendar`
- `WeatherState`
- `SeasonRule`
- `DailyEventBus`

### 6. `NpcModule`
职责：
- 负责 NPC 日程、路径、关系、对话入口和状态记忆。
- 负责 NPC 的地点切换、行为切换与条件判断。

输出：
- `NpcSchedule`
- `PathGoal`
- `RelationshipState`
- `NpcMood`

### 7. `QuestEventModule`
职责：
- 负责任务、事件树、过场、日记、邮件。
- 负责条件触发与分支推进。

输出：
- `EventGraph`
- `QuestState`
- `CutsceneStep`
- `MailItem`
- `EventCondition`

### 8. `PersistenceModule`
职责：
- 负责存档、读档、版本迁移、局部保存、完整性校验。
- 负责把世界状态恢复为可运行的完整快照。

输出：
- `SaveSlot`
- `SaveSchema`
- `MigrationStep`
- `StateSerializer`
- `SaveValidator`

### 9. `UIGameModule`
职责：
- 负责运行时 UI、焦点、窗口栈、输入上下文。
- 负责游戏内界面与编辑器界面分离。

输出：
- `ScreenStack`
- `WidgetTree`
- `FocusManager`
- `InputContext`
- `CursorState`

### 10. `ContentPipelineModule`
职责：
- 负责资源导入、图集、校验、热加载、内容打包。
- 负责把编辑资源转换为运行时资源。

输出：
- `Importer`
- `AtlasPacker`
- `ContentManifest`
- `HotReloadWatcher`
- `AssetValidator`

### 11. `AudioModule`
职责：
- 负责音乐、音效、环境音、分层播放、淡入淡出。
- 负责事件驱动的音频播放控制。

输出：
- `AudioBus`
- `MusicController`
- `SfxEmitter`
- `AmbientZone`
- `AudioMixerState`

### 12. `LocalizationModule`
职责：
- 负责字符串表、语言切换、字体回退、文本格式化。
- 负责 UI 文本与内容文本的统一管理。

输出：
- `StringTable`
- `LocalizedText`
- `FontFallbackSet`
- `TextFormatter`

### 13. `DebugToolsModule`
职责：
- 负责地图检查、碰撞可视化、实体拾取、存档诊断。
- 负责开发期叠层、调试命令与校验输出。

输出：
- `MapInspector`
- `ColliderOverlay`
- `EntityPicker`
- `SaveValidator`
- `DebugConsole`

### 14. `ModdingModule`
职责：
- 负责内容包、扩展点、脚本桥接、加载顺序。
- 负责模组的发现、解析与安全加载。

输出：
- `ModLoader`
- `ContentPack`
- `ScriptBridge`
- `LoadOrderResolver`
- `ModManifest`

## 三、推荐目录结构

```text
QuentlamEngine/
  src/Quentlam/
    World/
    Gameplay/
    UI/
    Audio/
    Content/
    Persistence/
    DebugTools/
    Modding/
```

## 四、开发顺序

### 第一阶段：打地基
1. `WorldGridModule`
2. `TileMap` 数据结构
3. `AnimationModule`
4. `InteractionModule`
5. `PersistenceModule`

### 第二阶段：形成可玩性
1. `InventoryModule`
2. `SimulationModule`
3. `UIGameModule`
4. `NpcModule`
5. `QuestEventModule`

### 第三阶段：内容生产能力
1. `ContentPipelineModule`
2. `LocalizationModule`
3. `AudioModule`
4. `DebugToolsModule`

### 第四阶段：MOD 框架
1. `ModdingModule` + Lua 脚本引擎（见第九节）

## 五、最小可玩版本目标

做到以下内容后，才算进入“可玩原型”阶段：
- 玩家可以在网格地图上移动
- 玩家可以与地块或物体交互
- 玩家可以打开背包并拾取物品
- 游戏内有时间推进和天气变化
- 游戏可以存档和读档
- 至少有一个 NPC 和一条事件链

## 六、技术原则

- 玩法层必须数据驱动。
- 地图、物品、NPC、事件要尽量配置化。
- 存档必须从一开始就设计版本迁移。
- 编辑器要为内容生产服务，而不是只用于摆放实体。
- 渲染、物理、逻辑、UI 要职责分离。
- 优先做通用框架，再做具体玩法。

## 七、下一步建议

1. 先实现 `WorldGridModule` 和 `TileMap`。
2. 再实现 `AnimationModule` 和 `InteractionModule`。
3. 然后补 `PersistenceModule`。
4. 最后再扩展 UI、NPC、任务、内容管线。

---

## 八、P0 编辑器核心功能（第四轮开发，2026-05-28）

### 已完成

- **P0-1: Undo/Redo 系统（Command 模式）**
  - `EditorCommand.h/.cpp` - Command 基类、EditorCommandStack、CreateEntityCommand、DeleteEntityCommand、TransformCommand、BatchCommand
  - `Scene` 新增 `DestroyEntity()`、`GetEntity()` 方法
  - EditorLayer 集成 Ctrl+Z/Y 快捷键，Delete 键改用 CommandStack，右键菜单和菜单栏创建实体均通过 Command
  - Play 时自动清空撤销栈

- **P0-2: TileMap 视口绘制交互**
  - `TileMapEditorPanel` 新增 `HandleBrushStrokeContinuous()`、`HandleBrushStrokeContinuousReset()` 支持拖拽连续绘制
  - `EditorLayer` 视口鼠标点击/拖拽时将屏幕坐标转为世界坐标，调用 TileMap 笔刷绘制

- **P0-3: 资源拖放创建实体**
  - EditorLayer 已存在视口拖放逻辑（.png/.jpg/.obj/.fbx），已集成 CommandStack 创建

- **P0-4: Scene Hierarchy 搜索过滤**
  - Hierarchy 面板顶部添加搜索框，实时按 Tag 过滤实体列表（大小写不敏感）

- **P1-5: Shader/Material 选择器**
  - `MaterialDefaults` 新增 `GetAllDefaults()` 接口
  - Properties Panel SpriteRendererComponent 区域添加 Material 下拉选择器，支持默认材质和内置材质切换

- **P1-6: UI 编辑器实时同步**
  - `UIGameModule` 新增 `GetRegisteredScreens()` 访问器
  - `UIHierarchyPanel` 新增 `SetUIGameModule()` 接口，`OnImGuiRender()` 改为每帧重建树实现实时同步
  - EditorLayer 构造函数连接 `UIHierarchyPanel` 到 `UIGameModule`
  - 支持查看已注册但未激活的 Screen，提供 Push 按钮

- **P1-7: 完善 Ctrl+C/V**
  - EditorLayer 新增 `m_CopiedEntity` 成员，Ctrl+C 复制选中实体 ID，Ctrl+V 在原位偏移创建新实体并复制所有渲染组件
  - 粘贴后新实体向右上方偏移 0.5 单位，自动选中

- **P2-8: 多选支持**
  - `m_SelectedEntities` 支持 Shift+点击多选，清除历史选择
  - Hierarchy 空白处点击清空所有选中
  - 右键菜单支持"删除选中实体 (N)"批量删除，通过 BatchCommand 支持撤销

- **P2-9: 资源右键菜单**
  - Content Browser 文件/目录支持右键菜单：重命名（弹出输入框）、在资源管理器中显示（ShellExecute）、删除（带确认对话框）
  - EditorLayer 新增 `m_RenamingAsset`、`m_DeletingAsset`、`m_RenameBuffer` 等状态变量

- **P2-10: Shader 热编译**
  - MaterialEditorPanel 重构，`LoadShaderSourceFromFile()` 从磁盘解析 `#type vertex/fragment` 单文件 Shader
  - "从磁盘热重载"按钮：选中自定义材质后，修改 .glsl 文件点击按钮即可重新编译 Shader 并应用到材质
  - 编译错误信息显示在编辑器内

### 进行中

- **P3-1: Lua 源码引入** ✅
  - 将 Lua 5.4.6 源码引入 `vendor/lua/`（59 个核心源文件）

- **P3-3: Sol2 基础绑定** ✅
  - `LuaScriptBridge` 实现 `ScriptBridge` 接口
  - 绑定 `QL` 枚举（VERSION、ENGINE_NAME）
  - 绑定 `QL_Log`、`QL_LogWarn`、`QL_LogError` 调试函数
  - 支持 `load()`、`update(dt)`、`onSave()`、`onLoad()` 生命周期调用

- **P3-4: ModdingModule 框架** ✅
  - `ModLoader.cpp` 完整实现：manifest.json 解析（id/name/author/version/dependencies/content）
  - 目录扫描 `ScanModsDirectory()` 自动发现 mods 文件夹下的 MOD
  - 依赖排序（拓扑排序 + 循环检测）
  - MOD 启用/禁用/重载完整生命周期
  - `ModdingModule` 接入 Application：启动时扫描，关闭时清理
  - 示例 MOD：`Sandbox/mods/example-mod/`，包含 manifest.json + main.lua

- **P3-7: 示例 MOD + 文档** ✅
  - `docs/MOD开发指南.md` — 完整 MOD 开发文档
  - 涵盖目录结构、manifest.json 字段说明、生命周期函数、引擎 API
  - 包含开发流程、调试方法、发布步骤

---

## 九、MOD 框架：Lua + SWIG（P3 阶段）

### 背景

QuentlamEngine 为 C++ 引擎，需要脚本层支持 MOD 扩展。考虑到：
- Android 发布目标 → C# .NET 运行时依赖成本高，不可行
- Lua 轻量（~15KB，无运行时依赖）、Android NDK 原生支持、业界手游主流方案
- SWIG 提供半自动化绑定，减少手写胶水代码
- MOD 开发者上手门槛低（1-2 天可入门）

### 技术方案

```
C++ 引擎  ←(SWIG 绑定层)→  Lua 5.4 脚本
                              ↓
                         Sol2 C++ API
```

- **Lua**：运行时引擎，MOD 脚本执行环境
- **Sol2**：C++ 到 Lua 的现代绑定库，比手写绑定更清晰
- **SWIG**：辅助生成部分重复性绑定代码

### P3 开发任务

#### P3-1: Lua 源码引入
- 将 Lua 5.4 源码引入 `vendor/lua/` 目录
- Lua 5.4.tar.gz 解压后仅 4 个 `.c` + 2 个 `.h` 文件

#### P3-2: CMake 构建配置
- 在 QuentlamEngine CMakeLists.txt 中配置 Lua 编译为静态库
- 解决 Android NDK 交叉编译问题
- 验证 `lua_State*` 可用

#### P3-3: Sol2 引入与基础绑定
- 将 Sol2 引入 `vendor/sol2/`
- 绑定基础引擎 API：`Game`、`Scene`、`Entity`、`TransformComponent`
- 验证 Lua 脚本可读取/修改引擎对象

#### P3-4: ModdingModule 框架
- `ModManifest` — mod.json 清单解析（id、name、version、dependencies）
- `ModLoader` — MOD 目录扫描、依赖排序、加载顺序
- `ModSandbox` — 沙箱隔离（每个 MOD 独立 Lua state）

#### P3-5: 扩展点 API 绑定
- `Inventory` — `AddItem`、`RemoveItem`、`GetItem`、`GetItemCount`
- `GameClock` — `GetDay`、`GetSeason`、`AdvanceDay`
- `EventBus` — `Subscribe`、`Fire`、`Unsubscribe`
- `NpcModule` — `GetNpc`、`SetRelationship`、`GetRelationship`
- `QuestSystem` — `StartQuest`、`CompleteQuest`、`GetQuestState`

#### P3-6: MOD 生命周期管理
- MOD 初始化 `load()`
- 每帧更新 `update(dt)`
- 存档时 `onSave(context)`
- 读档时 `onLoad(context)`
- 引擎事件挂接（`OnDayStart`、`OnItemUsed`、`OnNpcTalked`）

#### P3-7: 示例 MOD + 文档
- 一个可运行的示例 MOD（添加新物品 + 新事件）
- MOD 开发文档（编写规范、API 列表、调试方法）

### MOD 目录结构

```
mods/
  my-mod/
    manifest.json          # MOD 元信息
    scripts/
      main.lua            # 入口脚本
    assets/
      textures/           # MOD 资源
      data/               # MOD 数据
```

### 开发顺序

1. P3-1 Lua 源码引入
2. P3-2 CMake 构建配置
3. P3-3 Sol2 基础绑定
4. P3-4 ModdingModule 框架
5. P3-5 扩展点 API 绑定
6. P3-6 MOD 生命周期
7. P3-7 示例 MOD + 文档