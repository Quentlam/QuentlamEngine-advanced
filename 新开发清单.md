# QuentlamEngine 开发清单（新版）

> **状态**: 第七轮开发完成，待整理编辑器集成
> **更新时间**: 2026-06-01
> **对比**: 与 `开发清单.md` 相比，新增了编辑器集成待办项，并更新了当前实际状态

---

## 一、项目完成状态总览

### 核心引擎模块（已完成 ✅）

| 模块 | 状态 | 备注 |
|------|------|------|
| ECS 实体/组件系统 | ✅ | entt 实现 |
| 场景序列化 | ✅ | JSON 格式，含 PrefabReferenceComponent |
| 2D/3D 渲染 | ✅ | Renderer2D/Renderer3D |
| 物理系统 2D/3D | ✅ | Box2D + 自定义 3D |
| 导航系统 | ✅ | NavigationModule + A* 寻路 |
| 粒子系统 2D | ✅ | ParticleSystem2D.h/.cpp |
| 动画系统 | ✅ | AnimationModule + SpriteAnimationComponent |
| 交互系统 | ✅ | InteractionModule + InteractableComponent |
| UI 实体系统 | ✅ | UIGameModule + UI 组件 |
| Lua 脚本系统 | ✅ | LuaScriptBridge + ModLoader |
| 场景管理器 | ✅ | SceneManager 多场景支持 |
| 音频系统 | ✅ | 框架已存在 |
| 网络同步 | ✅ | NetworkManager 框架 |

### 渲染增强模块（已完成 ✅）

| 模块 | 状态 | 备注 |
|------|------|------|
| 后处理管线 | ✅ | PostProcessing.h/.cpp，含 Bloom/ColorGrading/Vignette 等 |
| 2D 光照 | ✅ | Light2D.h/.cpp + Light2DRenderer.h/.cpp |
| LOD 系统 | ✅ | LODComponent.h/.cpp |
| 天空盒渲染 | ✅ | SkyRenderer.h/.cpp + SkyboxComponent |
| Sprite Atlas 打包 | ✅ | SpriteAtlasPacker.h/.cpp |
| Cubemap 天空 | ✅ | CubemapTexture.h/.cpp |

### 物理扩展（已完成 ✅）

| 模块 | 状态 | 备注 |
|------|------|------|
| 2D 物理关节 | ✅ | PhysicsJoints.h/.cpp（7 种关节类型） |
| 触发器系统 | ✅ | Collider isTrigger 支持 |
| 物理材质 | ✅ | PhysicsMaterial.h（2D/3D） |

### 内容生产与工具（已完成 ✅）

| 模块 | 状态 | 备注 |
|------|------|------|
| TileMap 编辑器 | ✅ | TileMapEditorPanel.h/.cpp |
| 导航网格编辑器 | ✅ | NavMeshEditorPanel.h/.cpp（存在但未集成） |
| 动画编辑器 | ✅ | AnimationEditorPanel.h/.cpp + AnimatorEditorPanel |
| 控制台面板 | ✅ | EditorConsolePanel.h/.cpp（存在但未集成） |
| Profiler 面板 | ✅ | ProfilerPanel.h/.cpp（存在但未集成） |
| 资源浏览器 | ✅ | AssetBrowserPanel.h/.cpp（存在但未集成） |
| 图集构建器 | ✅ | AtlasBuilderPanel.h/.cpp（存在但未集成） |
| 材质编辑器 | ✅ | MaterialEditorPanel.h/.cpp（已集成） |

### 框架与基础设施（已完成 ✅）

| 模块 | 状态 | 备注 |
|------|------|------|
| 输入系统重构 | ✅ | InputAction.h/.cpp（Action/ActionMap/Context） |
| 字体系统 | ✅ | FontAsset.h/.cpp |
| 资源 GUID 系统 | ✅ | AssetDatabase.h/.cpp + AssetMetaFile |
| UI 事件系统 | ✅ | EventSystem.h/.cpp |
| UI 渲染管线 | ✅ | UIRenderer.h/.cpp |
| UI 组件 | ✅ | Button/Text/Image/Toggle/Canvas/RectTransform |
| 构建自动化 | ✅ | BuildAutomation.h/.cpp |
| 植被实例化 | ✅ | VegetationSystem.h/.cpp |
| 场景分组 | ✅ | SceneGroupComponent（层级树形结构） |

---

## 二、待集成到编辑器的功能（最重要）

以下功能已有完整实现，但未集成到 `EditorLayer.cpp` 的 UI 中。

### E-I1: 控制台面板集成（EditorConsolePanel）
- **文件**: `QL-Editor/src/EditorConsolePanel.h/.cpp`
- **状态**: ✅ 实现完成，❌ 未集成到 EditorLayer
- **需要做**: 在 `OnImGuiLayer()` 中添加面板窗口，添加菜单项

### E-I2: Profiler 面板集成（ProfilerPanel）
- **文件**: `QL-Editor/src/ProfilerPanel.h/.cpp`
- **状态**: ✅ 实现完成，❌ 未集成到 EditorLayer
- **需要做**: 在 `OnImGuiLayer()` 中添加面板窗口，添加菜单项

### E-I3: 资源浏览器集成（AssetBrowserPanel）
- **文件**: `QL-Editor/src/AssetBrowserPanel.h/.cpp`
- **状态**: ✅ 实现完成，❌ 未集成到 EditorLayer
- **需要做**: 替换现有的旧 Content Browser 面板，添加菜单项

### E-I4: 动画编辑器集成（AnimationEditorPanel）
- **文件**: `QL-Editor/src/AnimationEditorPanel.h/.cpp`
- **状态**: ✅ 实现完成，❌ 未集成到 EditorLayer
- **需要做**: 在 `OnImGuiLayer()` 中添加面板窗口，添加菜单项

### E-I5: 动画状态机编辑器集成（AnimatorEditorPanel）
- **文件**: `QuentlamEngine/src/Quentlam/Gameplay/AnimatorEditorPanel.h/.cpp`
- **状态**: ✅ 实现完成，❌ 未集成到 EditorLayer
- **需要做**: 在 `OnImGuiLayer()` 中添加状态机可视化编辑器

### E-I6: 导航网格编辑器集成（NavMeshEditorPanel）
- **文件**: `QuentlamEngine/src/Quentlam/Gameplay/NavMeshEditorPanel.h/.cpp`
- **状态**: ✅ 实现完成，❌ 未集成到 EditorLayer
- **需要做**: 在 `OnImGuiLayer()` 中添加 NavMesh 烘焙面板，添加菜单项

### E-I7: 图集构建器集成（AtlasBuilderPanel）
- **文件**: `QL-Editor/src/AtlasBuilderPanel.h/.cpp`
- **状态**: ✅ 实现完成，❌ 未集成到 EditorLayer
- **需要做**: 在 `OnImGuiLayer()` 中添加面板窗口，添加菜单项

### E-I8: EditorLayer.h 同步更新
- **文件**: `QL-Editor/src/EditorLayer.h`
- **状态**: ❌ 严重过时（当前版本相当于 original_EditorLayer.h）
- **问题**: 缺少以下已实现功能的状态变量：
  - `m_ShowConsole` / `m_ShowProfiler` / `m_ShowAssetBrowser`
  - `m_ShowAnimationEditor` / `m_ShowAnimatorEditor` / `m_ShowAtlasBuilder` / `m_ShowNavMeshEditor`
  - `m_SceneViewportResolution` / `m_GameViewportResolution`
  - `m_CamControlActive` / `m_CamBaseSpeed`
  - `m_ShowSkybox`
  - `m_SplitRatio` / `m_SplitHovered` / `m_DraggingSplit`
  - `m_GameFramebuffer`
  - `SceneState::Step` 枚举值
  - Game/Split View 相关成员

---

## 三、编辑器核心体验完善（E7 系列）

### 已完成 ✅
| 任务 | 说明 |
|------|------|
| E7-1 | 相机移动速度 + 鼠标右键捕获 |
| E7-2 | 视口分辨率独立性（固定分辨率渲染） |
| E7-3 | 游戏摄像机在层级显示 + 绑定 API |
| E7-4 | 默认天空盒（SkyboxComponent） |
| E7-5 | 默认平行光（DirectionalLightComponent 同步） |
| E7-6 | 摄像机 Gizmo（视锥体 + 颜色编码） |
| E7-7 | 场景层级分组功能（树形 + 展开/折叠） |

### 待完成
无 — E7 系列全部完成

---

## 四、P5-P6 系列任务完成状态

### P5 系列（第五轮，编辑器基础）

| 任务 | 状态 | 备注 |
|------|------|------|
| P5-1: UI 实体纳入 ECS 层级 | ✅ | UIEntityComponent |
| P5-2: Prefab 系统 | ✅ | PrefabSystem + PrefabReferenceComponent |
| P5-3: 控制台面板 | ✅ | EditorConsolePanel（待集成） |
| P5-4: 编辑器 Profiler | ✅ | ProfilerPanel（待集成） |
| P5-5: Physics2D 完善 | ✅ | 碰撞体/关节/射线 |
| P5-6: 粒子系统 | ✅ | ParticleSystem2D |
| P5-7: 音频与 ECS 集成 | ✅ | AudioSource/AudioListener Component |
| P5-8: Camera2D 跟随 | ✅ | CameraFollowComponent |
| P5-9: 音效音乐统一接口 | ✅ | Lua API 完整 |
| P5-10: Sprite Sheet 动画编辑器 | ✅ | AnimationEditorPanel（待集成） |
| P5-11: 资源 Asset Browser 重构 | ✅ | AssetBrowserPanel（待集成） |
| P5-12: TileMap 图层编辑增强 | ✅ | 擦除/填充/选择工具 |

### P6 系列（第六轮，核心能力）

| 任务 | 状态 | 备注 |
|------|------|------|
| P6-1: 动画系统 | ✅ | AnimationModule + 状态机 |
| P6-2: 交互系统 | ✅ | InteractionModule |
| P6-3: Game/Split View | ✅ | 分离视图（待完善 EditorLayer.h） |
| P6-4: 粒子系统 | ✅ | ParticleSystem2D |
| P6-5: 导航系统 | ✅ | NavigationModule |
| P6-6: 场景流加载 | ✅ | SceneManager 完整实现 |
| P6-7: 帧步进调试 | ✅ | SceneState::Step（待完善 EditorLayer.h） |
| P6-8: Sprite Atlas 打包 | ✅ | SpriteAtlasPacker |

---

## 五、P0-P4 阶段完成状态

| 阶段 | 任务 | 状态 |
|------|------|------|
| P0-A | Prefab 实例化系统 | ✅ |
| P0-B | UI 控件系统 | ✅ |
| P0-C | 2D 射线检测 | ✅ |
| P0-D | 导航网格编辑器 | ✅（NavMeshEditorPanel 存在） |
| P1-A | 动画状态机 | ✅（AnimatorController 完整实现） |
| P1-B | 资源 GUID 引用系统 | ✅（AssetDatabase + AssetMetaFile） |
| P1-C | 输入系统重构 | ✅（InputAction 完整实现） |
| P1-D | 字体 + 国际化 | ✅（FontAsset 完整实现） |
| P2-A | 后处理管线 | ✅（PostProcessing 完整实现） |
| P2-B | 2D 光照 | ✅（Light2D + Light2DRenderer 完整实现） |
| P2-C | LOD 系统 | ✅（LODComponent 完整实现） |
| P3-A | 地形系统 | ⚠️ 框架存在，待完善 |
| P3-B | 植被实例化 | ✅（VegetationSystem 完整实现） |
| P4-A | 2D 物理关节 | ✅（7 种关节类型） |
| P4-B | 触发器 + 物理材质编辑器 | ✅ |
| P5-A | 网络同步基础 | ✅（NetworkManager 框架完整） |
| P5-B | 构建自动化 | ✅（BuildAutomation 完整实现） |

---

## 六、Lua + MOD 框架

| 任务 | 状态 | 备注 |
|------|------|------|
| P3-1: Lua 5.4 源码引入 | ✅ | `vendor/lua/` |
| P3-3: Sol2 基础绑定 | ✅ | 完整 API 绑定 |
| P3-4: ModdingModule 框架 | ✅ | ModLoader 完整实现 |
| P3-7: 示例 MOD + 文档 | ✅ | example-mod + snake-game |

---

## 七、UI 组件完成状态

| 组件 | 状态 | 备注 |
|------|------|------|
| CanvasComponent | ✅ | Screen Space / World Space |
| RectTransformComponent | ✅ | Anchors / Pivot / Offset / SizeDelta |
| ImageComponent | ✅ | Sprite / Color / Fill |
| TextComponent | ✅ | 文字渲染 |
| ButtonComponent | ✅ | 过渡动画 + Lua 回调 |
| ToggleComponent | ✅ | 选中状态 |
| EventSystem | ✅ | PointerEnter/Click/Drag/Select |

---

## 八、尚需完善的功能

### 8.1 地形系统（P3-A）
- **现状**: `TerrainComponent` 框架存在，但编辑器面板 `TerrainEditorPanel` 不存在
- **需做**: 创建 `TerrainEditorPanel.h/.cpp`，实现 Raise/Lower/Smooth/Flatten 地形刷工具

### 8.2 场景分组编辑器集成
- **现状**: `SceneGroupComponent` 存在，`drawEntityNode` 有递归树形渲染
- **需做**: 右键菜单"创建分组"、"移动到分组"、"解散分组"已在 EditorLayer.cpp 中实现
- **待验证**: 需确认编辑器中实际可用

### 8.3 2D 光照实际渲染集成
- **现状**: `Light2D.h/.cpp` 和 `Light2DRenderer.h/.cpp` 存在
- **需做**: 在 `Scene::OnUpdateRuntime` 中调用光照渲染 pass

### 8.4 后处理实际渲染集成
- **现状**: `PostProcessing.h/.cpp` 存在
- **需做**: 在 `Scene::Render` 或 `Renderer2D/Renderer3D` 中集成后处理 pass

---

## 九、开发顺序建议

### 立即行动（Step 0）
1. **更新 `EditorLayer.h`** — 补充所有缺失的成员变量和枚举值
2. **集成 E-I1 ~ E-I7** — 将已实现的面板全部接入 EditorLayer.cpp
3. **验证 E7 系列** — 确认所有第七轮功能在编辑器中实际可用

### 验证阶段
4. 逐一测试已集成的面板是否正常工作
5. 确认 Game/Split View 模式可用
6. 确认帧步进调试可用

### 完善阶段
7. 地形编辑器（TerrainEditorPanel）
8. 2D 光照渲染 Pass 集成
9. 后处理渲染 Pass 集成

---

## 十、技术债务

| 债务项 | 现状 | 清理方案 |
|--------|------|----------|
| EditorLayer.h 过时 | ❌ 缺少大量成员变量 | 同步更新 |
| EditorConsolePanel 未集成 | ❌ 存在但不可见 | 添加到 OnImGuiLayer |
| ProfilerPanel 未集成 | ❌ 存在但不可见 | 添加到 OnImGuiLayer |
| AssetBrowserPanel 未集成 | ❌ 存在但不可见 | 替换 Content Browser |
| AnimationEditorPanel 未集成 | ❌ 存在但不可见 | 添加到窗口菜单 |
| AnimatorEditorPanel 未集成 | ❌ 存在但不可见 | 添加到窗口菜单 |
| NavMeshEditorPanel 未集成 | ❌ 存在但不可见 | 添加到窗口菜单 |
| AtlasBuilderPanel 未集成 | ❌ 存在但不可见 | 添加到窗口菜单 |
| TerrainEditorPanel 不存在 | ❌ 框架有，无编辑器 | 新建 TerrainEditorPanel |
| 2D 光照 Pass 缺失 | ⚠️ 渲染器有，Pass 未集成 | 在 Scene 渲染循环中调用 |
| 后处理 Pass 缺失 | ⚠️ 有实现，Pass 未集成 | 在 Scene 渲染循环中调用 |

---

## 十一、EditorLayer.h 缺失成员清单

以下成员变量和枚举值需要在 `EditorLayer.h` 中补充：

```cpp
// SceneState 枚举缺少 Step
enum class SceneState { Edit = 0, Play = 1, Pause = 2, Step = 3 };

// Game/Split View 相关
Ref<FrameBuffer>  m_GameFramebuffer;
glm::vec2         m_GameViewportSize{ 0.0f, 0.0f };
glm::vec2         m_SceneRenderSize{ 1280.0f, 720.0f };
glm::vec2         m_GameRenderSize{ 1280.0f, 720.0f };
float             m_SplitRatio = 0.5f;
bool              m_SplitHovered = false;
bool              m_DraggingSplit = false;

// 视口布局
enum class ViewportLayout { SceneOnly, GameOnly, SplitH, SplitV };
ViewportLayout    m_ViewportLayout = ViewportLayout::SceneOnly;

// 相机控制
bool              m_CamControlActive = false;
float             m_CamBaseSpeed = 10.0f;

// 天空盒
bool              m_ShowSkybox = true;

// 编辑器面板显示开关
bool              m_ShowConsole = true;
bool              m_ShowProfiler = true;
bool              m_ShowAssetBrowser = true;
bool              m_ShowAnimationEditor = false;
bool              m_ShowAnimatorEditor = false;
bool              m_ShowAtlasBuilder = false;
bool              m_ShowNavMeshEditor = false;

// 视口分辨率
enum class ViewportResolution { Fixed1280x720, Fixed1920x1080, Fixed2560x1440, WindowSize };
ViewportResolution m_SceneViewportResolution = ViewportResolution::Fixed1280x720;
ViewportResolution m_GameViewportResolution = ViewportResolution::Fixed1280x720;

// Game View 工具栏按钮图标
Ref<Texture2D>    m_IconStep;
Ref<Texture2D>    m_IconPrev;

// 编辑器面板实例
EditorConsolePanel    m_ConsolePanel;
ProfilerPanel         m_ProfilerPanel;
AssetBrowserPanel     m_AssetBrowserPanel;
AnimationEditorPanel  m_AnimationEditorPanel;
// AnimatorEditorPanel  m_AnimatorEditorPanel; // 如果在 QL-Editor 中
AtlasBuilderPanel     m_AtlasBuilderPanel;
// NavMeshEditorPanel  m_NavMeshEditorPanel; // 如果在 QL-Editor 中
```

---

## 十二、EditorLayer.cpp 待补充面板调用

在 `OnImGuiLayer()` 的面板渲染区域（大约 2400-2450 行附近）需要补充以下面板调用：

```cpp
// 控制台面板
if (m_ShowConsole) m_ConsolePanel.OnImGuiRender();

// Profiler 面板
if (m_ShowProfiler) m_ProfilerPanel.OnImGuiRender();

// 资源浏览器（替换旧 Content Browser）
if (m_ShowAssetBrowser) m_AssetBrowserPanel.OnImGuiRender();

// 动画编辑器
if (m_ShowAnimationEditor) m_AnimationEditorPanel.OnImGuiRender();

// 动画状态机编辑器
if (m_ShowAnimatorEditor) m_AnimatorEditorPanel.OnImGuiRender();

// 图集构建器
if (m_ShowAtlasBuilder) m_AtlasBuilderPanel.OnImGuiRender();

// 导航网格编辑器
if (m_ShowNavMeshEditor) m_NavMeshEditorPanel.OnImGuiRender();
```

同时在"窗口"菜单中添加对应的菜单项。

---

## 十三、EditorLayer.h 更新优先级

**高优先级（阻塞其他功能）:**
1. 补充 `SceneState::Step` 枚举
2. 补充 Game/Split View 相关成员
3. 补充所有 `m_Show*` 面板开关 bool
4. 补充所有面板实例成员
5. 添加 `m_IconStep` / `m_IconPrev` 图标

**中优先级（体验相关）:**
6. 补充 `m_CamControlActive` / `m_CamBaseSpeed`
7. 补充 `m_ShowSkybox`
8. 补充视口分辨率相关

---

*本文档由开发清单对比分析生成，生成时间: 2026-06-01*
