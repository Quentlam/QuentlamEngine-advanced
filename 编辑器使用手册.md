# QL-Editor 使用手册

> 版本：1.3（2026-05-28）
> 适用：QuentlamEngine 2D 生活模拟游戏开发

---

## 一、概述

QL-Editor 是 QuentlamEngine 的可视化编辑器，用于在游戏运行时之前设计场景、实体、UI 和地图。

### 编辑器架构

```
游戏运行模式分离：
  Edit Mode  → 在编辑器中设计场景（Inspector/Viewport/Hierarchy）
  Play Mode  → 运行游戏（Scene runtime, physics, scripts active）
  Pause     → 暂停运行，inspect 游戏状态
```

编辑器基于 Dear ImGui 实现多窗口停靠布局，所有面板通过 ImGui 的 `Begin/End` API 构建。

### 启动编辑器

```
bin/Debug-windows-x86_64/QL-Editor/QL-Editor.exe
```

首次启动会自动创建一个空白场景。

---

## 二、界面导览

### 2.1 工具栏（Toolbar）

顶部中央区域，包含：

| 元素 | 功能 |
|------|------|
| Play 按钮 | 进入 Play Mode（绿色三角） |
| Pause 按钮 | 暂停运行（双竖线） |
| Stop 按钮 | 停止运行，返回 Edit Mode（方形） |
| Gizmo 选择 | Q/W/E/R 对应 None/Translate/Rotate/Scale |

### 2.2 菜单栏

| 菜单 | 内容 |
|------|------|
| 文件 | 新建场景 / 保存 / 另存为 / 打开 / 退出 |
| 窗口 | 面板开关（Content Browser、UI Hierarchy、Canvas Editor、TileMap Editor、Material Editor、Physics Colliders） |
| 游戏对象 | 3D 对象创建 / 空对象 / 2D 精灵 / 复制对象 |
| 组件 | 添加组件到选中实体（如 Sprite Animation） |

### 2.3 场景层级（Scene Hierarchy）

左侧面板，显示场景中所有实体。顶部有搜索框，支持按名称实时过滤。支持 **多选**：点击选中一个，**Shift+点击** 添加或移除选中。Hierarchy 空白处点击清空所有选中。点击选中实体会在右侧 Properties 面板显示其属性。右键菜单支持创建实体、批量删除（选中多个时显示"删除选中实体 (N)"）。按 Delete 键删除当前实体（支持撤销）。

### 2.4 属性面板（Properties Panel）

右侧面板，显示选中实体的所有组件属性。核心组件包括：

- **Transform** — 位置/旋转/缩放（可通过 Gizmo 交互编辑）
- **Sprite Renderer (SpriteTransformComponent)** — 2D 精灵颜色
- **Sprite Renderer2 (SpriteRendererComponent)** — 高级2D精灵，支持颜色、Tiling、Flip、Material
- **Sprite Animation** — 精灵动画控制（Play/Stop/Pause）
- **Triangle Renderer** — 2D 三角形
- **Cube Renderer / Primitive Renderer** — 3D 渲染
- **Rigidbody 2D/3D、Box Collider 2D/3D** — 物理碰撞体

底部有 **添加组件** 按钮，弹出组件选择菜单。

### 2.5 视口（Viewport）

中央主画布，显示场景渲染画面。点击实体选中，ImGuizmo 控制 Transform。支持 2D/3D 相机切换。

### 2.6 内容浏览器（Content Browser）

显示 `assets/` 目录下的所有资源文件（纹理、Shader、JSON 数据等）。支持拖拽到 Viewport 创建实体。

---

## 三、场景编辑

### 3.1 创建实体

**方法一：菜单**
- 游戏对象 / 3D 对象 / 立方体（Sphere / Cylinder / Capsule / Cone / Torus）
- 游戏对象 / 创建空对象
- 游戏对象 / 2D 对象 / 精灵（Sprite）

**方法二：Content Browser 拖拽**
- 将 `.png` 纹理拖入 Viewport，自动创建带 `SpriteRendererComponent` 的实体

### 3.2 Transform 操作

选中实体后，使用 ImGuizmo 工具：

| 快捷键 | 工具 | 功能 |
|--------|------|------|
| W | Translate | 移动实体 |
| E | Rotate | 旋转实体 |
| R | Scale | 缩放实体 |
| Q | None | 仅选中 |
| F | Frame | 将选中实体居中视图 |

按住 **Ctrl** 可启用吸附（Snap）。

### 3.3 实体操作

- **删除**：Scene Hierarchy 右键菜单 → 删除实体 / Delete 键（**支持撤销 Ctrl+Z**）
- **多选**：Scene Hierarchy 中 Shift+点击 多个实体，右键批量删除
- **复制**：游戏对象菜单 → 复制对象，或 Ctrl+C 复制 + Ctrl+V 粘贴
- **重命名**：Scene Hierarchy 中双击实体名称
- **撤销**：Ctrl+Z（撤销最近一次操作）
- **重做**：Ctrl+Y（重做撤销的操作）

---

## 四、2D 渲染与动画

### 4.1 SpriteRendererComponent 属性

选中带 `SpriteRendererComponent` 的实体，在 Properties 面板中：

| 属性 | 说明 |
|------|------|
| Color | 精灵颜色（RGBA） |
| Size | 精灵尺寸 |
| Tiling Factor | 纹理平铺系数 |
| Flip X / Y | 水平/垂直翻转 |
| Material | 材质选择（可从下拉菜单选择默认材质或自定义材质） |

### 4.2 精灵动画

**添加动画组件**：选中实体 → 组件菜单 → 渲染 / 精灵动画

**动画组件属性**：

| 属性 | 说明 |
|------|------|
| Auto Play | 启动时自动播放 |
| State | 当前播放状态（Playing / Stopped） |
| Current Frame | 当前帧索引 |
| Clip | 当前动画片段名称 |
| Play / Stop / Pause 按钮 | 编辑器内预览动画 |

**动画数据加载**：通过 `AnimationLoader::LoadAnimationData()` 从 JSON 文件加载。需要提供 Atlas 纹理路径和帧索引数据。

### 4.3 动画 JSON 格式

```json
{
    "atlas": "assets/textures/player.png",
    "columns": 8,
    "rows": 4,
    "frameWidth": 64,
    "frameHeight": 64,
    "clips": [
        {
            "name": "idle",
            "fps": 8,
            "wrapMode": "Loop",
            "frames": [0, 1, 2, 3]
        },
        {
            "name": "walk",
            "fps": 12,
            "wrapMode": "Loop",
            "frames": [4, 5, 6, 7, 8, 9]
        }
    ]
}
```

---

## 五、Shader 和材质

### 5.1 材质编辑器（Material Editor）

**打开**：窗口菜单 → 材质编辑器

界面分为左右两栏：
- **左侧**：内置材质列表（DefaultSprite / FlatColor）+ 自定义材质创建区
- **右侧**：材质属性编辑 + Shader 信息

**创建新材质**：
1. 输入 Shader 路径（如 `assets/shaders/Texture2DShader.glsl`）
2. 输入材质名称
3. 点击 Create

### 5.2 当前 Shader 架构

引擎目前采用**硬编码 Shader** 策略：

| Shader 文件 | 用途 |
|-------------|------|
| `Texture2DShader.glsl` | 2D 精灵批渲染（主渲染器） |
| `CubeShader.glsl` | 3D 立方体/图元（带 Blinn-Phong 光照） |
| `ModelShader.glsl` | 3D 模型加载 |
| `FlatColorShader.glsl` | 纯色 2D 渲染 |
| `BlueShader.glsl` | 纯蓝 2D 渲染 |
| `OutlineShader.glsl` | 编辑器选中共用（后处理） |

**可配置的渲染参数**（通过组件属性）：
- 2D 精灵：Color、Tiling Factor、Flip X/Y
- 3D 图元：Color、Ambient、Diffuse、Specular、Shininess

### 5.3 材质绑定流程

每个 `SpriteRendererComponent` 可选引用一个 `Material`。若未引用，引擎自动使用 `MaterialDefaults::GetSpriteMaterial()`（基于 `Texture2DShader.glsl`）。

---

## 六、TileMap 编辑

### 6.1 打开 TileMap 编辑器

**窗口**菜单 → **瓦片地图编辑器**

### 6.2 瓦片调色板

面板左侧显示 6 种地块类型：

| 地块类型 | 颜色 | 用途 |
|----------|------|------|
| Grass | 绿色 | 草地 |
| Water | 蓝色 | 水体 |
| Sand | 米色 | 沙地 |
| Stone | 灰色 | 石地 |
| Wood | 棕色 | 木质 |
| Solid | 暗色 | 碰撞固体 |

### 6.3 画笔工具

- **画笔大小**：设置每次涂抹的瓦片半径（1x1 / 2x2 / 3x3 / 5x5）
- **地图设置**：图集纹理路径、瓦片大小、地图尺寸
- **视口绘制**：在调色板中选择地块类型，在 Viewport 空白区域**点击放置**地块，**按住鼠标左键拖拽**可连续绘制
- **右键**：可切换为橡皮擦（放置 Empty 地块）

---

## 七、UI 编辑

### 7.1 UI 层级面板（UI Hierarchy Panel）

**窗口**菜单 → **UI 层级面板**

以树形视图显示所有注册的 UI Screen 及控件层级结构。支持选择控件并跳转编辑。

### 7.2 UI 画布编辑器（UI Canvas Editor）

**窗口**菜单 → **UI 画布编辑器**

- Screen 列表：选择要编辑的 Screen
- Canvas 预览：显示 Screen 的控件布局
- 属性面板：编辑选中控件的位置、尺寸等属性
- 支持缩放和拖拽导航，右键重置视图

---

## 八、资源管理

### 8.1 Content Browser 使用

- 显示 `assets/` 目录树
- 根据文件扩展名显示不同图标（.png / .glsl / .json / .wav）
- 双击文件可在外部编辑器打开
- 右键菜单：删除、重命名

### 8.2 资源导入

将资源文件（.png、.json、.glsl）放入 `assets/` 目录，编辑器下次启动后 Content Browser 会自动显示。

### 8.3 动画资源

1. 准备 Sprite Sheet 纹理（包含所有帧的拼合图）
2. 编写 JSON 动画配置文件（格式见 4.3 节）
3. 将 JSON 放入 `assets/data/animations/` 目录
4. 在编辑器中为实体添加 Sprite Animation 组件后，通过代码加载 JSON

---

## 九、场景保存与加载

### 9.1 保存场景

**文件**菜单 → **保存场景**（Ctrl+S）或 **另存为...**

场景以 YAML 格式保存在 `scenes/` 目录。

### 9.2 打开场景

**文件**菜单 → **打开场景**（Ctrl+O）

### 9.3 Play Mode 预览

- 点击工具栏 **Play** 进入 Play Mode，场景实时运行
- 物理系统（Box2D / JoltPhysics）、NPC 日程、任务系统、音频全部激活
- 点击 **Pause** 可暂停，检查游戏状态
- 点击 **Stop** 返回 Edit Mode

---

## 十、快捷键参考

| 快捷键 | 功能 |
|--------|------|
| W / E / R | Transform / Rotate / Scale Gizmo |
| Q | 取消 Gizmo |
| F | Frame Selected（居中选中实体） |
| Delete | 删除选中实体（支持撤销） |
| Ctrl+S | 保存场景 |
| Ctrl+O | 打开场景 |
| Ctrl+N | 新建场景 |
| Ctrl+Shift+A | 快速添加面板 |
| Ctrl+Z | 撤销（Undo） |
| Ctrl+Y | 重做（Redo） |

---

## 十一、组件参考

### 渲染组件

| 组件 | 说明 |
|------|------|
| `SpriteTransformComponent` | 基础 2D 精灵（仅颜色） |
| `SpriteRendererComponent` | 高级 2D 精灵（Texture + Tiling + Material） |
| `SpriteAnimationComponent` | 精灵帧动画 |
| `TriangleRendererComponent` | 2D 三角形 |
| `CubeRendererComponent` | 3D 立方体（支持光照参数） |
| `PrimitiveRendererComponent` | 3D 图元（Sphere/Cylinder/Capsule/Cone/Torus） |

### 物理组件

| 组件 | 说明 |
|------|------|
| `Rigidbody2DComponent` | 2D 刚体（Static/Dynamic/Kinematic） |
| `BoxCollider2DComponent` | 2D 盒碰撞体 |
| `TriangleCollider2DComponent` | 2D 三角碰撞体 |
| `Rigidbody3DComponent` | 3D 刚体（JoltPhysics） |
| `BoxCollider3DComponent` | 3D 盒碰撞体 |

---

## 十二、已知限制

- 动画组件需要在运行时通过代码加载 JSON 初始化（编辑器内编辑动画 JSON 路径待完善）
- 所有 P0/P1/P2 编辑器功能均已实现（P0-1~P0-4，P1-5~P1-7，P2-8~P2-10）
